## Current Work Order

Use this section as the active checklist for the current desktop-client pass. Do not jump to a lower item while a higher item has an unresolved correctness or usability blocker.

- [x] Resolve the `develop` branch split before release.
  Required behavior: the feature branch must either contain only desktop/sync/SDK work that is intentionally separate from `develop`, or every server/shared/main-application change must go through a separate reviewed transfer to `develop` before the desktop branch is considered clean.
  Current status: resolved. `origin/develop` is merged into this branch, the main docker workflow matches `origin/develop`, and `git diff origin/develop..HEAD -- src/Cotton.Server src/Cotton.Shared .github/workflows src/Cotton.sln` is now limited to desktop/release wiring: `.github/workflows/desktop-sync.yml` and `src/Cotton.sln`.
  Partial 2026-06-08: desktop release/package smoke steps were moved out of `.github/workflows/docker-image.yml` into `.github/workflows/desktop-sync.yml`; `docker-image.yml` was verified to match `origin/develop`.
  Verification 2026-06-08: `git diff --name-status origin/develop..HEAD -- src/Cotton.Server src/Cotton.Shared src/Cotton.sln .github/workflows` returned only `.github/workflows/desktop-sync.yml` and `src/Cotton.sln`. The solution diff adds SDK, sync core, sync app, desktop, CLI, and their test projects. The workflow diff is a standalone desktop package smoke pipeline. No server/shared transfer remains open from this branch.
- [ ] Stabilize sync controls during active work.
  Required behavior: global pause/resume is available while synchronization is running, disabling one pair does not resume other pairs after a global pause, and paused state survives application restart.
  Current status: code-side command/state audit is done; keep open for Windows two-pair manual verification.
  Audit 2026-06-08: durable global pause is already implemented through `AppPreferences.IsSyncPaused`; `PauseAllAsync`/`ResumeAllAsync` save the preference, `StartSyncCoreAsync` reloads it and starts the supervisor paused when needed, and `SaveSyncPairAsync` reapplies the loaded global pause after a sync-core restart. Do not reimplement pause persistence unless a Windows restart check disproves this behavior.
  Verification 2026-06-08: focused `Cotton.Sync.App.Tests` filter `SaveSyncPairAsync_ReappliesGlobalPauseWhenSyncCoreRestarts` passed 1/1; focused `Cotton.Sync.Desktop.Tests` filter `ToggleSelectedSyncPairEnabledCommand_KeepsOtherPairsPausedWhenDisablingDuringGlobalPause|GlobalControls_RemainAvailableWhileManualSyncIsRunning|PauseResumeCommands_AreMutuallyAvailable|PauseResumeCommand_ShowsPausingWhilePauseRequestIsRunning` passed 4/4. This proves the current code preserves global pause through sync-core restart and keeps neighboring enabled rows paused when one pair is disabled while globally paused. Windows native tray/menu behavior remains open.
  Verification: focused command/state tests plus Windows manual check with two sync pairs.
- [ ] Stabilize dashboard progress.
  Required behavior: the top progress card represents the whole active sync cycle, not only the current file; progress does not reset on every file; speed and ETA use smoothed global samples and do not flicker; many-small-file sync still shows useful global file-rate/ETA.
  Current status: global file-rate smoothing is implemented, run-level byte and file remaining-time predictions now dampen raw ETA changes instead of recomputing jumpy estimates on every sample, and the refreshed Linux visual-smoke progress screen shows aggregate multi-folder progress; keep open for animated Windows verification and any remaining global-progress corrections.
  Audit 2026-06-08: first-upload large-file prehash is already removed in the sync-core path: metadata-only local scans feed local-only uploads without a precomputed content hash, and `SdkRemoteFileSynchronizer` computes the full content hash in the same sequential chunk-upload stream used to build the server manifest. Do not add another first-upload hashing pipeline; remaining large-file delay should be investigated as scan/reconciliation/baseline/remote I/O evidence, not assumed to be a missing one-pass upload feature.
  Partial 2026-06-08: the top progress card now feeds speed/ETA from aggregate run-progress bytes as well as active transfer bytes. This keeps global byte speed and remaining-time visible for many-small-file syncs where individual file transfers are too short to produce useful per-file estimates, and ignores an initial zero-byte sample so stale idle time does not poison the estimate.
  Partial 2026-06-08: long local content checks now report byte-level `Hash` progress through the existing live file-progress pipeline. Desktop rows and the top progress card render this as `Checking file.ext`, so baseline comparison no longer leaves the UI apparently frozen on `Checking files` while SHA-256 reads a large local file.
  Partial 2026-06-08: per-file transfer speed/ETA now waits for a meaningful byte delta before showing estimates. For known file sizes this means at least 1% of the file, capped at 1 MiB; for unknown totals it waits for 64 KiB. This prevents huge noisy first estimates on large files while preserving estimates for small files and leaving global run file-rate/ETA intact.
  Visual verification 2026-06-08: refreshed Linux Xvfb screenshot smoke from a fresh Release `linux-x64` publish captured `/tmp/cotton-sync-linux-verify-20260608/cotton-sync-desktop-linux-progress.png`. The reviewed progress state shows `Syncing 2 folders`, aggregate file count, right-aligned global size/speed, stable top-card height, and active per-folder operation rows without the old dangling `Idle` text.
  Verification 2026-06-08: focused `Cotton.Sync.App.Tests` filter `AppTransferProgressEstimatorTests` passed 11/11; focused `Cotton.Sync.Desktop.Tests` filter `RunProgressChanged_|TransferProgressChanged_` passed 36/36; `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --no-restore` passed with 0 warnings.
  Verification 2026-06-08: focused `Cotton.Sync.Tests` filter `ComputeContentHashAsync_ReportsHashProgress|RunOnceAsync_ReportsLocalHashProgressWhenCheckingBaselineFile` passed 2/2; focused `Cotton.Sync.Desktop.Tests` filter `TransferProgressChanged_ShowsHashProgressAsChecking|TransferProgressChanged_UpdatesCurrentTransferState|GlobalControls_RemainAvailableWhileManualSyncIsRunning` passed 3/3; `dotnet build src/Cotton.Sync/Cotton.Sync.csproj --no-restore` passed with 0 warnings; `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --no-restore` passed with 0 warnings.
  Verification 2026-06-08: focused `Cotton.Sync.Desktop.Tests` filter `TransferProgressChanged_|RunProgressChanged_` passed 34/34; `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --no-restore` passed with 0 warnings.
  Partial 2026-06-07: refreshed Linux visual-smoke from a fresh desktop publish at `/tmp/cotton-sync-linux-walkthrough-20260607-r2`; the progress screen shows `Syncing 2 folders`, aggregate file count, transfer count, right-aligned size/speed, and per-folder active rows without clipping.
  Partial 2026-06-07: global file-rate/ETA can now appear after one second of many-file reconciliation instead of waiting at least three seconds, so large trees with many small files get useful top-card progress sooner. Verification: focused `Cotton.Sync.Desktop.Tests` filter `RunProgressChanged_ShowsGlobalFileRate` passed 2/2; `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --no-restore` passed with 0 warnings after rerunning separately from the focused test to avoid a transient parallel `.pdb` file lock.
  Partial 2026-06-07: aggregate many-file speed/ETA now stays visible when a second active sync pair increases the top-card file total, instead of clearing the estimate until another sample window fills. Verification: focused `Cotton.Sync.Desktop.Tests` filter `RunProgressChanged_KeepsGlobalFileRateWhenAggregateTotalGrows|RunProgressChanged_ShowsGlobalFileRate` passed 3/3.
  Partial 2026-06-07: the top work-progress card now stays cycle-focused while run progress is active and no longer adds per-file secondary transfer text such as `Uploading report.txt`, `Downloading clip.mp4`, or `2 files transferring`; per-file operation text remains on folder rows. This reduces header height churn and file-to-file text flicker during large sync cycles. Verification: focused `Cotton.Sync.Desktop.Tests` filter `ApplyVisualSmokeScenarioAsync_ShowsProgressCards|TransferProgressChanged_KeepsAggregateRunProgressPrimaryForMultipleFolders|TransferProgressChanged_KeepsRunProgressPrimaryForOneFolder|TransferProgressChanged_KeepsGlobalRunByteProgressPrimaryForOneFolder|TransferProgressChanged_AggregatesHeaderMetricsForMultipleActiveTransfers|TransferProgressChanged_UsesGlobalFileRateWhenActiveTransferHasNoByteRate` passed 6/6; `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --no-restore` passed with 0 warnings.
  Verification: focused view-model tests plus Windows manual check with many small files and at least one large file.
- [ ] Fix folder-list usability.
  Required behavior: folder rows use compact status dots/icons instead of dangling status text, expanded row actions remain reachable in a small window, the folder list scrolls independently, and Activity does not consume the primary folder-management area unless explicitly opened.
  Current status: layout-side fixes are implemented, and the refreshed Linux visual-smoke folder-controls screen shows the expanded editor reachable with Activity hidden; keep open for Windows visual verification with multiple sync pairs.
  Audit 2026-06-08: current XAML already has `SyncPairsScrollViewer`, Activity hidden by default behind the history button, and Activity capped at `MaxHeight="150"`. Do not add another folder-list container unless Windows verification disproves this layout.
  Partial 2026-06-08: the sync metadata folder name is centralized in `SyncMetadataDirectory`; writer-created `.cotton-sync` folders are hidden on Windows, and existing metadata folders are re-hidden when a sync root is probed. This keeps local sync roots cleaner in Explorer without changing Linux behavior. Keep the Windows Explorer visibility check in the Windows-only work list.
  Visual verification 2026-06-08: refreshed Linux Xvfb screenshot smoke from a fresh Release `linux-x64` publish captured `/tmp/cotton-sync-linux-verify-20260608/cotton-sync-desktop-linux-folder-controls.png` and `/tmp/cotton-sync-linux-verify-20260608/cotton-sync-desktop-linux-settings-diagnostics.png`. The reviewed folder-controls state shows status dots, compact icon actions, reachable expanded row controls, and no Activity panel crowding. The diagnostics state shows `Export logs`, a visible exported path row with `Open`, and an internal diagnostics scrollbar.
  Partial 2026-06-07: refreshed Linux visual-smoke from a fresh desktop publish at `/tmp/cotton-sync-linux-walkthrough-20260607-r2`; folder rows use status dots, compact folder/menu buttons, an independently sized Folders area, and the expanded editor remains reachable without Activity consuming the panel.
  Verification: visual check on Linux for layout regressions and Windows manual check with two or more sync pairs.
- [ ] Fix startup and authentication polish.
  Required behavior: saved sessions show a connecting/restoring state instead of the login form, login supports Enter, auth errors stay human-readable without moving the whole form offscreen, and the app-code browser login flow is integrated into both the CLI and desktop app after its server/shared contract is reviewed or transferred to `develop`.
  Current status: code-side password-login audit is done; startup loading already hides setup while session restore is in progress; app-code browser login is integrated through shared DTOs, SDK, app layer, CLI, and desktop login UI. Keep open for Windows restart/sign-in/browser-approval verification.
  Audit 2026-06-08: app-code/browser login is not missing. Desktop exposes `SignInWithBrowserCommand` and cancel state, CLI creates `AppCodeBrowserAuthFlow`, and SDK exposes `StartAppCodeAsync`/`PollAppCodeAsync`. Do not reimplement this flow; verify it against the current `develop` server and clean up the branch split above.
  Verification 2026-06-09: app-code polling now has Release evidence for transient network retry and expiration bounds in the app layer, and full CLI Release browser-auth coverage passed after removing stale fixed fake-session expiry dates. Commands: `dotnet test src/Cotton.Sync.App.Tests/Cotton.Sync.App.Tests.csproj --configuration Release --no-restore --filter "AppCodeBrowserAuthFlowTests|SignInWithBrowserAsync"` passed 9/9; `dotnet test src/Cotton.Sync.Cli.Tests/Cotton.Sync.Cli.Tests.csproj --configuration Release --no-restore` passed 24/24. Keep this item open for Windows restart/sign-in/browser-approval verification against the real desktop UI.
  Verification 2026-06-09: setup server probing now ignores stale probe failures after the user edits the server address again, so an old delayed failure cannot push a newly verified Cotton Cloud server back into `Cotton server not found`. Focused startup/server-probe tests passed 4/4 in Debug and 4/4 in Release, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --no-restore` passed with 0 warnings.
  Verification 2026-06-09: after approval, app-code browser auth now retries transient current-user lookup failures instead of reporting auth failure immediately. Focused AppCodeBrowserAuthFlowTests passed 10/10 in Debug and Release, downstream Release browser-auth slices passed in CLI 4/4 and desktop 2/2, and dotnet build src/Cotton.Sync.App/Cotton.Sync.App.csproj --configuration Release --no-restore passed with 0 warnings.
  Verification: focused SDK/app/CLI/view-model tests plus Windows manual restart/sign-in/browser-approval check.
- [ ] Fix desktop notification and tray polish.
  Required behavior: tray icon is the normal Cotton icon when idle, changes only for syncing/paused/error states, Windows notifications use the product name and icon where the platform API allows it, and key desktop app events notify outside the web app too.
  Current status: code-side tray/notification audit is done. Tray unavailable actions are now removed from the native menu instead of relying on disabled/hidden menu items, so Windows native menu rendering should not show a disabled `Pause sync` entry when pause/resume is unavailable. Windows installed-app identity and visual notification check remain in Windows-only work.
  Partial 2026-06-07: `DesktopTrayController` now rebuilds the native tray menu from available actions and does not set disabled action items. Focused tray/menu verification passed 15/15 after a clean desktop build.
  Verification: Linux smoke where supported and Windows manual notification/tray check.
- [x] Re-run a minimal desktop verification pass.
  Required behavior: avoid full-suite churn for every tiny step; run focused tests for the changed behavior, then desktop Release build before commit.
  Current status: completed for this pass with focused desktop tests and desktop build.
  Verification: record exact commands and results before marking done.
  Verification result: `dotnet test src/Cotton.Sync.Desktop.Tests/Cotton.Sync.Desktop.Tests.csproj --no-restore --filter "RunProgressChanged|TransferProgressChanged|DesktopSetupVisualContractTests|ShowSelectedSyncPairEditorCommand|ToggleActivityCommand|InitializeAsync_ShowsStartupLoadingInsteadOfSetupWhileRestoringSession|SignInCommand_ShowsHuman|SignInInputs_SubmitOnEnterAndReturnKeys|DesktopTrayStatusResolverTests|DesktopTrayMenuContractTests|DesktopNotificationServiceFactoryTests"` passed 94/94; `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --no-restore` passed with 0 warnings and 0 errors.
