## Phase 9 - Tray, Autostart, Notifications, And Lifecycle

- [x] Add single-instance enforcement.
  Verification 2026-06-03: commit `Add desktop single-instance guard`; desktop startup acquires a per-user app-data lock file before starting Avalonia, and a second process exits when the lock is already held. Focused `DesktopSingleInstanceGuardTests` passed 3/3, full `Cotton.Sync.Desktop.Tests` passed 18/18, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
  Verification 2026-06-04: second launch now sends a single-instance activation request to the running process through a per-data-dir named pipe before exiting, and the first process posts `MainWindow.ShowShell()` on the Avalonia UI thread. Focused single-instance tests passed 8/8, full `Cotton.Sync.Desktop.Tests` passed 246/246, `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings, and full solution Release build passed with 0 warnings.
- [x] Add close-to-tray behavior.
  Verification 2026-06-03: Windows-only tray lifecycle uses `ShutdownMode.OnExplicitShutdown`; `MainWindow` cancels close and hides to tray when tray lifecycle is available, while Linux keeps normal close semantics because tray lifecycle is unsupported. `DesktopWindowLifecyclePolicyTests` passed 4/4, full `Cotton.Sync.Desktop.Tests` passed 22/22, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings. Manual Windows tray verification remains open below.
- [x] Add explicit quit behavior.
  Verification 2026-06-03: tray Quit calls `MainWindow.RequestQuit()` before application shutdown, and the window lifecycle policy then resolves close as a real close instead of hide-to-tray. `DesktopWindowLifecyclePolicyTests` passed 4/4, full `Cotton.Sync.Desktop.Tests` passed 22/22, and solution Release build passed with known NU1903 warnings.
- [x] Add tray icon states.
  Required states: idle, syncing, paused, offline, error.
  Partial 2026-06-03: added a tested tray status model/resolver for signed-out, idle, syncing, paused, offline, and error states, and wired it to the tray tooltip.
  Verification 2026-06-03: added dedicated tray status icon assets for signed-out, idle, syncing, paused, offline, and error states, plus `DesktopTrayIconAssetResolver`; `DesktopTrayController` now updates the tray icon when status changes. Focused tray tests passed 7/7, full `Cotton.Sync.Desktop.Tests` passed 89/89, and full solution Release build passed with the known NU1903 warning. Manual Windows tray verification remains open below.
  Verification 2026-06-04: tray status now follows the shell attention summary instead of raw action-required errors only, so conflict-only states use the attention/error tray icon and tooltip `Cotton Sync - Conflicts need review` instead of falling back to `Connected`. Focused tray resolver tests passed 8/8, full `Cotton.Sync.Desktop.Tests` passed 256/256, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-05: `DesktopTrayStatusKind` now reserves `Unknown = 0`, and icon resolution rejects unknown tray states instead of mapping default enum values to a valid signed-out icon. Focused tray tests passed 11/11, and full solution Release build passed with 0 warnings.
  Verification 2026-06-06: idle tray state now uses the plain Cotton package icon (`Assets/icon-192.png`), while syncing, paused, offline, error, and signed-out continue to use dedicated state assets. Removed the stale `tray-idle.png` overlay asset. Focused tray resolver tests passed after this change.
- [x] Make active sync visible from tray/taskbar without opening the window.
  Required behavior: during upload/download/scanning, the Windows tray/taskbar surface should visibly indicate that sync is active through a distinct tray icon state, overlay/badge, subtle animation or alternating sync-status icon when supported, tooltip progress, taskbar progress, or another platform-appropriate signal. When everything is connected and idle, the tray must show the normal Cotton icon rather than a sync-specific state. The user should not need to open the dashboard to know that synchronization is currently running.
  Partial 2026-06-06: the tray status resolver now uses the shell's real active-progress flag to switch to the syncing icon, and keeps the normal Cotton idle icon for connected/idle or sync-related text without active work. `DesktopTrayController` listens for `HasCurrentWorkProgress` changes so upload/download/scanning state can update the tray without reopening the app window. Verification: `dotnet test src/Cotton.Sync.Desktop.Tests/Cotton.Sync.Desktop.Tests.csproj --configuration Release --no-restore` passed 343/343, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-06: while active sync work is visible, the tray tooltip now includes the current progress title and compact progress details, preferring byte/speed/ETA header metrics for transfers and falling back to run-progress details such as local scan file-discovery counts. `DesktopTrayController` updates the tooltip when the current work title/details/header metrics change, so the tray can answer what is syncing without opening the dashboard. Verification: focused tray tests passed 15/15, full `Cotton.Sync.Desktop.Tests` passed 367/367, `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings, and `git diff --check` passed.
  Verification 2026-06-06: current tray status tests prove active scan/upload/download progress selects `tray-syncing.png`, keeps the plain Cotton icon for connected/idle and non-active sync text, and exposes active progress title plus byte/speed/ETA or scan details in the tooltip. Focused tray tests passed 7/7, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings. Manual OS tray rendering remains tracked separately under Windows/Linux verification.
- [x] Add tray menu.
  Required commands: show app, open folder, open web, sync now, pause/resume, settings, quit.
  Partial 2026-06-03: commit `Expand desktop tray menu commands`; Windows tray menu now wires show app, open selected local folder, open Cotton Cloud in browser, sync now, pause, resume, settings overlay, and quit.
  Verification 2026-06-03: `DesktopTrayController` keeps the required menu command surface wired through `ShellViewModel` commands while the tray status/icon slice passed focused tray tests, full desktop tests, and solution Release build. Manual Windows tray behavior remains open below.
  Partial 2026-06-04: unavailable tray actions are now hidden instead of kept as disabled menu clutter. The local-folder action remains visible only when the dashboard has exactly one sync folder, while open-web, sync-now, pause/resume, and settings are shown only when their commands can execute. Verification: `DesktopTrayMenuContractTests` passed 2/2, full `Cotton.Sync.Desktop.Tests` passed 277/277, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-06: tray and dashboard secondary menus now label the browser/cloud action as `Open in Cotton Cloud`, keeping it distinct from `Open local folder` when a user's sync folder is itself named `Cloud`. Focused tray/menu visual-contract tests passed after this change.
- [x] Add Windows autostart adapter.
  Acceptable options: installer-managed startup, registry Run entry, startup shortcut, or scheduled task.
  Verification 2026-06-03: `WindowsRunAutostartService` uses the current-user `Software\Microsoft\Windows\CurrentVersion\Run` entry `Cotton Sync` with the packaged launch command, and registry access is behind `IWindowsRunRegistry` so the enable/disable/matching-command behavior is covered without touching a real registry. `WindowsRunAutostartServiceTests` passed 3/3, full `Cotton.Sync.Desktop.Tests` passed 25/25, and solution Release build passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings. Manual reboot verification remains open below.
  Verification 2026-06-04: Windows autostart now refuses development launch contexts such as `dotnet` hosts and non-published `bin/Debug` or `bin/Release` apphost output, so running from Visual Studio/debug builds cannot register a throwaway build path in `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`. Published apphosts and installed apphosts remain valid launch targets. Focused packaging/autostart tests passed 35/35, full `Cotton.Sync.Desktop.Tests` passed 262/262, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-04: Windows Run autostart now formats the registry value with Windows command-line quoting instead of XDG desktop-entry escaping, so installed paths like `C:\Users\qa\AppData\Local\Programs\Cotton Sync\Cotton.Sync.Desktop.exe` are quoted without doubled path backslashes. XDG autostart keeps desktop-entry escaping separately. Focused Windows/XDG autostart tests passed 12/12, full `Cotton.Sync.Desktop.Tests` passed 285/285, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
- [x] Add Linux autostart adapter.
  Target: XDG autostart `.desktop`.
  Verification 2026-06-03: commit `Clarify desktop lifecycle platform support`; Linux uses an XDG autostart `.desktop` adapter, and the factory no longer adds `--start-minimized` on Linux while tray lifecycle is unsupported. `Cotton.Sync.Desktop.Tests` passed 15/15 including the Linux factory regression test, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
  Verification 2026-06-03: the frontend `icon-192.png` is now copied as a loose `Assets/icon-192.png` desktop asset for XDG `.desktop` `Icon=` paths; `ValidateDesktopLooseAssets` fails the build if it is missing. `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` produced `Assets/icon-192.png`, and `dotnet publish ... -p:GeneratePublishChecksums=false` produced the same loose icon file.
  Verification 2026-06-04: first-run `LoadAsync` no longer applies the default autostart preference before a restored or newly authenticated session exists, avoiding external XDG/Run-entry side effects on the sign-in screen. Focused lifecycle tests covered pre-session no-op and post-auth apply behavior, a temp XDG smoke kept autostart empty before login, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: Linux XDG autostart now also refuses development launch contexts through `AutostartLaunchCommand.TryCreateDefault`, matching Windows behavior and preventing a debug or non-published build path from being written to `~/.config/autostart/cotton-sync.desktop` after sign-in. Focused Windows/XDG autostart tests passed 12/12, full `Cotton.Sync.Desktop.Tests` passed 285/285, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
- [x] Add notification adapter.
  Required notifications: initial sync complete, conflict created, action-required error.
  Verification 2026-06-04: `DesktopNotificationTracker` emits initial-sync-complete, conflict, and action-required notification requests; in-app notification history is wired through `ShellViewModel`; settings persists `AppPreferences.EnableNotifications` and suppresses native notifications when disabled. Native platform adapters are implemented through `IDesktopNotificationService`: Linux uses `notify-send` with unsupported fallback, and Windows uses `WindowsToastNotificationService` through an encoded PowerShell toast command. `DesktopNotificationServiceFactoryTests`, `NotifySendNotificationServiceTests`, `WindowsToastNotificationServiceTests`, and full `dotnet test src/Cotton.Sync.Desktop.Tests/Cotton.Sync.Desktop.Tests.csproj --configuration Release --logger "console;verbosity=minimal"` passed 179/179. Manual OS notification behavior remains open in the Windows/Linux verification items below.
  Verification 2026-06-04: initial-sync-complete notifications now require the sync-pair status snapshot to include `LastSyncedAtUtc`, matching the dashboard's successful-baseline source of truth instead of relying on a raw `Syncing` to `Idle` transition. Focused notification tracker tests passed 6/6, full `Cotton.Sync.Desktop.Tests` passed 257/257, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-04: action-required notifications now use the same normalized user-readable sync-pair error text as the dashboard, so old-backend HTML/JSON parser failures do not leak raw exception text into toast or in-app notifications. Error states without details emit the generic action-required message instead of staying silent. Focused notification/resolver tests passed 23/23, full `Cotton.Sync.Desktop.Tests` passed 293/293, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-06: action-required resolver and notification tracker now normalize sync-pair status strings that already contain embedded HTTP problem-details JSON, including `/api/v1/files/from-chunks` 400 upload failures, so banners, activity-derived action text, and Windows toast messages do not expose raw `Response: {...}` payloads or trace ids. Focused resolver/notification tests passed 38/38, full `Cotton.Sync.Desktop.Tests` passed 369/369, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings.
- [ ] Add desktop notifications for core account/session events.
  Required notifications: signed in/session restored, signed out, session revoked, and other core app notifications that the web app would surface for account/session state. These should respect notification preferences and avoid noisy duplicates during startup restore.
  Partial 2026-06-06: explicit sign-in and sign-out now emit native desktop notifications through the shared notification preference/support gate, without adding extra dashboard notification cards on top of Activity. Session restore remains activity-only until startup-noise behavior is manually reviewed. Verification: focused sign-in/sign-out/notification tests passed 31/31, full `Cotton.Sync.Desktop.Tests` passed 354/354, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-06: server-driven session revocation now has a dedicated app-level event publisher, is forwarded through the desktop shell, and moves the UI to signed-out state with a `Session expired` native notification while clearing transient secrets/progress surfaces. Session restore remains activity-only to avoid noisy startup toasts until a manual installed-app review decides otherwise. Verification: focused app/desktop session-revocation tests passed 20/20, full `Cotton.Sync.App.Tests` passed 150/150, full `Cotton.Sync.Desktop.Tests` passed 371/371, desktop Release build passed with 0 warnings, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-06: visible launches with a restored signed-in session now emit a `Session restored` native notification through the same preference/support gate. Tray/autostart starts and visual-smoke launches suppress this startup toast to avoid noisy duplicate notifications. Focused notification tests passed 5/5, full `Cotton.Sync.Desktop.Tests` passed 392/392, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings. Keep unchecked until installed-app Windows and Linux notification behavior is manually reviewed.
- [ ] Fix Windows notification identity and icon.
  Required behavior: Windows toast notifications must display the product name `Cotton Sync`, not implementation names such as `Cotton.Sync.Desktop`, and should use the Cotton app icon when the Windows notification API/path supports passing an app logo or AppUserModelID-backed identity. Verification must include an actual Windows notification screenshot or manual Windows run, because Linux/headless tests cannot prove the OS-facing display name/icon.
  Current implementation note: `WindowsToastNotificationService` uses PowerShell with `Windows.UI.Notifications.ToastNotificationManager.CreateToastNotifier(DesktopAppIdentity.AppUserModelId)`. The fix should investigate the installed Start Menu shortcut/AppUserModelID identity and toast XML image support such as app-logo override before changing constants, because debug launches without shell registration may display different sender metadata than installed apps.
  Partial 2026-06-06: desktop assembly metadata now uses the user-facing `Cotton Sync` title and product name instead of `Cotton Sync Desktop` / `Cotton`, keeping packaged Windows metadata aligned with the installer/window/notification branding. Verification: focused packaging and Windows toast tests passed 31/31, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings. Keep unchecked until installed Windows notification screenshots prove the OS sender name and icon.
  Partial 2026-06-06: Windows toast XML now builds a `ToastGeneric` binding directly before adding `appLogoOverride`, instead of attaching the icon node to the legacy `ToastText02` template. This keeps title/body text node creation safe while making the packaged app icon path compatible with the modern toast logo slot. Verification: focused notification tests passed 34/34, full `Cotton.Sync.Desktop.Tests` passed 387/387, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings. Keep unchecked until installed Windows notification screenshots prove the OS sender name and icon.
- [x] Add tests for single-instance lock where practical.
  Verification 2026-06-03: `DesktopSingleInstanceGuardTests` cover first acquire, blocked second acquire, and acquire after dispose; focused tests passed 3/3.
- [ ] Add manual Windows verification: autostart after reboot, tray behavior, notifications.
- [ ] Add manual Linux verification: autostart after login, tray behavior, notification behavior.
- [ ] Verify Linux desktop notification support and limitations.
  Required behavior: record actual `notify-send`/libnotify behavior on the tested Linux desktop environments, including sender name, icon support, timeout behavior, action support if any, and fallback behavior when notifications are unavailable. If the tested desktop environment needs portal-based notifications instead of `notify-send`, record the adapter change before release readiness.
  Partial 2026-06-06: Linux notification capability detection now requires both `notify-send` and a DBus session bus before the adapter is considered supported, preventing self-test from claiming notifications work in headless/no-session contexts. The self-test details also record the important libnotify limitations: sender name, icon rendering, timeout, and actions are controlled by the desktop notification daemon, and Cotton Sync does not use notification actions. Local Linux self-test on XFCE reported `notify-send`, DBus session bus available, packaged `icon-192.png`, and the daemon-dependent limitations. Verification: focused notification capability tests passed 14/14, full `Cotton.Sync.Desktop.Tests` passed 389/389, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings. Keep unchecked until clean-machine Linux notification center screenshots/manual behavior are recorded.
- [x] Document Linux tray limitations in app capabilities and self-test.
  Verification 2026-06-03: `DesktopPlatformCapabilities.CreateSnapshot()` records OS/session/current desktop and explains why Linux tray lifecycle is not release-supported yet; settings and self-test now surface that reason. `Cotton.Sync.Desktop.Tests` includes a Linux guard that verifies the app does not claim tray lifecycle support on Linux.
- [ ] Record actual tested Linux desktop environments after clean-machine/manual runs.
  Partial 2026-06-03: desktop settings now explicitly explain when tray lifecycle is unavailable, and Linux/XDG autostart tests verify autostart does not use `--start-minimized` without tray lifecycle support. Keep unchecked until manual Linux desktop-environment results are recorded.
