# Cotton Sync Desktop Release Plan

This is the ground-up release plan for the Cotton Cloud desktop synchronization client. It replaces the previous prototype/foundation plan and should be treated as the source of truth for building a real releasable application, not a demo.

All implementation checkboxes start unchecked. A task can be marked done only after its verification item is completed and recorded in this file or in the linked task notes.

## Product Target

Cotton Sync Desktop is a polished Windows/Linux desktop application that behaves like a real cloud-folder sync client:

- [ ] The user can install and launch the application without developer tools.
- [ ] The user can sign in with Cotton credentials and TOTP when required.
- [ ] The user can configure one or more sync pairs: local folder to remote Cotton folder.
- [ ] The application can start with the operating system.
- [ ] The application runs continuously in the background and is controlled from the tray.
- [ ] Local changes are uploaded automatically.
- [ ] Remote changes are downloaded automatically.
- [ ] Conflicts preserve both versions and are visible to the user.
- [ ] Dangerous deletes are blocked before data loss.
- [ ] The application exposes clear status, progress, diagnostics, and logs.
- [ ] Release packages are built and smoke-tested on clean Windows and Linux machines.

## Non-Negotiable Decisions

- [ ] Use the existing .NET stack: `Cotton.Sync`, `Cotton.Sdk`, EF Core SQLite, and Avalonia.
- [ ] Keep synchronization logic out of desktop UI code.
- [x] Use EF Core SQLite for durable local state and settings. Do not use raw SQL for normal app state.
  Verification 2026-06-03: `SqliteSyncStateStore`, `SqliteSyncPairSettingsStore`, and `SqliteAppPreferencesStore` use EF Core SQLite contexts and migrations; `rg "Microsoft.Data.Sqlite|SqliteConnection|CommandText|CREATE TABLE|SELECT |INSERT |UPDATE |DELETE "` across `Cotton.Sync`, `Cotton.Sync.App`, and `Cotton.Sync.Desktop` found no raw SQL command usage. `dotnet test src/Cotton.Sync.Tests/Cotton.Sync.Tests.csproj --configuration Release --no-restore` passed 89/89.
- [ ] Treat WebDAV as interoperability, not as the native desktop sync engine.
- [ ] Treat SignalR as a wake-up signal, not as the durable source of remote truth.
- [ ] Implement full-mirror folder sync first. Virtual files/placeholders are a later platform feature.
- [ ] Support multiple sync pairs in the product model from the start.
- [ ] Do not support overlapping local sync roots.
- [ ] Do not ship insecure token storage in the final release.
- [ ] Do not mark release readiness until clean-machine install and sync smoke tests pass.

## Architecture Overview

Target layers:

- [ ] `Cotton.Sdk`: typed API client, auth, token refresh, chunks, files, nodes, sync changes, SignalR event connection.
- [ ] `Cotton.Sync`: headless sync core, state store, local scanner/writer, remote crawler, reconciler, conflict handling, delete guards.
- [ ] `Cotton.Sync.App`: application layer for sync-pair settings, auth session, continuous supervisor, status/activity streams, platform command orchestration.
- [ ] `Cotton.Sync.Desktop`: Avalonia shell only. It displays state and sends commands to `Cotton.Sync.App`.
- [x] `Cotton.Sync.Cli`: headless validation and recovery tool using the same app/core services.
  Verification 2026-06-03: added real `Cotton.Sync.Cli` and `Cotton.Sync.Cli.Tests` projects to `src/Cotton.sln`; CLI supports `state-summary --database <path> --sync-pair <id>` through the existing EF-backed `SqliteSyncStateStore`, printing entry count and remote cursor for headless validation/recovery. `dotnet test src/Cotton.Sync.Cli.Tests/Cotton.Sync.Cli.Tests.csproj --configuration Release --no-restore` passed 3/3, and full solution Release build passed with the known NU1903 warning.
  Verification 2026-06-04: CLI now also supports `sync-once --server <url> --username <name> (--password <password> | --password-env <name>) --local-root <path> --remote-root <node-id> --sync-pair <id> --database <path> [--two-factor-code <code>]`, using the SDK and `SyncEngine` for a real one-shot full-mirror sync pass without persisting tokens. `SyncCliCommandRunnerTests` passed 5/5, and `SyncClientEndToEndTests.SyncCliCommandRunner_SyncOnceUploadsLocalFileThroughSdkToServer` verified the CLI path against the integration-test server, remote upload, downloaded content, and SQLite baseline.
  Verification 2026-06-04: `Cotton.Sdk.CottonServerUrl` now shares HTTP/HTTPS and bare-host server URL normalization between desktop startup/sign-in and CLI `sync-once`, so `--server app.cottoncloud.dev` resolves to HTTPS consistently. Focused SDK URL tests passed 12/12, CLI tests passed 7/7, desktop URL/startup tests passed 26/26, full SDK tests passed 40/40, full CLI tests passed 7/7, and full desktop tests passed 241/241.
  Verification 2026-06-04: `SyncCliCommandRunnerTests.SyncOnce_UploadsLocalFileAndPersistsBaseline` now covers the CLI one-shot path inside the CLI test project with a fake Cotton HTTP server, real local file scan, SDK auth/tree/settings/chunk/file requests, and real EF SQLite sync-state baseline persistence. Full CLI tests passed 8/8, full SDK tests passed 40/40, full sync-core tests passed 123/123, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-04: CLI `sync-once` now composes `SyncEngine` with `SdkRemoteDirectorySynchronizer`, so the headless validation tool performs the same full-mirror remote folder creation path as the desktop app. `SyncCliCommandRunnerTests.SyncOnce_UploadsEmptyLocalDirectoryAndPersistsDirectoryBaseline` verifies an empty local folder is created remotely through the node API and persisted as a directory baseline in EF SQLite. Full CLI tests passed 9/9, full sync-core tests passed 123/123, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-04: `SyncClientEndToEndTests.SyncCliCommandRunner_SyncOnceUploadsLocalFileThroughSdkToServer` now verifies the CLI full-mirror output and state after remote directory support: one remote folder activity, one file upload activity, one directory baseline, and one file baseline. Focused test passed 1/1 and full `Cotton.Server.IntegrationTests` passed 365/365.
- [ ] Platform adapters: autostart, tray, notifications, open folder/browser, secure token store, single instance.

## Phase 0 - Ground Truth And Branch Hygiene

- [x] Start from `develop` and create a dedicated feature branch.
  Verification 2026-06-03: branch `feature/desktop-sync-client`; `git status --short --branch` reported `## feature/desktop-sync-client...origin/feature/desktop-sync-client [ahead 7]`.
- [ ] Review the current `Cotton.Sync`, `Cotton.Sdk`, `Cotton.Sync.Cli`, and `Cotton.Sync.Desktop` code.
  Verification: record the existing classes that will be reused and the classes that must be replaced.
- [ ] Review current server file/node/chunk/auth/SignalR endpoints.
  Verification: record concrete endpoint names and DTOs. Do not guess model fields.
- [ ] Review existing tests before changing architecture.
  Verification: record current passing/failing test baseline.
- [ ] Confirm local dev server startup path and test credentials strategy.
  Verification: record the command and whether public-instance auto-create is used.

## Phase 1 - Release-Grade App Model

- [x] Add a `SyncPairSettings` domain model.
  Required fields: id, display name, local root path, remote root node id, remote display path, enabled flag, sync mode, created/updated timestamps.
  Verification 2026-06-03: implemented in `src/Cotton.Sync.App/SyncPairs/SyncPairSettings.cs`; covered by `Cotton.Sync.App.Tests`.
- [x] Add sync mode values.
  Required modes: full mirror now, virtual files placeholder reserved for later.
  Verification 2026-06-03: implemented in `src/Cotton.Sync.App/SyncPairs/SyncPairMode.cs`; unsupported placeholder mode is rejected by tests.
  Verification 2026-06-05: `SyncPairMode` now reserves `Unknown = 0`, keeps `SyncPairSettings.Mode` explicitly defaulted to `FullMirror`, and validator coverage rejects unknown modes so default enum values cannot silently become a real sync configuration. Focused app/desktop mode tests passed 137/137, and full solution Release build passed with 0 warnings.
- [x] Add sync pair validation.
  Required checks: local path exists or can be created, local roots do not overlap, remote target is resolvable, mode is supported.
  Verification 2026-06-03: structural validator plus async prerequisite validator implemented; tests cover overlap, unsupported mode, local root creation/unavailable path, remote prerequisite errors, and save rejection without persistence.
  Verification 2026-06-04: sync-pair update flow now has regression coverage proving an existing pair is replaced in the validation set before overlap checks, so editing the same pair does not self-reject as an overlapping local root. Focused `SyncApplicationServiceTests` passed 19/19, full `Cotton.Sync.App.Tests` passed 107/107, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: Windows UNC sync roots are now covered by overlap validation tests, including case-insensitive nested network-share paths. Focused `SyncPairSettingsValidatorTests` passed 9/9, full `Cotton.Sync.App.Tests` passed 108/108, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: the desktop add-folder wizard now applies the existing overlap validator immediately after local folder selection, so selecting an already configured or nested local root stays on the local step and does not fetch remote folders or surface a later unrelated server error. The wizard also clears that local error when the next selected folder is valid or the wizard is closed. Focused add-folder local-selection tests passed 7/7, full `Cotton.Sync.Desktop.Tests` passed 283/283, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
- [x] Add a settings store through EF Core SQLite.
  Required stores: sync-pair settings, app preferences, remembered server URL, startup preferences.
  Verification 2026-06-03: `SqliteSyncPairSettingsStore` and `SqliteAppPreferencesStore` use EF Core SQLite through shared `SyncAppDbContext`; tests cover sync-pair and app-preferences roundtrip.
  Verification 2026-06-03: commit `Version app settings database`; `sync-app.db` now uses EF-generated `InitialSyncAppState` migrations and store initialization calls `Database.MigrateAsync` instead of `EnsureCreatedAsync`. Focused SQLite store tests passed 10/10, full `Cotton.Sync.App.Tests` passed 81/81, and `dotnet build src/Cotton.sln --configuration Release` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
- [x] Keep token storage behind `ITokenStore`; do not couple it to app settings.
  Verification 2026-06-03: auth uses SDK `ICottonTokenStore` through `ICottonAuthClient`; app preferences store has no token fields.
- [x] Add tests for settings roundtrip, pair enable/disable, and pair deletion.
  Verification 2026-06-03: `SqliteSyncPairSettingsStoreTests` cover roundtrip, update with `IsEnabled = false`, and deletion.
- [x] Add tests for overlapping local roots on Windows-style and Linux-style paths.
  Verification 2026-06-03: `SyncPairSettingsValidatorTests` cover Windows case-insensitive nested/equal roots and Unix nested roots.
- [x] Add tests for unsupported virtual-file mode returning a deliberate not-implemented result.
  Verification 2026-06-03: `Validate_RejectsVirtualFilesPlaceholderMode` passes.
- [x] Build the solution.
  Verification: `dotnet build src/Cotton.sln --configuration Release`.
  Verification 2026-06-03: command passed with 0 warnings and 0 errors after Phase 1 app-model slices.

## Phase 2 - Application Layer

- [x] Create `Cotton.Sync.App` or an equivalent application-layer project.
  Verification 2026-06-03: `src/Cotton.Sync.App/Cotton.Sync.App.csproj` and `src/Cotton.Sync.App.Tests/Cotton.Sync.App.Tests.csproj` are in the solution; Release build passes.
- [x] Move desktop orchestration out of `MainWindow`.
  Verification 2026-06-03: `MainWindow` only owns Avalonia window lifecycle, sizing, folder picker binding, and view-model creation; auth, sync-pair persistence, supervisor control, status subscription, diagnostics, and server probing live behind `DesktopShellController`, `DesktopSyncApplicationFactory`, and `Cotton.Sync.App`. Full `Cotton.Sync.Desktop.Tests` passed 62/62 and full solution Release build passed with the known NU1903 warning.
- [x] Add `SyncApplicationService` for high-level commands.
  Commands: sign in, sign out, add sync pair, update sync pair, remove sync pair, sync now, pause, resume, open folder, open web.
  Verification 2026-06-03: `SyncApplicationService` exposes sign in/out, preferences, sync-pair save/delete/list, start/stop/sync-now, pause/resume, and platform open commands. `SyncApplicationServiceTests` cover sign-out shutdown, start/stop sync, platform command delegation, valid sync-pair save, overlap/prerequisite rejection, and deletion; full `Cotton.Sync.App.Tests` passed 83/83.
- [x] Add `SyncSupervisor` for continuous background operation.
  Verification 2026-06-03: `SyncSupervisor` creates one runner per saved sync pair, starts/stops runners, fans out sync/pause/resume commands, and publishes aggregate status. `SyncSupervisorTests` cover start status publishing, selected-runner pause/resume, sync-all fanout, and stop publishing; full `Cotton.Sync.App.Tests` passed 83/83.
  Verification 2026-06-04: `SyncSupervisor.StartAsync` now stops existing runners before replacing them, preventing repeated app starts/session restores from orphaning old per-pair runners. Focused `SyncSupervisorTests` passed 5/5, full `Cotton.Sync.App.Tests` passed 92/92, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: `SyncSupervisor.StartAsync` now also stops already-created runners and clears its runner snapshot if a later runner fails during startup, preventing half-started sync pairs from surviving failed app startup. Focused `SyncSupervisorTests` passed 6/6, full `Cotton.Sync.App.Tests` passed 104/104, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: `SyncSupervisor` no longer holds its global operation gate while awaiting long runner sync passes, so stop can reach the active runner during an in-flight sync instead of waiting for the whole sync-all command to finish. Focused `SyncSupervisorTests` passed 7/7, full `Cotton.Sync.App.Tests` passed 113/113, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
- [x] Add `SyncPairRunner` for per-pair state.
  States: disabled, idle, scanning, syncing, paused, offline, conflict, error.
  Verification 2026-06-03: `SyncPairRunState` defines disabled, idle, scanning, syncing, paused, offline, conflict, and error; `SyncPairRunner` manages disabled/idle/paused/syncing/offline/error transitions and queue-collapsed sync requests. `SyncPairRunnerTests` cover disabled start, pause/resume, paused no-op sync, successful sync, queued requests, retry, offline transient failures, and error state; full `Cotton.Sync.App.Tests` passed 83/83.
  Verification 2026-06-04: `SyncPairRunner` now blocks incoming queued sync requests as soon as pause or stop is requested, so an in-flight sync can finish without launching a stale follow-up pass. `SyncPairRunnerTests` passed 20/20, full `Cotton.Sync.App.Tests` passed 112/112, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: `SyncPairRunner.StopAsync` now cancels the active `ISyncPairWork.RunOnceAsync` pass through an internal linked cancellation token, so shutdown/sign-out does not have to wait for a long manual or background sync to finish naturally. `SyncPairRunnerTests` passed 21/21, full `Cotton.Sync.App.Tests` passed 114/114, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-05: `SyncPairRunState` now reserves `Unknown = 0`, and `SyncPairStatus` rejects unknown runtime states so an uninitialized status cannot be displayed as a valid disabled sync pair. Focused app status/runner/supervisor tests passed 36/36, and full solution Release build passed with 0 warnings.
- [x] Add `IAppStatusPublisher` or equivalent observable status stream.
  Verification 2026-06-03: `IAppStatusPublisher` and `InMemoryAppStatusPublisher` expose current app status and observable updates; tests cover current snapshot, immediate subscriber replay, publish delivery, and unsubscribe behavior.
- [x] Add activity history model.
  Required activity types: uploaded, downloaded, deleted local, deleted remote, conflict, skipped, error, warning.
  Verification 2026-06-03: `SyncActivity` and `SyncActivityType` include uploaded, downloaded, deleted local, deleted remote, conflict, skipped, error, and warning; `SyncActivityTests` cover UTC normalization and activity fields.
- [x] Add cancellation and shutdown flow.
  Verification 2026-06-03: app commands accept cancellation tokens, `SyncApplicationService.SignOutAsync` and `StopSyncAsync` stop remote, periodic, local, and supervisor components in order, and `DesktopShellController.Dispose` disposes the host/status subscription. App-layer tests cover start/stop and sign-out shutdown; full `Cotton.Sync.App.Tests` passed 83/83.
  Verification 2026-06-04: desktop host replacement is now async and stops the previous sync host before disposing it, preventing old watchers/remote listeners from surviving a restored-session replacement. `LoadAsync_StopsPreviousRestoredHostWhenSessionIsRestoredAgain` covers the regression; full `Cotton.Sync.Desktop.Tests` passed 143/143 and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: desktop window shutdown now follows `ShellViewModel.DisposeAsync` -> `DesktopShellController.DisposeAsync`, so the active restored host receives `StopSyncAsync` before disposal. `DesktopShellControllerHostLifecycleTests` passed 2/2, full `Cotton.Sync.Desktop.Tests` passed 144/144, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: desktop host disposal now owns and async-disposes the SignalR realtime client, so restored/replaced sessions do not leave `HubConnection` resources behind. `DesktopShellControllerHostLifecycleTests` verifies realtime async disposal on replacement and shutdown; full `Cotton.Sync.Desktop.Tests` passed 145/145 and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: synchronous `DesktopShellController.Dispose()` now also stops and async-disposes the active host, preventing command-line/test synchronous disposal paths from leaving a restored sync host alive. Focused lifecycle tests passed 8/8, full `Cotton.Sync.Desktop.Tests` passed 195/195, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: `SyncApplicationService` now rolls back already-started background components when sync startup fails part-way through restore/start. `SyncApplicationServiceTests` passed 18/18, full `Cotton.Sync.App.Tests` passed 94/94, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: supervisor shutdown can now enter active per-pair runners while a sync-all request is still running, reducing shutdown latency for manual or background sync commands that are already in progress. `SyncSupervisorTests.StopAsync_ReachesRunnerWhileSyncAllIsRunning` covers the regression.
  Verification 2026-06-04: per-pair stop now actively cancels the current sync work after blocking queued requests, then disables the runner after the work observes cancellation. `SyncPairRunnerTests.StopAsync_CancelsRunningSyncWorkAndDisablesRunner` covers the regression.
- [x] Add tests for supervisor state transitions.
  Verification 2026-06-03: `SyncSupervisorTests` cover start, pause/resume, sync-all, and stop transitions/status publishing.
- [x] Add tests for pause/resume and app shutdown.
  Verification 2026-06-03: `SyncPairRunnerTests` and `SyncSupervisorTests` cover pause/resume states, while `SyncApplicationServiceTests` cover sign-out and stop-sync shutdown flow.
  Verification 2026-06-04: added runner coverage for queued sync suppression during pause/stop and for canceled pause/stop calls restoring future sync-request acceptance; focused runner tests passed 20/20.
- [x] Add tests proving UI can use the app layer without directly constructing `SyncEngine`.
  Verification 2026-06-03: `DesktopUiBoundaryTests` assert `ShellViewModel` constructor dependencies are `IDesktopShellController`, `ILocalFolderPicker`, and `IDesktopNotificationService`, and that `MainWindow`/`ShellViewModel` do not store or require `Cotton.Sync.SyncEngine` or `SyncEnginePairWork`. Focused boundary tests passed 2/2, full `Cotton.Sync.Desktop.Tests` passed 64/64, and full solution Release build passed with the known NU1903 warning.
- [x] Build the solution.
  Verification 2026-06-03: `dotnet test src/Cotton.Sync.App.Tests/Cotton.Sync.App.Tests.csproj --configuration Release --no-restore` passed 83/83 and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with the known NU1903 Avalonia/Tmds.DBus.Protocol warning.

## Phase 3 - Backend Sync Change Feed

This phase is required for release-grade remote sync. SignalR alone is not enough because events can be missed while the client is offline.

- [x] Add a durable server change cursor/revision model.
  Verification: commit `Add durable sync change feed`; EF migration `20260603165614_AddSyncChanges` adds `sync_changes` with monotonic `revision`.
- [x] Record file and folder mutations in order.
  Required events: file created, file content updated, file renamed, file moved, file deleted, file restored, folder created, folder renamed, folder moved, folder deleted, folder restored.
  Verification: commit `Add durable sync change feed`; `EventNotificationService` records the required event kinds through `ISyncChangeRecorder`. Focused integration coverage exists for folder create, file create, file rename, and file delete ordering; the broader per-kind matrix remains open below.
- [x] Add `GET /api/v1/sync/changes?since=<cursor>`.
  Verification: commit `Add durable sync change feed`; endpoint implemented in `SyncController` and exercised by `SyncChangesEndpointsTests`.
- [x] Include enough data for the client to update or invalidate remote state.
  Required data: event id/revision, event kind, entity id, parent id, path or enough identifiers to resolve path, file manifest id, content hash, ETag/version when relevant, deletion/restoration markers.
  Verification: commit `Add durable sync change feed`; `SyncChangeDto` returns cursor, kind, layout/node/file ids, current/previous parent ids, manifest id, original file id, name, content hash, ETag, size, and created timestamp.
- [x] Add retention behavior and expired-cursor response.
  Verification: `SyncChangeRetentionService` prunes expired per-user rows, `SyncChangesResponseDto` returns `CursorExpired` and `EarliestAvailableCursor`, `SyncChangesEndpointsTests` passed 4/4, and `dotnet build src/Cotton.sln --configuration Release` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
- [x] Add SDK sync-change client.
  Verification: commit `Add durable sync change feed`; `ICottonSyncClient.GetChangesAsync` added to `ICottonCloudClient.Sync`.
- [x] Add server integration tests for ordered create/update/delete/move/rename changes.
  Verification: `dotnet test src/Cotton.Server.IntegrationTests/Cotton.Server.IntegrationTests.csproj --configuration Release --no-restore --filter FullyQualifiedName~SyncChangesEndpointsTests` passed 2/2; `Changes_RecordsUpdateMoveAndFolderMutationKindsInOrder` covers file create/update/move/rename/delete and folder create/rename/move ordering.
- [x] Add server integration test for missed SignalR recovery through changes API.
  Verification: `dotnet test src/Cotton.Server.IntegrationTests/Cotton.Server.IntegrationTests.csproj --configuration Release --no-restore --filter FullyQualifiedName~SyncChangesEndpointsTests` passed 3/3; `Changes_ReplaysMutationsWhenRealtimeEventsWereMissed` creates remote changes without opening the SignalR hub and recovers them through `/api/v1/sync/changes`.
- [x] Add SDK tests for changes request and response parsing.
  Verification: `dotnet test src/Cotton.Sdk.Tests/Cotton.Sdk.Tests.csproj --configuration Release --no-restore` passed 11/11 after commit `Add durable sync change feed`.
- [x] Add local smoke check: create remote change, fetch it through changes API.
  Verification: `dotnet test src/Cotton.Server.IntegrationTests/Cotton.Server.IntegrationTests.csproj --configuration Release --no-restore --filter FullyQualifiedName~SyncChangesEndpointsTests` passed 1/1 after commit `Add durable sync change feed`.
- [x] Build and test server plus SDK.
  Verification: `dotnet build src/Cotton.sln --configuration Release` passed after commit `Add durable sync change feed`; known NU1903 warnings remain for Avalonia/Tmds.DBus.Protocol.

## Phase 4 - Optimistic Concurrency And Safe Remote Operations

- [x] Add expected version/ETag support for file content update.
  Verification: `FileController.UpdateFileContent` validates `If-Match` against the current content ETag inside the update transaction; `dotnet test src/Cotton.Server.IntegrationTests/Cotton.Server.IntegrationTests.csproj --configuration Release --no-restore --filter "Name~If_Match"` passed 2/2.
- [x] Add expected version/ETag support for file delete where possible.
  Verification: `DeleteFileQuery` validates `If-Match` before delete/trash mutation and `FileController.DeleteFile` returns explicit `412 PreconditionFailed`; `dotnet test src/Cotton.Server.IntegrationTests/Cotton.Server.IntegrationTests.csproj --configuration Release --no-restore --filter "Name~If_Match"` passed 2/2.
- [x] Add expected version/ETag support for move/rename where possible.
  Verification: `MoveFileCommand` and `FileController.RenameFile` validate `If-Match` before changing parent/name; `ICottonFileClient.MoveAsync` and `RenameAsync` send optional expected ETags; `dotnet test src/Cotton.Server.IntegrationTests/Cotton.Server.IntegrationTests.csproj --configuration Release --no-restore --filter "Name~If_Match"` passed 4/4 and `CottonFileAndChunkClientTests` passed 8/8.
- [x] Return explicit conflict responses instead of silent overwrite.
  Verification: stale file update/delete/move/rename now return explicit `412 PreconditionFailed` instead of applying stale mutations; name collisions continue to return `409 Conflict`; `Name~If_Match` server integration tests passed 4/4.
- [x] Update SDK methods to pass expected remote state.
  Verification: `ICottonFileClient` file mutation methods accept optional expected ETags, send `If-Match`, and `SdkRemoteFileSynchronizer` passes remote DTO ETags for update/delete; SDK tests passed 8/8 for `CottonFileAndChunkClientTests`, sync tests passed 17/17 for `SdkRemoteFileSynchronizerTests|SyncEngineTests`.
- [x] Update sync core to preserve both versions when remote state changed after crawl.
  Verification: `SyncEngine` handles remote `412 PreconditionFailed` from stale upload/delete by re-crawling the remote tree and routing the latest remote file through conflict preservation; `dotnet test src/Cotton.Sync.Tests/Cotton.Sync.Tests.csproj --configuration Release --no-restore --filter SyncEngineTests` passed 15/15 and full `Cotton.Sync.Tests` passed 41/41.
- [x] Add tests for stale upload losing the concurrency race.
  Verification: `Update_File_Content_With_Stale_If_Match_Returns_Precondition_Failed` covers server stale update returning `412`, and `RunOnceAsync_PreservesBothVersionsWhenStaleUploadLosesRemoteRace` covers sync-core conflict preservation; `dotnet test src/Cotton.Server.IntegrationTests/Cotton.Server.IntegrationTests.csproj --configuration Release --no-restore --filter "Name~If_Match"` passed 4/4 and `dotnet test src/Cotton.Sync.Tests/Cotton.Sync.Tests.csproj --configuration Release --no-restore --filter SyncEngineTests` passed 15/15.
- [x] Add tests for stale delete losing the concurrency race.
  Verification: `Delete_File_With_Stale_If_Match_Returns_Precondition_Failed_And_Keeps_File` covers server stale delete returning `412` and preserving the file, and `RunOnceAsync_RestoresRemoteVersionWhenStaleDeleteLosesRemoteRace` covers sync-core local restoration; full `ChunksAndFilesEndpointsTests` passed 26/26 and full `Cotton.Sync.Tests` passed 41/41.
- [x] Add tests for stale rename/move conflict.
  Verification: `Rename_File_With_Stale_If_Match_Returns_Precondition_Failed_And_Keeps_Name` and `Move_File_With_Stale_If_Match_Returns_Precondition_Failed_And_Keeps_Parent` cover API-level stale rename/move conflicts; `Name~If_Match` server integration tests passed 4/4.
- [x] Build and run server, SDK, and sync tests.
  Verification: `dotnet test src/Cotton.Sdk.Tests/Cotton.Sdk.Tests.csproj --configuration Release --no-restore --filter CottonFileAndChunkClientTests` passed 8/8; full `dotnet test src/Cotton.Sync.Tests/Cotton.Sync.Tests.csproj --configuration Release --no-restore` passed 41/41; `dotnet test src/Cotton.Server.IntegrationTests/Cotton.Server.IntegrationTests.csproj --configuration Release --no-restore --filter ChunksAndFilesEndpointsTests` passed 26/26; `dotnet test src/Cotton.Server.IntegrationTests/Cotton.Server.IntegrationTests.csproj --configuration Release --no-restore --filter MoveEndpointsTests` passed 26/26; `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.

## Phase 5 - Sync Core Hardening

- [x] Keep `RunOnceAsync` as the deterministic reconciliation primitive.
  Verification: `SyncEngine.RunOnceAsync` remains the single reconciliation entry point used by continuous/app layers, with deterministic sorted path processing; `dotnet test src/Cotton.Sync.Tests/Cotton.Sync.Tests.csproj --configuration Release --no-restore --filter SyncEngineTests` passed 18/18 and full `Cotton.Sync.Tests` passed 44/44.
- [x] Add durable per-pair remote change cursor state.
  Verification: commit `Add sync change cursor state`; `sync_change_cursors` stores the last accepted server cursor per sync pair through EF Core migration `20260603172105_AddSyncChangeCursors`; `dotnet test src/Cotton.Sync.Tests/Cotton.Sync.Tests.csproj --configuration Release --no-restore --filter FullyQualifiedName~SqliteSyncStateStoreTests` passed 8/8, and `dotnet build src/Cotton.sln --configuration Release` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
  Verification 2026-06-04: `SqliteSyncStateStore.InitializeAsync` now serializes EF Core migrations per absolute SQLite path, matching the app-settings store and protecting shared `sync-state.db` startup when multiple sync-pair runners initialize concurrently. `SqliteSyncStateStoreTests` passed 11/11, full `Cotton.Sync.Tests` passed 124/124, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-04: `SqliteSyncStateStore` now lazily runs EF Core migrations before every public read/write path, so a freshly deleted or newly created `sync-state.db` cannot be queried for `sync_change_cursors` before the migration has created it. Focused `SqliteSyncStateStoreTests` passed 14/14, full `Cotton.Sync.Tests` passed 127/127, full `Cotton.Sync.Desktop.Tests` passed 266/266, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-06: desktop `LoadAsync` now initializes the sync-state database during normal startup, not only during self-test or first pair snapshot loading, so a fresh or cleaned desktop profile prepares `sync-state.db` before add-folder/sync actions can surface `sync_change_cursors` table errors. Focused desktop shell/ViewModel/visual-contract tests passed 114/114, full solution Release build passed with 0 warnings, and `git diff --check` passed.
- [x] Add safe remote change-feed reader with explicit acknowledgement.
  Verification: commit `Add remote change feed reader`; `RemoteChangeFeedReader.ReadAsync` reads from the stored cursor without advancing it, `AcknowledgeAsync` advances only after processing or marks expired cursors without skipping changes; `dotnet test src/Cotton.Sync.Tests/Cotton.Sync.Tests.csproj --configuration Release --no-restore --filter FullyQualifiedName~RemoteChangeFeedReaderTests` passed 3/3, and `dotnet build src/Cotton.sln --configuration Release` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
  Verification 2026-06-05: `RemoteChangeFeedReader` now rejects a changes page whose response `SinceCursor` does not match the requested cursor, preventing the client from acknowledging a page from the wrong checkpoint. Focused remote-feed tests passed 28/28, and full solution Release build passed with 0 warnings.
- [x] Add or refine remote snapshot representation for change-feed updates.
  Verification 2026-06-03: `RemoteChangeFeedSnapshot` and `RemoteChangeImpact` normalize durable change-feed DTOs into file/folder targets, semantic actions, affected node/file ids, and refresh hints; `RemoteChangeAwareSyncPairWork` now drains `HasMore` pages through explicit cursor reads before one sync pass and acknowledges only after success. Focused remote change-feed tests passed 19/19, `RemoteChangeAwareSyncPairWorkTests` passed 5/5, full `Cotton.Sync.Tests` passed 89/89, full `Cotton.Sync.App.Tests` passed 83/83, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
  Verification 2026-06-05: remote change snapshots now reject missing layout/item/parent/name identity fields, nonpositive cursors, non-increasing page cursors, returned changes at or before the requested cursor, and `NextCursor` values before the last returned change. Focused remote-feed tests passed 27/27, and full solution Release build passed with 0 warnings.
- [x] Add durable operation intent tracking if needed for crash recovery.
  Verification: no separate operation-intent table is needed for the current full-file sync primitive because operations update baseline only after success, downloads use temp files, and a remote upload that succeeds before baseline persistence is recovered by the next crawl without a duplicate upload; `RunOnceAsync_RecoversAfterRemoteUploadBeforeBaselineUpdate` passed in `SyncEngineTests` 22/22 and full `Cotton.Sync.Tests` passed 65/65.
- [x] Ensure downloads always use temp file plus atomic replace.
  Verification: `AtomicLocalFileSyncWriter.WriteFileAsync` writes into `.cotton-sync/tmp` and moves into place only after successful content write and flush; `WriteFileAsync_RemovesTemporaryFileWhenDownloadFailsAndPreservesExistingFile` and `RunOnceAsync_DoesNotUpdateBaselineWhenRemoteDownloadFails` passed in full `Cotton.Sync.Tests` 44/44.
  Verification 2026-06-04: downloaded file content is now verified against the remote manifest before the temp file is atomically moved into the sync root. A short or corrupted download stream throws before baseline update and leaves no target file or temp files; `RunOnceAsync_RejectsDownloadedContentThatDoesNotMatchManifest` covers this. `AtomicLocalFileSyncWriter` also removes stale `*.download` files left by a previously killed process before starting a new write. Full `Cotton.Sync.Tests` passed 134/134.
  Verification 2026-06-04: stale temp-download cleanup failures are now written to `Trace` instead of being silently swallowed, keeping support diagnostics useful if a locked or permission-denied `.cotton-sync/tmp/*.download` file survives a crash. Focused `AtomicLocalFileSyncWriterTests` passed 8/8, full `Cotton.Sync.Tests` passed 134/134, and full solution Release build passed with 0 warnings.
- [x] Ensure baseline updates happen only after successful local and remote operation.
  Verification: `RunOnceAsync_DoesNotUpdateBaselineWhenRemoteUploadFails`, `RunOnceAsync_DoesNotUpdateBaselineWhenRemoteDownloadFails`, and `RunOnceAsync_DoesNotDeleteBaselineWhenRemoteDeleteFails` cover upload/download/delete failure ordering; `dotnet test src/Cotton.Sync.Tests/Cotton.Sync.Tests.csproj --configuration Release --no-restore --filter SyncEngineTests` passed 18/18 and full `Cotton.Sync.Tests` passed 44/44.
- [x] Add local delete strategy.
  Target: safe delete or recycle/trash where available; direct delete only when deliberately accepted.
  Verification: `AtomicLocalFileSyncWriter.DeleteFileAsync` moves local deletes into `.cotton-sync/deleted/<timestamp-guid>/...` without directly deleting file content and preserves parent directories so folder removal is controlled by `SyncEngine` directory baselines and delete guards; `DeleteFileAsync_MovesFileToDeletedQuarantineAndPreservesParentDirectory` passed in full `Cotton.Sync.Tests` 112/112.
- [x] Add remote delete strategy.
  Target: trash/soft-delete by default.
  Verification: `SyncRunOptions.DeleteRemotePermanently` defaults to `false`, `SyncEngine` passes that value to `IRemoteFileSynchronizer.DeleteFileAsync`, and `RunOnceAsync_DeletesRemoteOnlyWhenBaselineKnowsLocalDelete` plus `RunOnceAsync_CanBypassRemoteTrashWhenExplicitlyConfigured` prove default trash delete and explicit permanent delete; `SyncEngineTests` passed 21/21, full `Cotton.Sync.Tests` passed 49/49, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
- [x] Add mass-delete guard per sync pair.
  Verification: `SyncEngine` preflights planned local and remote deletes per run and blocks all deletes for a direction when candidates exceed `SyncRunOptions` limits, preserving local files and sync baseline entries; `SyncEngineTests` passed 20/20, full `Cotton.Sync.Tests` passed 47/47, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
- [x] Add debounce for files still being written.
  Verification: local watcher bursts are debounced by `LocalChangeSyncCoordinator`, and `SyncPairRunner` treats `LocalFileUnavailableException` from locked/changing files as a bounded retriable local sync failure; `LocalChangeSyncCoordinatorTests` passed 3/3, `SyncPairRunnerTests` passed 10/10, full `Cotton.Sync.App.Tests` passed 75/75, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
- [x] Add handling for locked files.
  Verification: `LocalFileScanner` throws `LocalFileUnavailableException` instead of returning a partial scan when a file cannot be read safely or changes during hashing; `ScanAsync_ThrowsForLockedFile` passed in `LocalFileScannerTests` 5/5, full `Cotton.Sync.Tests` passed 48/48, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
- [x] Add ignore patterns for common temporary files.
  Verification: `LocalFileScanner.ShouldIgnore` excludes `.cotton-sync`, Office temp files, backup suffixes, `.tmp`, `.partial`, and `.crdownload`; `ScanAsync_IgnoresTempFilesAndCottonWorkingFolder` passed in `LocalFileScannerTests` 4/4 and full `Cotton.Sync.Tests` 45/45.
  Verification 2026-06-04: ignore rules were lifted to `SyncPathIgnoreRules` and applied to remote tree crawling too, so remote `.cotton-sync` or temporary paths cannot enter the local writer/baseline model. Focused remote/local ignore tests passed 11/11, full `Cotton.Sync.Tests` passed 118/118, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: `SyncEngine` now filters ignored paths while building reconciliation indexes too, so future remote crawler implementations or stale ignored baselines cannot drive local writes/deletes. Focused `SyncEngineTests` passed 48/48, full `Cotton.Sync.Tests` passed 119/119, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: `SyncEngine` now deletes stale ignored baseline entries from sync state before reconciliation, so old `.cotton-sync` or temporary baselines cannot keep influencing later sync runs. Focused `SyncEngineTests` passed 48/48, full `Cotton.Sync.Tests` passed 119/119, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: `AtomicLocalFileSyncWriter` now rejects ignored sync payload paths at the local writer boundary, so `.cotton-sync` and temporary paths cannot be written, deleted, created, or used for conflict files even if a caller bypasses `SyncEngine` filtering. Focused writer tests passed 7/7, full `Cotton.Sync.Tests` passed 120/120, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
- [x] Add symlink/reparse-point policy.
  Verification: local scanning skips `FileAttributes.ReparsePoint` entries and does not traverse reparse-point directories through `EnumerationOptions.AttributesToSkip`; `ScanAsync_IgnoresSymlinkFilesAndDoesNotTraverseSymlinkDirectories` passed in `LocalFileScannerTests` 4/4 and full `Cotton.Sync.Tests` 45/45.
- [x] Add empty-directory reconciliation.
  Required behavior: upload local-only empty folders, create remote-only empty folders locally, track directory baselines, and apply safe folder deletes without data loss.
  Partial 2026-06-04: `LocalFileScanner` now implements `ILocalTreeScanner` and returns `LocalTreeSnapshot` with directory and file snapshots, while legacy file-only scanning remains compatible. Ignore rules now apply to every path segment so temporary/metadata directories are excluded with their contents. Focused `LocalFileScannerTests` passed 8/8, full `Cotton.Sync.Tests` passed 98/98, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Partial 2026-06-04: added `IRemoteDirectorySynchronizer` and SDK-backed `SdkRemoteDirectorySynchronizer` for remote folder create/delete operations through the node API. Focused `SdkRemoteDirectorySynchronizerTests` passed 1/1, full `Cotton.Sync.Tests` passed 99/99, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Partial 2026-06-04: `ILocalFileSyncWriter` now has explicit local directory create/delete operations; `AtomicLocalFileSyncWriter.DeleteDirectoryAsync` moves directory content into `.cotton-sync/deleted` instead of permanently deleting it. Focused `AtomicLocalFileSyncWriterTests` passed 6/6, full `Cotton.Sync.Tests` passed 102/102, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Partial 2026-06-04: `SyncEngine` now reconciles no-baseline directories before files, creating local folders for remote-only directories and creating remote folders for local-only directories in parent-first order. Desktop composition passes `SdkRemoteDirectorySynchronizer` into the sync engine. Focused directory create tests passed 2/2, full `Cotton.Sync.Tests` passed 104/104, full `Cotton.Sync.Desktop.Tests` passed 145/145, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Partial 2026-06-04: `SqliteSyncStateStoreTests` now explicitly verify `SyncEntryKind.Directory` roundtrip with `RemoteNodeId` and no file hashes, so directory baselines are covered at the EF SQLite state boundary. Focused state-store tests passed 10/10, full `Cotton.Sync.Tests` passed 105/105, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Partial 2026-06-04: baseline-known empty directory deletes now use the same per-run delete guard as files. The engine deletes local empty folders only through `ILocalFileSyncWriter.DeleteDirectoryAsync`, deletes remote empty folders only through `IRemoteDirectorySynchronizer.DeleteDirectoryAsync`, keeps non-empty folders intact, and does not cascade parent folder deletes inside the same run after deleting a child folder. Focused directory delete tests passed 5/5, full `Cotton.Sync.Tests` passed 110/110, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Partial 2026-06-04: local writer delete operations now preserve parent directories, preventing remote file deletes or child folder deletes from implicitly removing folder baselines. Focused writer/sync regression tests passed 8/8, full `Cotton.Sync.Tests` passed 112/112, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Partial 2026-06-04: local and remote empty directory renames are covered as full-mirror create-new-folder plus delete-old-empty-folder reconciliation, preserving the new baseline and removing the old one. Focused rename tests passed 2/2, full `Cotton.Sync.Tests` passed 114/114, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: local and remote empty directory moves into an already synchronized parent are covered as full-mirror create-new-folder plus delete-old-empty-folder reconciliation, including the remote parent id assertion for uploaded moves. Focused move tests passed 2/2, full `Cotton.Sync.Tests` passed 116/116, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
- [x] Add Windows path validation.
  Required checks: reserved names, invalid characters, path length, case collisions.
  Verification: `SyncPath.Normalize` rejects portable-Windows-invalid paths through `SyncPathValidationException`: reserved device names, reserved/control characters, trailing dot/space segments, overlong segments, and overlong relative paths; case-insensitive collisions are rejected by `SyncPathCollisionException`. `SyncPathTests` passed 12/12, `SyncEngineTests` passed 21/21, full `Cotton.Sync.Tests` passed 62/62, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
- [x] Add Linux path validation.
  Required checks: case-sensitive collisions with remote case-insensitive model assumptions, file permissions.
  Verification: `SyncEngine` rejects case-insensitive collisions from Linux case-sensitive local paths through `SyncPathCollisionException`, and `LocalFileScanner` rejects Unix files with no read permission bits through `LocalFileUnavailableException`; `ScanAsync_ThrowsForUnreadableUnixFile` passed in `LocalFileScannerTests` 6/6, full `Cotton.Sync.Tests` passed 64/64, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
- [x] Add Unicode normalization policy.
  Verification: `SyncPath.Normalize` normalizes relative paths to Unicode Form C before validation/keying, so composed and decomposed names share the same sync identity; `Normalize_UsesUnicodeFormC` passed in `SyncPathTests` 13/13, full `Cotton.Sync.Tests` passed 63/63, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
- [x] Add tests for every local/remote/base create-update-delete combination.
  Verification 2026-06-03: commit `Cover baseline reconciliation matrix`; `RunOnceAsync_ReconcilesBaselineMatrix` covers the 9 baseline-present combinations for local/remote missing, unchanged, and changed states. The matrix exposed and fixed two unsafe delete-vs-update cases so local delete plus remote update and local update plus remote delete now preserve conflicts instead of silently choosing one side. Focused matrix tests passed 9/9, full `Cotton.Sync.Tests` passed 74/74, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
- [x] Add tests for conflict naming uniqueness.
  Verification: `CreateConflictRelativePath_UsesIndexedSuffixWhenTimestampNameExists` proves conflict copy names are indexed instead of overwriting an existing conflict file with the same timestamp; `AtomicLocalFileSyncWriterTests` passed 3/3, full `Cotton.Sync.Tests` passed 50/50, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
- [x] Add tests for mass delete guard.
  Verification: `RunOnceAsync_BlocksRemoteDeletesOverRunLimit` and `RunOnceAsync_BlocksLocalDeletesOverRunLimit` prove over-limit delete runs emit `Skipped` activities without partially deleting files or baseline state; `SyncEngineTests` passed 20/20 and full `Cotton.Sync.Tests` passed 47/47.
- [x] Add tests for locked or unreadable files.
  Verification: `ScanAsync_ThrowsForLockedFile` holds a file with `FileShare.None`, and `ScanAsync_ThrowsForUnreadableUnixFile` removes Unix read permission bits; both verify that scanning fails with `LocalFileUnavailableException` carrying the affected relative and full paths. `LocalFileScannerTests` passed 6/6 and full `Cotton.Sync.Tests` passed 64/64.
- [x] Add tests for Windows reserved file names.
  Verification: `Normalize_RejectsWindowsReservedDeviceNames`, `Normalize_RejectsWindowsReservedCharacters`, `Normalize_RejectsWindowsTrailingDotOrSpace`, `Normalize_RejectsTooLongPathSegments`, and `Normalize_RejectsTooLongRelativePaths` cover reserved Windows names and path constraints; `SyncPathTests` passed 12/12 and full `Cotton.Sync.Tests` passed 62/62.
- [x] Add tests for case conflicts.
  Verification: `SyncEngine` rejects case-insensitive local and remote path collisions before reconciliation with `SyncPathCollisionException`; `RunOnceAsync_RejectsLocalCaseInsensitivePathCollision` and `RunOnceAsync_RejectsRemoteCaseInsensitivePathCollision` passed in `SyncEngineTests` 20/20, full `Cotton.Sync.Tests` passed 47/47, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
- [x] Add tests for crash during download.
  Verification: `RunOnceAsync_DoesNotUpdateBaselineWhenRemoteDownloadFails` verifies failed download preserves the existing local file, removes `.cotton-sync/tmp` partials, and leaves baseline unchanged; full `Cotton.Sync.Tests` passed 65/65 and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
  Verification 2026-06-04: `RunOnceAsync_RecoversAfterRemoteDownloadBeforeBaselineUpdate` now covers the sharper crash point where the remote file was already atomically written locally but SQLite baseline persistence failed. The next run sees matching local/remote content, records the baseline, emits no duplicate activity, and does not upload a duplicate. Focused crash-download test passed 1/1, and full `Cotton.Sync.Tests` passed 121/121.
  Verification 2026-06-04: `SyncCliCommandRunnerTests.SyncOnce_ExternalProcessRecoversAfterCrashDuringRemoteDownload` starts a real external `dotnet Cotton.Sync.Cli.dll sync-once` process against a local HTTP server that blocks halfway through the first download, kills the process after the partial `.cotton-sync/tmp/*.download` file exists, then starts a second external CLI process with the same local root and SQLite state database. The recovery run removes the stale temp file, downloads the complete remote file, records one baseline, and reports `State entries: 1`. Full `Cotton.Sync.Cli.Tests` passed 16/16.
- [x] Add tests for crash after remote upload before baseline update.
  Verification: `RunOnceAsync_RecoversAfterRemoteUploadBeforeBaselineUpdate` simulates state-store failure after remote upload, then verifies the next run adopts the remote file with matching content without uploading again; `SyncEngineTests` passed 22/22, full `Cotton.Sync.Tests` passed 65/65, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
  Verification 2026-06-04: sync-core crash recovery now also covers delete-side baseline failure. `RunOnceAsync_RecoversAfterRemoteDeleteBeforeBaselineDelete` proves a remote delete that succeeded before SQLite baseline delete failure is not repeated on the next run, and `RunOnceAsync_RecoversAfterLocalDeleteBeforeBaselineDelete` proves a local safe-delete that succeeded before baseline delete failure is adopted without upload/delete side effects. Focused recovery tests passed 4/4, full `Cotton.Sync.Tests` passed 123/123, and full solution Release build passed with 0 warnings.
- [x] Run full sync test suite.
  Verification: `dotnet test src/Cotton.Sync.Tests/Cotton.Sync.Tests.csproj --configuration Release --no-restore` passed 89/89 and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.

## Phase 6 - Continuous Sync

- [x] Add local file watcher abstraction.
  Verification: `ILocalSyncRootWatcher`, `ILocalSyncRootWatcherFactory`, and `LocalChangeSyncCoordinator` isolate watcher behavior from sync supervision; `LocalChangeSyncCoordinatorTests` passed 3/3, full `Cotton.Sync.App.Tests` passed 77/77, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
- [x] Implement Windows/Linux watcher adapter with fallback periodic scan.
  Verification: `FileSystemLocalSyncRootWatcher` uses cross-platform `FileSystemWatcher`, `FileSystemLocalSyncRootWatcherTests` passed 2/2 for missing-root and file-event publication, and `PeriodicSyncCoordinator` remains the safety fallback when watcher events are missed; full `Cotton.Sync.App.Tests` passed 77/77.
- [x] Add watcher debounce and event coalescing.
  Verification: `LocalChangeSyncCoordinator` cancels superseded pending requests per sync pair and emits one sync request after the debounce interval; `LocalChanges_AreCoalescedIntoOneSyncRequest` passed in `LocalChangeSyncCoordinatorTests` 3/3 and full `Cotton.Sync.App.Tests` passed 77/77.
  Verification 2026-06-04: `LocalChangeSyncCoordinator.StartAsync` now cleans already-created watchers if a later watcher fails during startup, preventing stale local filesystem subscriptions after partial startup failure. Focused `LocalChangeSyncCoordinatorTests` passed 4/4, full `Cotton.Sync.App.Tests` passed 102/102, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: `LocalChangeSyncCoordinator.StopAsync` now cancels and waits for debounced local sync requests that already reached the supervisor, preventing old local file-change triggers from surviving coordinator shutdown or session replacement. Focused local/remote coordinator tests passed 12/12, full `Cotton.Sync.App.Tests` passed 106/106, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
- [x] Add SignalR remote event listener through SDK.
  Verification: commit `Add SDK realtime event hub client`; `CottonRealtimeClient` connects to `Routes.V1.EventHub` with SDK token storage, publishes file-tree mutation wake-up events and session-revoked events. `dotnet test src/Cotton.Sdk.Tests/Cotton.Sdk.Tests.csproj --configuration Release --filter FullyQualifiedName~CottonRealtimeClientTests` passed 2/2, and `dotnet build src/Cotton.sln --configuration Release` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
- [x] Connect SignalR events to changes API fetch.
  Verification: commit `Connect realtime events to sync requests`; `RealtimeRemoteChangeSyncCoordinator` subscribes to SDK remote file-tree events, debounces them into `SyncAllAsync`, and desktop composition wires it into `SyncApplicationService`. Sync-pair work then reads and acknowledges the durable changes API page. `dotnet test src/Cotton.Sync.App.Tests/Cotton.Sync.App.Tests.csproj --configuration Release --no-restore --filter FullyQualifiedName~RealtimeRemoteChangeSyncCoordinatorTests` passed 3/3, `dotnet test src/Cotton.Sync.App.Tests/Cotton.Sync.App.Tests.csproj --configuration Release --no-restore --filter FullyQualifiedName~SyncApplicationServiceTests` passed 16/16, and `dotnet build src/Cotton.sln --configuration Release` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
  Verification 2026-06-04: `RealtimeRemoteChangeSyncCoordinator.StartAsync` now unsubscribes and stops the realtime client if SignalR startup fails after event subscription, preventing remote/session events from firing after a failed app startup. Focused `RealtimeRemoteChangeSyncCoordinatorTests` passed 6/6, full `Cotton.Sync.App.Tests` passed 103/103, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: `RealtimeRemoteChangeSyncCoordinator.StopAsync` now cancels and waits for debounced remote sync requests that already reached the supervisor, preventing old SignalR wake-up triggers from surviving realtime shutdown or session replacement. Focused local/remote coordinator tests passed 12/12, full `Cotton.Sync.App.Tests` passed 106/106, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: `RealtimeRemoteChangeSyncCoordinator` now cancels every pending remote-triggered sync request when a session-revoked event arrives, including a request that already entered `SyncAllAsync` before the revocation handler completes. Focused realtime coordinator tests passed 8/8, full `Cotton.Sync.App.Tests` passed 115/115, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-04: `DesktopSyncApplicationFactoryTests.Create_WiresContinuousSyncCoordinators` now locks desktop composition to real local watcher, SignalR remote, and periodic sync coordinators instead of the app-layer null fallbacks. Full `Cotton.Sync.Desktop.Tests` passed 250/250.
- [x] Connect sync-pair runs to changes API checkpointing.
  Verification: commit `Connect sync work to remote change feed`; `RemoteChangeAwareSyncPairWork` reads change-feed pages before full sync and acknowledges only after successful sync, with expired cursors recovered to the earliest retained cursor after full recrawl. `dotnet test src/Cotton.Sync.App.Tests/Cotton.Sync.App.Tests.csproj --configuration Release --no-restore --filter FullyQualifiedName~RemoteChangeAwareSyncPairWorkTests` passed 3/3, `dotnet test src/Cotton.Sync.Tests/Cotton.Sync.Tests.csproj --configuration Release --no-restore --filter FullyQualifiedName~RemoteChangeFeedReaderTests` passed 4/4, and `dotnet build src/Cotton.sln --configuration Release` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
- [x] Add periodic reconcile as safety fallback.
  Verification: commit `Add periodic sync safety fallback`; `PeriodicSyncCoordinator` requests `SyncAllAsync` on a 10-minute default interval and is wired into desktop application lifecycle. `dotnet test src/Cotton.Sync.App.Tests/Cotton.Sync.App.Tests.csproj --configuration Release --no-restore --filter FullyQualifiedName~PeriodicSyncCoordinatorTests` passed 2/2, `dotnet test src/Cotton.Sync.App.Tests/Cotton.Sync.App.Tests.csproj --configuration Release --no-restore --filter FullyQualifiedName~SyncApplicationServiceTests` passed 16/16, and `dotnet build src/Cotton.sln --configuration Release` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
- [x] Add offline detection and backoff.
  Verification: commit `Add sync runner offline retry`; `SyncPairRunner` classifies transient network failures and 5xx/timeout/429 HTTP failures as `Offline`, with exponential bounded backoff between attempts. `dotnet test src/Cotton.Sync.App.Tests/Cotton.Sync.App.Tests.csproj --configuration Release --no-restore --filter FullyQualifiedName~SyncPairRunnerTests` passed 8/8, and `dotnet build src/Cotton.sln --configuration Release` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
- [x] Add retry policy with bounded attempts and user-visible error state.
  Verification: commit `Add sync runner offline retry`; retry attempts are capped by `SyncPairRunnerRetryOptions`, transient final failures surface as `Offline`, and non-transient failures remain `Error` with `LastError`.
- [x] Add per-pair queue so repeated triggers collapse into one sync pass.
  Verification: commit `Coalesce sync pair run requests`; `SyncPairRunner` collapses overlapping `SyncNowAsync` requests into one queued follow-up run. `dotnet test src/Cotton.Sync.App.Tests/Cotton.Sync.App.Tests.csproj --configuration Release --no-restore --filter FullyQualifiedName~SyncPairRunnerTests` passed 9/9, and `dotnet build src/Cotton.sln --configuration Release` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
  Verification 2026-06-04: pause/stop now clear pending per-pair queue state before waiting on the active run, preventing repeated triggers from surviving an explicit user pause or shutdown. `SyncPairRunnerTests` passed 20/20, full `Cotton.Sync.App.Tests` passed 112/112, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
- [x] Add tests for local watcher event coalescing.
  Verification: `LocalChanges_AreCoalescedIntoOneSyncRequest` covers coalesced local watcher events per sync pair; focused continuous-sync tests passed 19/19, full `Cotton.Sync.App.Tests` passed 77/77, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
- [x] Add tests for remote SignalR trigger causing changes fetch.
  Verification: `RealtimeRemoteChangeSyncCoordinatorTests` cover SignalR wake-up debounce into sync requests, and `RemoteChangeAwareSyncPairWorkTests` cover change-feed read/acknowledge around sync work; focused continuous-sync tests passed 19/19 and full `Cotton.Sync.App.Tests` passed 77/77.
- [x] Add tests for offline to online recovery.
  Verification: `SyncNowAsync_RetriesTransientNetworkFailureAndReturnsIdleOnRecovery` covers transient offline recovery back to `Idle`; focused continuous-sync tests passed 19/19 and full `Cotton.Sync.App.Tests` passed 77/77.
- [x] Add two-client integration test against local dev server.
  Verification: `SyncClientEndToEndTests` covers two-client create/update propagation, rename/move propagation, and simultaneous edit conflict against the local integration server. Focused 8-test sync e2e run passed, full `dotnet test src/Cotton.Server.IntegrationTests/Cotton.Server.IntegrationTests.csproj --configuration Release --logger "console;verbosity=minimal"` passed 363/363, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
- [ ] Run continuous sync soak test for at least 2 hours before this phase is considered done.
  Partial 2026-06-04: added `Cotton.Sync.Cli sync-soak` as a repeatable release soak harness. It signs in through the SDK, reuses the same sync state database and `SyncEngine`, repeats full-mirror sync passes by iteration count or duration, optionally updates a relative probe file inside the local root before each pass, and prints iteration/activity/state-entry totals plus elapsed seconds, CPU seconds, peak working set bytes, peak managed memory bytes, and a zero-failure summary. Verification: `SyncCliCommandRunnerTests` cover help, required soak limiter validation, invalid probe path rejection, and a one-iteration fake-server soak upload with metric output; full `Cotton.Sync.Cli.Tests` passed 12/12, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings. Keep unchecked until the actual 2-hour run completes and records final convergence/memory/error evidence.
  Partial 2026-06-04: tightened `sync-soak` so the harness runs one final no-op convergence pass after the measured loop and reports `Final convergence activities`, `Final state entries`, `Converged`, and `Failures`; the command exits non-zero when the final pass still produces sync activity. Verification: full `Cotton.Sync.Cli.Tests` passed 12/12, `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings, and `git diff --check` passed.
  Partial 2026-06-04: added negative soak coverage so `SyncCliCommandRunnerTests.SyncSoak_ReturnsFailureWhenFinalConvergenceStillHasActivities` forces the final convergence pass to emit activity and verifies exit code `1`, `Converged: no`, and `Failures: 1`. Verification: full `Cotton.Sync.Cli.Tests` passed 14/14, `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings, and `git diff --check` passed.

## Phase 7 - Authentication And Token Storage

- [x] Keep password/TOTP login as the first implemented auth flow.
  Verification: `PasswordAuthFlow` maps username/password plus optional TOTP into SDK `LoginRequestDto`, trims user-entered non-secret fields, and returns `AuthSession`; `PasswordAuthFlowTests` passed 5/5, focused auth/service tests passed 21/21, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
- [x] Add `IAuthFlow` abstraction.
  Required implementations: password flow now, browser flow placeholder later.
  Verification: `IAuthFlow` is the app-layer auth boundary used by `SyncApplicationService`, with `PasswordAuthFlow` as the current implementation; focused auth/service tests passed 21/21 and full `Cotton.Sync.App.Tests` passed 77/77.
- [x] Add named-device metadata where server supports it.
  Verification: commit `Send desktop device metadata`; SDK `CottonSdkOptions` now supports `UserAgent` and `DeviceName`, desktop factory sends a generated Cotton Sync Desktop user agent and device name, and server auth session issuing stores shared-contract `X-Cotton-Device-Name` into the existing refresh-token `Device` field when present. `CottonAuthClientTests` passed 6/6, full `Cotton.Sdk.Tests` passed 21/21, `AuthSmokeTests` passed 5/5 including session device persistence, and `dotnet build src/Cotton.sln --configuration Release` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
- [x] Add secure token store abstraction.
  Verification: commit `Add token payload protection boundary`; desktop token persistence now writes a protected envelope through `ITokenPayloadProtector`, with tests proving the file store uses the protector, rejects mismatched protection schemes, ignores unreadable protected payloads, roundtrips valid tokens, clears tokens, and keeps Unix file permissions restricted. `dotnet test src/Cotton.Sync.Desktop.Tests/Cotton.Sync.Desktop.Tests.csproj --configuration Release --no-restore --filter FileCottonTokenStoreTests` passed 9/9.
- [x] Implement Windows secure token store.
  Preferred target: Windows Credential Manager or DPAPI-backed per-user storage.
  Verification: commit `Add token payload protection boundary`; `WindowsDpapiTokenPayloadProtector` uses DPAPI current-user protection via `System.Security.Cryptography.ProtectedData` and is selected by `DesktopTokenPayloadProtectorFactory` on Windows. The Windows-only roundtrip test is present in `FileCottonTokenStoreTests` and runs on Windows; Linux build/test verification passed, while manual Windows verification remains tracked below.
- [x] Implement Linux secure token store.
  Preferred target: Secret Service/libsecret where available.
  Verification 2026-06-03: added `LinuxSecretServiceTokenPayloadProtector`, selected by `DesktopTokenPayloadProtectorFactory` on Linux when `secret-tool` is present in `PATH`; token files store only the Secret Service payload id, and `FileCottonTokenStore` now cleans external protected payloads on overwrite, logout, and failed token-file commits. Focused auth storage tests passed 22/22, full `Cotton.Sync.Desktop.Tests` passed 85/85, and full solution Release build passed with the known NU1903 Avalonia/Tmds.DBus.Protocol warning. Local environment did not have `secret-tool`, so real Secret Service manual verification remains open below.
- [x] Add explicit fallback policy for Linux environments without a secret service.
  Release decision must be explicit: block login, prompt user, or encrypted local store.
  Verification: commit `Add token payload protection boundary`; current non-Windows fallback is explicitly `RestrictedFileTokenPayloadProtector` with a protected-envelope scheme and chmod-restricted token file. This is treated as a development fallback only; release policy checks are recorded below.
  Verification 2026-06-03: self-test now includes `Token storage` and reports restricted-file storage as a development fallback that is not release-secure. `DesktopTokenStorageCapabilitiesTests` cover restricted-file, Linux Secret Service, and Windows DPAPI capability classification.
  Verification 2026-06-04: the desktop self-test now fails `Token storage` when the active protector is not release-secure, so Linux environments without Secret Service no longer pass the release smoke gate silently. Focused self-test/CLI/token-storage tests passed 20/20, full `Cotton.Sync.Desktop.Tests` passed 141/141, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: desktop sign-in now refuses to create an application host or save tokens when token storage is not release-secure, and startup session restore skips stored sessions under the same condition. Focused `DesktopShellControllerHostLifecycleTests` passed 4/4.
  Verification 2026-06-04: release-secure token storage now performs a live protect/unprotect cleanup probe before self-test, sign-in, or startup session restore accept it, so Linux `secret-tool` must have a working Secret Service session instead of merely existing in `PATH`. Focused token-storage/lifecycle/self-test/CLI tests passed 26/26.
- [x] Ensure logout clears tokens and stops all sync runners.
  Verification: `SyncApplicationService.SignOutAsync` stops remote changes, periodic sync, local changes, and supervisor before delegating to `IAuthFlow.SignOutAsync`; SDK `CottonAuthClient.LogoutAsync` clears `ICottonTokenStore`, including failed server logout responses. `SyncApplicationServiceTests` plus `PasswordAuthFlowTests` passed in focused auth/service tests 21/21, and `CottonAuthClientTests` passed 3/3.
  Verification 2026-06-04: desktop sign-out now also clears transient setup secrets in the shell, including password and TOTP fields, closes settings/add-folder surfaces, and disables sign-out commands after the session is gone. Focused `ShellViewModelSyncPairCommandTests` passed 21/21, full `Cotton.Sync.Desktop.Tests` passed 142/142, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
- [x] Handle refresh-token revocation and session-revoked SignalR event.
  Verification: `CottonAuthClient.LogoutAsync` clears saved tokens in a `finally` block when server logout fails, `RealtimeRemoteChangeSyncCoordinator` subscribes to SDK `SessionRevoked`, cancels pending remote-triggered sync, invokes `SessionRevocationHandler`, and stops realtime observation. `SessionRevocationHandler` stops periodic/local sync, signs out, and stops the supervisor while logging and continuing through non-cancellation failures. Focused `CottonAuthClientTests` passed 3/3; focused `RealtimeRemoteChangeSyncCoordinatorTests|SessionRevocationHandlerTests` passed 7/7; full `Cotton.Sdk.Tests` passed 18/18; full `Cotton.Sync.App.Tests` passed 81/81; `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
- [x] Add tests for login, refresh, logout, token clear, and invalid saved token.
  Verification: `PasswordAuthFlowTests` cover login/TOTP/logout delegation, `CottonHttpTransportTests|CottonAuthClientTests` passed 3/3 for refresh/logout/token clear behavior, and `FileCottonTokenStoreTests` cover token clear, invalid saved-token rejection, and external protected-payload cleanup.
- [ ] Add manual verification on Windows secure storage.
- [ ] Add manual verification on Linux secure storage.

## Phase 8 - Desktop UX And Visual Design

- [ ] Keep desktop UI customization minimal and intentional.
  Required behavior: prefer Avalonia/default component sizing, layout, scrolling, and control behavior unless a product-specific reason requires an override. Avoid hard-coded widths/heights/max-widths, nested card-like surfaces, and custom control behavior when the standard component already solves the layout. Any remaining explicit size constraints must be justified by visual QA evidence for compact Windows and Linux windows.
  Partial 2026-06-06: the setup/sign-in form no longer pins the content stack to a fixed 296px width. It now stretches with simple side margins so compact windows keep the same usable width while wider/debug profiles can use the available space without another hard-coded form size. Focused visual-contract test passed 1/1.
- [x] Use one desktop icon source for repeated command icons.
  Required behavior: common UI actions such as save, remove, open, sync, pause/resume, settings, and diagnostics must use a maintained Avalonia icon source instead of emoji, ad hoc SVG paths, or text placeholders when a standard icon exists. Product/tray/app icons remain branded assets. Icons must render monochrome by default through theme brushes, and visual-contract tests must prevent colored emoji regressions.
  Partial 2026-06-06: desktop now references `Material.Icons.Avalonia`, registers `MaterialIconStyles`, and uses monochrome Material icons for the folder editor save/remove commands instead of colored emoji or inline SVG path data. Focused visual-contract tests cover icon-library registration and reject emoji/path regressions. Verification: `dotnet test src/Cotton.Sync.Desktop.Tests/Cotton.Sync.Desktop.Tests.csproj --configuration Release --no-restore` passed 343/343, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-06: repeated command icons in the compact dashboard, action-required/conflict rows, folder rows, expanded folder controls, add-folder wizard, remote cloud picker, and settings close action now use Material icons instead of glyph text such as `+`, `...`, arrows, checkmarks, or close symbols. Focused visual-contract tests reject the old symbol content for command buttons. Verification: `dotnet test src/Cotton.Sync.Desktop.Tests/Cotton.Sync.Desktop.Tests.csproj --configuration Release --no-restore` passed 343/343, `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings, and Linux window-bound smoke screenshots were captured and reviewed under `/tmp/cotton-sync-icon-source-current` for dashboard, folder-controls, add-folder, progress, and settings.
  Partial 2026-06-06: refresh actions now use the plain Material `Refresh` icon instead of `RefreshCircle`, removing the visible circular glyph background from the dark dashboard toolbar, action-required retry, and conflicts retry buttons. Focused visual-contract tests passed 28/28 and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-06: refresh actions now use transparent `flatIcon` button chrome and the Cotton primary brush for the glyph, preventing the Windows/default button background from reading as a white circular refresh badge. Focused visual-contract tests passed 29/29, full `Cotton.Sync.Desktop.Tests` passed 361/361, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-06: the remaining sign-in server-verified check and cloud-folder row chevron now use monochrome Material icons instead of text glyph placeholders. Focused visual-contract tests passed 4/4 and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-06: current desktop XAML search found no repeated command icon placeholders using text glyphs such as `+`, `...`, arrows, checkmarks, close symbols, or inline `<Path Data=...>` shapes. All repeated dashboard, folder, add-folder, settings, refresh, close, menu, and cloud-picker commands now use `materialIcons:MaterialIcon` from `Material.Icons.Avalonia` with theme foreground brushes; product/tray/app icons remain branded assets. Focused icon visual-contract tests passed 7/7, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings.
- [ ] Verify desktop notification identity and icons.
  Required behavior: Windows toast notifications from the installed app must show the user-facing app name `Cotton Sync`, not the raw process or AUMID such as `Cotton.Sync.Desktop`, and should use the packaged app icon where the platform supports it. Linux notifications must use the same app name and should pass an icon when the installed desktop entry/icon theme can resolve one. Debug launches may differ, but release readiness requires installed Windows and Linux package smoke evidence for sign-in, sign-out, initial-sync-complete, conflict, and action-required notifications.
  Partial 2026-06-06: desktop notification adapters now pass the packaged `Assets/icon-192.png` when it is available: Windows toast XML adds `appLogoOverride`, and Linux `notify-send` receives `--icon` while keeping `--app-name "Cotton Sync"`. Focused platform notification tests passed 11/11. Keep unchecked until installed Windows/Linux package smokes prove the user-facing app name and icon in real desktop notification centers; debug launches may still show the raw AUMID/process identity.
  Partial 2026-06-06: desktop self-test now reports notification adapter identity details instead of a bare `Supported` value: adapter name, user-facing app name, Windows AppUserModelID, executable path, and icon path or missing-icon state. Focused notification factory/self-test tests passed 32/32, desktop Release build passed with 0 warnings, and `git diff --check` passed. Keep unchecked until installed Windows/Linux notification centers are visually verified.
- [ ] Verify tray icon state transitions.
  Required behavior: the idle/connected tray icon is the normal Cotton icon. The syncing tray icon is shown only while active sync work is in progress; paused, offline, signed-out, and action-required states use distinct status icons. Do not show a sync/status overlay when everything is healthy and idle.
  Partial 2026-06-06: idle/connected tray status now resolves to the plain packaged Cotton icon (`Assets/icon-192.png`) instead of a status-overlay tray asset, and the obsolete `tray-idle.png` asset was removed so healthy idle packages do not carry a misleading badge icon. Keep unchecked until Windows tray/taskbar visual verification proves the OS renders the expected icon.
- [x] Replace prototype screen with release-grade app structure.
  Verification 2026-06-06: the desktop shell is no longer a prototype single screen. `MainWindow.axaml` separates setup, signed-in dashboard, add-folder wizard, settings overlay, action-required, conflict, progress, folders, and activity surfaces; `ShellViewModel` exposes deterministic visual-smoke states for first-run, sign-in-error, empty-dashboard, add-folder, dashboard, folder-controls, progress, settings, settings-diagnostics, error, and conflict; the refreshed compact screenshot matrix under `/tmp/cotton-sync-responsive-verify` was reviewed without overlap or primary-action clipping. Onboarding completion, manual walkthroughs, and clean-machine screenshot review remain tracked as separate release gates.
- [x] Add onboarding flow.
  Steps: welcome, sign in, optional autostart, add first sync pair, initial sync, finished state.
  Partial 2026-06-03: sign-in onboarding now keeps a fresh app database empty, remembers the saved server URL and username as sign-in hints after real user input, and skips session-restore network calls when the token store is empty. Focused `DesktopShellControllerSelfTestTests` passed 12/12, full `Cotton.Sync.Desktop.Tests` passed 119/119, and full solution Release build passed with the known NU1903 warning. Keep unchecked until the full onboarding flow, first-pair setup, initial sync, and screenshot QA are verified together.
  Partial 2026-06-04: sign-in startup/probe/sign-in URL handling now accepts bare server hosts such as `app.cottoncloud.dev` and normalizes them to HTTPS while still rejecting unsupported schemes. Focused startup URL tests passed 21/21, full `Cotton.Sync.Desktop.Tests` passed 133/133, full solution Release build passed with the known NU1903 warning, and a headless setup screenshot with `--server app.cottoncloud.dev` showed the normalized URL plus green Cotton Cloud verification without clipping.
  Partial 2026-06-04: manual sign-in server probing now rewrites a verified bare host in the input field to the normalized HTTPS URL before sign-in. Focused `ShellViewModelSyncPairCommandTests.ServerProbe_NormalizesVerifiedBareHostAndEnablesSignIn` passed, full `Cotton.Sync.Desktop.Tests` passed 134/134, and full solution Release build passed with the known NU1903 warning.
  Partial 2026-06-04: setup now behaves as a compact two-step flow instead of showing every login field at launch: the first window asks only for the server and probes it, then the sign-in step expands the window after Cotton Cloud verification. `ChangeServerCommand` returns to the server step and clears password/TOTP fields. `Cotton.Sync.Desktop.Tests` passed 136/136, full solution Release build passed with 0 warnings, and headless Xvfb screenshots were captured at `/tmp/cotton-setup.png` and `/tmp/cotton-signin.png`.
  Partial 2026-06-04: first successful sign-in with no configured sync folders now opens the add-folder wizard immediately, matching the expected install -> sign in -> choose local/cloud folders flow instead of dropping the user into an empty dashboard. Focused `ShellViewModelSyncPairCommandTests` passed 18/18, full `Cotton.Sync.Desktop.Tests` passed 137/137, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Partial 2026-06-04: opening the add-folder wizard now immediately prompts for the local folder through the desktop folder picker; a successful selection moves the user to the cloud-folder step, while cancel keeps the wizard on the local-folder step. Focused `ShellViewModelSyncPairCommandTests` passed 20/20, full `Cotton.Sync.Desktop.Tests` passed 139/139, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Revision 2026-06-04: after Windows walkthrough feedback, successful sign-in no longer opens the add-folder wizard automatically, and opening the wizard no longer launches the native folder picker automatically. The signed-in empty dashboard now waits for the explicit `Add a folder` action, and the wizard waits for the explicit local-folder `Change` action. Focused ViewModel/visual-contract tests passed 60/60, full `Cotton.Sync.Desktop.Tests` passed 258/258, `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings, and Linux Xvfb screenshots were refreshed under `/tmp/cotton-sync-ui-current`.
  Partial 2026-06-04: canceling the local-folder picker now leaves the add-folder wizard on the local step without fetching or preselecting cloud folders; cloud browsing starts only after a local folder is selected. Focused add-folder ViewModel tests passed 4/4, full `Cotton.Sync.Desktop.Tests` passed 144/144, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Partial 2026-06-04: after an initial local-folder picker cancel, choosing a folder from the wizard local step now advances directly into a populated cloud-folder browser instead of leaving the cloud step empty. Focused wizard tests passed 6/6, full `Cotton.Sync.Desktop.Tests` passed 145/145, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Partial 2026-06-04: setup no longer shows a misleading numbered `1 Server / 2 Account` stepper, and authentication failures now render directly on the sign-in form instead of silently updating a dashboard-only action-required state. Focused ViewModel tests cover failed sign-in staying on setup with a visible action-required message; `/tmp/cotton-start-no-stepper.png` was captured through Xvfb.
  Partial 2026-06-04: sign-in errors now normalize real backend auth messages into user-readable desktop text, including 403 `Invalid password`, missing/invalid 2FA, and TOTP lockout responses. Focused resolver tests passed 14/14, full `Cotton.Sync.Desktop.Tests` passed 193/193, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Partial 2026-06-06: sign-in credential fields now submit through `SignInCommand` for both Avalonia `Enter` and `Return` key events, covering Windows keyboard behavior where pressing Enter in the login form previously did not trigger sign-in. Verification: focused setup visual-contract tests passed 29/29, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-04: the sign-in ViewModel path now also covers the backend 403 `Invalid password` response, keeping the user on the sign-in step with `Invalid username or password.` instead of surfacing a raw backend string. Focused ViewModel tests passed 34/34, full `Cotton.Sync.Desktop.Tests` passed 194/194, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Partial 2026-06-04: setup window positioning now avoids pre-open manual `Position` writes and recenters only after the Avalonia window is opened or its profile changes. Full-size Xvfb screenshot `/tmp/cotton-desktop-smoke-full.png` shows the sign-in window centered with no clipping after `--server app.cottoncloud.dev`; full `Cotton.Sync.Desktop.Tests` passed 194/194 and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-06: onboarding now covers welcome/server verification, normalized Cotton server probe, remembered server/username hints, sign-in with visible auth/TOTP errors and Enter-key submit, explicit first-folder setup, explicit local folder picker, cloud folder browser, cloud folder creation, first-sync pending state, and finished/up-to-date state. Autostart is intentionally not shown on the sign-in screen after Windows UX review; it remains a post-login Settings/Startup preference. Focused onboarding/sign-in/add-folder tests passed 22/22, and the current desktop branch's full `Cotton.Sync.Desktop.Tests` passed 392/392 before this plan-only status update.
- [x] Add main dashboard.
  Required content: global status, sync-pair list, per-pair status, current progress, recent activity.
  Required progress depth: the activity list is not progress. Release UI must show a dedicated current-transfer area with a determinate progress bar when total bytes are known, an indeterminate progress state for scanning/reconciling/unknown totals, current file/folder name, direction, transferred bytes, total bytes, transfer speed, and ETA when enough samples exist.
  Required progress telemetry: sync pair id, operation kind, direction, local/remote path, item name, transferred bytes, total bytes when known, files completed, files total when known, started timestamp, latest sample timestamp, instantaneous or rolling average bytes per second, and completion/error state.
  Required progress behavior: chunk upload/download must report byte progress through the SDK and sync core, app layer must aggregate per-pair progress without blocking sync work, desktop must clear stale progress after completion/error, and paused/offline/action-required states must not show fake percentages.
  Required progress tests: SDK upload/download progress callback tests, sync-core activity/progress ordering tests, app-layer aggregation tests for speed and ETA, desktop ViewModel tests for determinate/indeterminate/current-file states, and screenshot/visual-contract checks proving the progress UI fits the compact window.
  Required ETA behavior: transfer-speed samples may update frequently, but `EstimatedTimeRemaining` must be smoothed with a rolling or damped estimator so large-file uploads do not show a visibly jumpy remaining-time prediction.
  Partial 2026-06-04: added a server-backed upload progress smoke. `SyncClientEndToEndTests.RunOnceAsync_ReportsUploadProgressThroughSdkToServer` now runs `SyncEngine` through the SDK/test server, uploads a multi-chunk local file, verifies transfer byte samples from 0 through completion, verifies aggregate run progress reaches the current file and completed state, verifies the upload activity stream, and round-trips the remote file content. Verification: focused test passed 1/1 and full `Cotton.Server.IntegrationTests` passed 369/369.
  Partial 2026-06-04: added sync-core ordering coverage for progress telemetry. `RunOnceAsync_ReportsRunTransferAndActivityProgressForUpload` now verifies one upload pass reports the current file in aggregate run progress, emits transfer start/completion byte samples, emits the upload activity only after transfer completion, and finishes with a completed run-progress event. Verification: focused test passed 1/1 and full `Cotton.Sync.Tests` passed 131/131.
  Partial 2026-06-04: transfer speed and ETA are now calculated in the app layer instead of being derived ad hoc by the desktop ViewModel. `AppTransferProgressEstimator` keeps a short rolling sample window per transfer, publishes `SpeedBytesPerSecond` and `EstimatedTimeRemaining` through `AppTransferProgress`, and `DesktopTransferProgressSnapshot` carries those values to the UI for formatting only. Verification: focused app/desktop progress tests passed 5/5 and 28/28, full `Cotton.Sync.App.Tests` passed 121/121, full `Cotton.Sync.Desktop.Tests` passed 277/277, `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings, and the `progress` screenshot smoke captured `/tmp/cotton-sync-progress-smoke/cotton-sync-desktop-linux-progress.png`.
  Partial 2026-06-06: `AppTransferProgressEstimator` now dampens `EstimatedTimeRemaining` with an aged previous estimate and separate increase/decrease smoothing factors, so a temporary slow sample cannot make the large-file remaining-time prediction jump sharply while rolling speed stays responsive. Verification: focused `AppTransferProgressEstimatorTests` passed 7/7, full `Cotton.Sync.App.Tests` passed 149/149, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-06: the desktop dashboard now aggregates active transfer metrics across multiple syncing folders instead of showing only the latest transfer in the global progress header. When two or more folders are syncing, the compact progress card keeps `Syncing N folders` on the left and right-aligns summed transferred/total bytes, summed speed, and aggregate ETA, while the secondary line collapses to `N files transferring`. Verification: focused progress ViewModel tests passed 6/6, full `Cotton.Sync.Desktop.Tests` passed 387/387, `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings, and `/tmp/cotton-sync-aggregate-progress/progress.png` was reviewed without clipping or overlap.
  Partial 2026-06-04: added a `progress` desktop visual-smoke scenario and included it in the Linux GUI screenshot matrix. The scenario renders an active sync pass with one compact combined progress card: current transfer, transferred bytes, speed/ETA, and aggregate file count details in the same card to keep the tray-sized dashboard from pushing Activity off-screen. Verification: focused desktop visual-smoke/progress/packaging tests passed 28/28, full `Cotton.Sync.Desktop.Tests` passed 277/277, `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings, and `smoke-gui-screenshot.sh` captured `/tmp/cotton-sync-progress-smoke/cotton-sync-desktop-linux-progress.png`, which was reviewed for clipping and spacing.
  Partial 2026-06-04: added aggregate sync-pass progress on top of byte-level transfer progress. `SyncEngine` now reports local scan, remote scan, directory reconciliation, file reconciliation, completed stage, `FilesCompleted`, `FilesTotal`, current file path, pass start time, and sample time through `SyncRunOptions.RunProgress`; `SyncEnginePairWork` maps it to app-level run progress; `DesktopShellController` exposes run-progress snapshots; `ShellViewModel` renders a compact pass-progress card with determinate file count progress when totals are known and indeterminate progress during scans. Verification: focused core/app/desktop run-progress tests passed, full `Cotton.Sync.Tests` passed 130/130, full `Cotton.Sync.App.Tests` passed 117/117, full `Cotton.Sync.Desktop.Tests` passed 273/273, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings. Keep unchecked until end-to-end transfer smoke on real server/client flows is verified.
  Partial 2026-06-04: added real transfer-progress telemetry for file upload/download. `SdkRemoteFileSynchronizer` reports upload bytes by chunk and download bytes through the SDK download progress callback; `SyncEngine` passes `SyncRunOptions.TransferProgress`; `SyncEnginePairWork` maps core progress to an app-level transfer-progress publisher; `DesktopShellController` exposes transfer snapshots; `ShellViewModel` renders a compact transfer card with progress value, current file, bytes, and speed/ETA when enough samples exist. Verification: full `Cotton.Sync.Tests` passed 129/129, full `Cotton.Sync.App.Tests` passed 116/116, full `Cotton.Sync.Desktop.Tests` passed 271/271, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings. Keep unchecked until end-to-end transfer smoke on real server/client flows is verified.
  Partial 2026-06-03: compact dashboard flyout now shows global status, account, sync-pair list/empty state, per-pair status rows, recent activity, quick add/sync actions, and a menu for secondary actions. Keep unchecked until screenshot-based visual QA confirms the dashboard hierarchy and spacing.
  Partial 2026-06-03: recent activity now receives real sync-core events through an app-layer `IAppActivityPublisher`; `SyncEnginePairWork` maps upload/download/delete/conflict/skipped progress into app activities, `DesktopShellController` exposes desktop activity snapshots, and `ShellViewModel` appends them to the dashboard activity list with a bounded history. Focused app/desktop activity tests passed, full `Cotton.Sync.App.Tests` passed 86/86, full `Cotton.Sync.Desktop.Tests` passed 95/95, and full solution Release build passed with the known NU1903 warning. Keep unchecked until screenshot-based visual QA confirms the dashboard hierarchy and spacing.
  Partial 2026-06-03: `SyncPairRunner` now exposes user-visible `CurrentOperation` values for syncing/offline/error/conflict states, `DesktopSyncPairStatusSnapshot` carries them into the shell, and the dashboard status card shows a compact current progress line such as `Documents: Uploading report.txt`. Focused app runner tests passed 11/11, focused desktop ViewModel tests passed 14/14, full `Cotton.Sync.App.Tests` passed 87/87, full `Cotton.Sync.Desktop.Tests` passed 115/115, and full solution Release build passed with the known NU1903 warning. Keep unchecked until screenshot-based visual QA confirms the dashboard hierarchy and spacing.
  Partial 2026-06-04: the dashboard no-sync-folder state was compacted from a large centered card into a small row action, reducing wasted space in the tray-sized window. Full `Cotton.Sync.Desktop.Tests` passed 146/146 and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings. Keep unchecked until refreshed dashboard/settings/conflict/error screenshots pass visual QA.
  Partial 2026-06-04: dashboard top actions were tightened after manual Windows review: the duplicate top add button and ambiguous global `Open` folder button were removed, each sync-folder row now owns its local-folder open action, compact text buttons match icon-button height, pause/resume actions are mutually available, and manual sync is disabled while sync is paused. Focused ViewModel tests cover row-specific folder opening and pause/resume/sync command availability.
  Partial 2026-06-04: dashboard progress now avoids claiming `All folders are up to date` until an enabled sync pair has a recorded successful sync baseline; `SyncPairRunner` carries `LastSuccessfulSyncAtUtc` through app status into the shell, disabled folders stay out of global sync/pause/resume status updates, and command failures switch the progress line to an action-required message. Focused app/controller/ViewModel tests passed, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Partial 2026-06-04: action-required and conflict dashboard rows now use compact icon actions with tooltips instead of narrow text buttons, and diagnostics export uses a compact label inside the Diagnostics section. `DesktopSetupVisualContractTests` passed 10/10, full `Cotton.Sync.Desktop.Tests` passed 190/190, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Partial 2026-06-04: the dashboard header `Sync now` command now uses the same 32px icon-button treatment as other narrow shell actions, keeping the compact toolbar visually consistent while preserving the tooltip. `DesktopSetupVisualContractTests` passed 14/14, full `Cotton.Sync.Desktop.Tests` passed 205/205, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: the dashboard header `Sync now` icon is now hidden when `CanSyncNow` is false instead of remaining as a disabled toolbar action while sync is paused, busy, or no enabled folders exist. `DesktopSetupVisualContractTests` now requires the `CanSyncNow` visibility binding.
  Partial 2026-06-04: the dashboard now stretches the Activity panel into the remaining window height instead of keeping it as a small fixed row when the shell is resized taller. The XAML visual contract now requires `RowDefinitions="Auto,*"` for dashboard content and keeps Activity in the `*` row. Focused ViewModel/visual-contract tests passed 60/60, full `Cotton.Sync.Desktop.Tests` passed 258/258, and refreshed Xvfb dashboard smoke screenshot `/tmp/cotton-sync-ui-current/cotton-sync-desktop-linux-dashboard.png` was reviewed.
  Partial 2026-06-04: dashboard sync folders no longer show a permanent duplicated selected-folder editor under the folder list. Folder rows now render as ordinary cards without `ListBox` selection chrome, row actions include open and manage, and the selected-folder controls appear only after the row manage action is used. Focused ViewModel/visual-contract tests passed 61/61, full `Cotton.Sync.Desktop.Tests` passed 263/263, `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings, and `/tmp/cotton-sync-folders-fix/cotton-sync-desktop-linux-dashboard.png` was reviewed.
  Partial 2026-06-04: the signed-in dashboard header no longer repeats the product name already present in the native window chrome; it now shows the account plus global status. The empty no-folder dashboard hides the redundant green status card and removes the obvious `Add a folder to start syncing` progress line, while dashboards with folders keep a single status-card line such as `All folders are up to date.`. Focused ViewModel/visual-smoke/visual-contract tests passed 110/110, full `Cotton.Sync.Desktop.Tests` passed 266/266, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-04: removed the low-value `Sync roots` subtitle from the dashboard `Folders` section after visual review; the folder list and add action already communicate the section purpose. `DesktopSetupVisualContractTests` passed 17/17, full `Cotton.Sync.Desktop.Tests` passed 266/266, `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings, and `/tmp/cotton-sync-no-sync-roots/cotton-sync-desktop-linux-dashboard.png` was reviewed.
  Partial 2026-06-04: the dashboard `Folders` add action now lives in the section header when folders already exist, removing the separate bottom row previously reserved for a tiny plus button. The empty no-folder state keeps its single explicit add-folder CTA. `DesktopSetupVisualContractTests` passed 17/17, full `Cotton.Sync.Desktop.Tests` passed 267/267, `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings, and `/tmp/cotton-sync-folders-header-add/cotton-sync-desktop-linux-empty-dashboard.png` plus `/tmp/cotton-sync-folders-header-add/cotton-sync-desktop-linux-dashboard.png` were reviewed.
  Partial 2026-06-04: tightened the dashboard `Folders` empty state again so the header is the only add-folder action in both empty and non-empty states. The reviewed smoke screenshot `/tmp/cotton-sync-empty-dashboard.png` shows one header plus button, a passive `No folders yet` empty state, and Activity stretching into the remaining compact window height. `DesktopSetupVisualContractTests` passed 19/19, full `Cotton.Sync.Desktop.Tests` passed 278/278, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-04: sync-folder management copy no longer refers to an ambiguous "selected local folder"; the inline folder controls now use the same `Open local folder` wording as row actions. Focused `DesktopSetupVisualContractTests.DashboardFolders_ExposeSelectedPairManagementActions` passed 1/1, full `Cotton.Sync.Desktop.Tests` passed 287/287, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-04: add-folder and settings overlays now hide the dashboard status/progress/action chrome behind them, so wizard-local errors are not visually stacked over background action-required banners. Focused overlay/visual-contract tests passed 5/5, full `Cotton.Sync.Desktop.Tests` passed 288/288, `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings, Linux publish passed, and `/tmp/cotton-sync-wizard-chrome/cotton-sync-desktop-linux-add-folder.png` was reviewed after republishing.
  Partial 2026-06-04: sync-folder rows now show their current operation line, such as upload/download file names or aggregate file-check progress, instead of hiding per-folder work behind only the global status card. The row operation visibility clears when the pair returns to idle. Focused ViewModel/visual-contract tests passed 78/78, and full `Cotton.Sync.Desktop.Tests` passed 290/290.
  Partial 2026-06-04: sync-folder rows now also show a compact per-folder progress bar while work is active. Status-only `Syncing`/`Scanning` rows use an indeterminate bar, while transfer/run progress events update determinate percentages and idle statuses clear stale row progress. Focused ViewModel/visual-contract tests passed 78/78, full `Cotton.Sync.Desktop.Tests` passed 290/290, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-04: refreshed the current compact progress screenshot at `/tmp/cotton-sync-progress-current/cotton-sync-desktop-linux-progress.png` after the latest dashboard changes. The reviewed state shows a dedicated current-transfer card with current file, transferred/total bytes, speed, ETA, aggregate file-count progress, and per-folder progress bars without clipping. Verification: `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings and the window-bound Linux visual-smoke `progress` capture passed PNG/runtime checks.
  Partial 2026-06-04: sync-folder management controls now expand inside the managed folder row instead of rendering a second standalone editor row below the list, removing the one-folder duplicate-card state seen during Windows walkthrough. `SyncPairRowViewModel.IsEditorVisible` tracks only the active row, and the XAML visual contract rejects the old `IsSelectedSyncPairEditorVisible` editor binding inside the `Folders` section. Focused ViewModel/visual-contract/tray tests passed 81/81, full `Cotton.Sync.Desktop.Tests` passed 293/293, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-06: decorative `Idle` folder status is now trimmed and hidden case-insensitively so whitespace/casing cannot leave an isolated idle label in the row, and the row overflow button is covered as the open/close toggle for inline controls instead of requiring a separate close button. Focused ViewModel/visual-contract tests passed 33/33, desktop Release build passed with 0 warnings, and `git diff --check` passed.
  Partial 2026-06-04: added a dedicated `folder-controls` visual-smoke scenario to the Linux screenshot matrix so the compact window captures the expanded sync-folder controls state instead of only the closed dashboard. Verification: focused startup/ViewModel/packaging tests passed 105/105, Xvfb captured `/tmp/cotton-sync-folder-controls-smoke/cotton-sync-desktop-linux-folder-controls.png` and it was reviewed for duplicate rows, clipping, and section overlap; full `Cotton.Sync.Desktop.Tests` passed 295/295, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-04: expanded sync-folder controls no longer duplicate the row-level `Open local folder` action; the folder row owns opening, while the expanded controls focus on rename, pause/enable, remove, and close. `DesktopSetupVisualContractTests` now requires exactly one local-folder open action in the `Folders` section. Verification: focused visual-contract tests passed 21/21, Xvfb captured and reviewed `/tmp/cotton-sync-folder-controls-smoke/cotton-sync-desktop-linux-folder-controls-deduped.png`, full `Cotton.Sync.Desktop.Tests` passed 295/295, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-04: refreshed the window-bound screenshot matrix and caught the expanded folder-controls state clipping the second folder row in the compact dashboard. The sync-pair list now gets enough bounded height for one expanded row plus the next folder while Activity remains visible. Verification: focused visual-contract tests passed 22/22, refreshed `/tmp/cotton-sync-folder-controls-current/cotton-sync-desktop-linux-folder-controls.png` was reviewed, full `Cotton.Sync.Desktop.Tests` passed 304/304, and full solution Release build passed with 0 warnings.
  Verification 2026-06-06: refreshed the current dashboard visual-smoke set from a freshly published Linux apphost at `/tmp/cotton-sync-dashboard-verify`: `empty-dashboard`, `dashboard`, `folder-controls`, and `progress`. Reviewed screenshots prove the dashboard has a non-duplicated global account/status header, sync-pair list, per-pair status/actions, expanded row controls without clipping, a dedicated progress card with current file, transferred/total bytes, speed, ETA, aggregate file count, per-folder progress, and recent Activity stretched into the remaining window space.
- [x] Fix multi-folder dashboard progress and stale completion states.
  Required behavior: when 2+ sync pairs are active, the top progress surface must represent aggregate app work, not only one currently transferred file. Per-folder rows should show each pair's own operation/progress and settle to a clear complete/idle state instead of endless indeterminate progress after work is done. `Initial sync complete` must not remain as a prominent status/notification card while another folder is scanning, syncing, or the app is paused.
  Observation 2026-06-06 from Windows manual testing: two configured folders can show an active `Scanning local files` progress card, a separate `Initial sync complete` card, row-level endless indeterminate progress without a visible 100% completion state, and later a global `Sync is paused.` state while the stale completion card remains below it. The top progress surface also currently reflects one active file/pair more than the aggregate app run when multiple folders are active.
  Partial 2026-06-06: dashboard notification cards now use a separate visibility gate from raw notification history. Completion notifications remain recorded and can still toast, but they no longer render as a prominent in-app dashboard card, preventing `Initial sync complete` from appearing as a second status under current progress or pause state. Verification: focused ViewModel/visual-contract tests passed 112/112, full `Cotton.Sync.Desktop.Tests` passed 359/359, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-06: desktop now aggregates top run progress when multiple sync folders report active run-progress samples. The top progress card uses `Syncing N folders`, summed file counts, and an aggregate progress value, while the current file transfer remains visible as secondary detail. Single-folder progress keeps the previous file/folder-specific presentation. Verification: focused ViewModel/visual-smoke tests passed 85/85, full `Cotton.Sync.Desktop.Tests` passed 348/348, `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings, and the reviewed Linux window-bound screenshot `/tmp/cotton-sync-aggregate-progress.png` shows `Syncing 2 folders`, `10 of 40 files across 2 folders`, and the current uploaded file as secondary detail.
  Partial 2026-06-06: completed run-progress samples now clear the affected folder row and remove that folder from aggregate top progress immediately, instead of leaving stale `Scanning local files`/`Checking files` text or an endless indeterminate bar until the next status packet arrives. Verification: focused `ShellViewModelSyncPairCommandTests` passed 81/81, full `Cotton.Sync.Desktop.Tests` passed 350/350, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-06: aggregate top progress now puts compact byte/speed/ETA details in the header's right side opposite the aggregate title, keeping transfer metrics visible while the current file stays on the next line. Verification: focused progress and visual-contract tests passed 13/13, full `Cotton.Sync.Desktop.Tests` passed 364/364, `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings, and `/tmp/cotton-sync-progress-header-metrics/cotton-sync-desktop-linux-progress.png` was reviewed with speed/ETA visible beside `Syncing 2 folders`.
  Partial 2026-06-06: the top progress card header now uses a `*,Auto` layout so the right-side byte/speed/ETA metrics keep their space while long folder/action titles trim instead of pushing those metrics out. Verification: focused visual-contract test passed 1/1 and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-06: aggregate top-progress ETA now uses the longest per-transfer estimated time remaining, preferring the app layer's smoothed transfer estimates instead of recomputing a raw global ETA from summed bytes and summed speed. This keeps the dashboard prediction steadier and more accurate while multiple folders transfer in parallel. Verification: focused aggregate progress tests passed 3/3, full `Cotton.Sync.Desktop.Tests` passed 388/388, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings.
  Observation 2026-06-06 from Windows manual testing: a large sync can sit for a long time in a quiet preparation phase before visible per-file transfer progress appears. The dashboard must show global synchronization progress throughout preparation, including large-folder counts such as `0 of 29185 files`, so the app never looks frozen or silent before the first upload/download starts.
  Partial 2026-06-06: local metadata scanning now reports file-discovery heartbeat progress through an optional scanner progress interface, `SyncEngine` maps those samples into existing `ScanningLocal` run progress, and the desktop progress card renders `N files found` plus the current file name while totals are still unknown. This reduces the silent preparation window during large local tree scans without introducing pre-upload full-file hashing. Verification: focused local-scan/core/desktop tests passed 3/3, full `Cotton.Sync.Tests` passed 157/157, full `Cotton.Sync.Desktop.Tests` passed 365/365, `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings, and full `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-06: local scanning now prunes ignored directories before enumerating their children, instead of relying only on per-child ignore checks after recursive enumeration. This prevents `.cotton-sync`, temp, partial-download, and other ignored folder trees from contributing unbounded traversal work during large local scans. Focused scanner tests passed 11/11, and full `Cotton.Sync.Tests` passed 163/163.
  Partial 2026-06-06: local content hashing now opens files with explicit async sequential-scan file options for the cases where baseline comparison truly requires a full hash. Local-only upload candidates still avoid pre-upload hashing and derive the baseline hash from the uploaded manifest; focused scanner tests passed 11/11.
  Partial 2026-06-06: multi-folder local scans now aggregate discovery counts in the top progress card, e.g. `579 files found across 2 folders`, instead of falling back to the generic `N folders are syncing` while totals are still unknown. This keeps long preparation phases visibly alive before the first transfer starts. Focused progress ViewModel tests passed 3/3, full `Cotton.Sync.Desktop.Tests` passed 393/393, and desktop Release build passed with 0 warnings.
  Verification 2026-06-06: current tests and recorded visual review prove the multi-folder dashboard now keeps aggregate top progress primary, renders current transfer metrics in the progress header, clears completed folder rows out of aggregate progress, hides completion notifications from dashboard cards, and reports local-scan discovery counts during long preparation phases. This satisfies the stale completion and 2+ folder progress requirements; remaining 30k/50k performance verification stays tracked in Phase 13.
- [x] Keep global dashboard status/progress stable while folder lists scroll.
  Required behavior: growing multi-folder content should not make the whole upper dashboard chrome scroll away. Status/action-required/current-progress stays visible, the folder list gets its own bounded scroll when needed, and Activity remains reachable without layout overlap in compact Windows and Linux windows.
  Observation 2026-06-06 from Windows manual testing: with 2+ folders the whole dashboard area scrolls, including the top progress/completion/status cards. The intended shape is stable top status/progress chrome, a bounded scroll for folders, then reachable Activity without overlapping compact-window content.
  Partial 2026-06-06: the signed-in dashboard layout now separates stable top chrome, bounded folder list, and Activity into separate grid rows instead of wrapping status/progress/folders in one scroll viewer. The sync-folder list owns its own scroll surface, while Activity remains its own lower panel in compact windows. Verification: focused visual-contract tests passed 27/27, full `Cotton.Sync.Desktop.Tests` passed 346/346, `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings, `git diff --check` passed, and reviewed Linux window-bound screenshots `/tmp/cotton-sync-stable-top-progress.png` and `/tmp/cotton-sync-stable-top-folder-controls.png` show stable progress/folder/activity sections without whole-dashboard top scrolling.
  Partial 2026-06-06: the dashboard row sizing now keeps top chrome `Auto` and gives folders/activity bounded star rows (`2*` and `*`) with minimum heights, so the folder list's internal `ScrollViewer` receives a real height instead of growing the row by content. Verification: focused dashboard layout visual-contract test passed 1/1 and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-06: the current dashboard layout uses separate `Auto`, bounded `Auto`, and `*` rows for top chrome, folders, and Activity, with the sync-pair list owning its own `ScrollViewer`. Visual-contract coverage guards against the previous whole-dashboard scroll behavior, so the stable chrome/folders/activity requirement is satisfied for the compact desktop shell.
- [x] Clarify pause versus disable semantics in folder controls.
  Required behavior: global pause should pause all enabled folders and make their row status read paused. A per-folder action that disables a folder must be labeled and iconed as disable/enable, not as pause/resume. If per-folder pause is added later, it must be separate from disable and should not mutate the pair's enabled setting.
  Observation 2026-06-06 from Windows manual testing: global pause makes folder rows read `Paused`, but the per-folder expanded action changes the row to `Disabled`. Keep this distinction explicit in labels/icons and do not make the per-folder disable control look like a pause control.
  Partial 2026-06-06: the expanded folder control no longer uses pause/play icons for the per-folder enable toggle. It now uses folder-off/folder-check Material icons while keeping the tooltip copy `Disable sync folder` / `Enable sync folder`, so the control matches the current behavior instead of implying per-folder pause.
  Partial 2026-06-06: folder rows now treat `Idle` as decorative and hide the compact row status while inline controls are expanded, preventing the status text from hanging beside rename/enable/remove actions as a duplicate visual state. Verification: focused row/visual-contract tests passed 28/28, full `Cotton.Sync.Desktop.Tests` passed 351/351, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-06: current ViewModel/controller tests prove global pause/resume calls `PauseAllAsync`/`ResumeAllAsync`, marks only enabled folders as `Paused`, leaves disabled folders `Disabled`, disables manual sync while paused, and treats disabled folders as out-of-scope for global paused status. Current visual-contract coverage rejects pause/play icons in the per-folder editor and requires folder-off/folder-check enable-state icons, so per-folder disable no longer reads as pause.
- [x] Theme built-in control accents to the Cotton brand.
  Required behavior: `ProgressBar` fill, selected `ListBoxItem` background, and selected `TabItem` indicator must use the Cotton lime/teal brand, not the FluentTheme default `SystemAccentColor`. The project currently overrides no accent resources, so all of these render in the default purple.
  Observation 2026-06-06 from Windows visual review: purple accent appears on the progress bars (`Progress`, `resize-large`), the selected conflict row (`Conflict`, `resize-wide` — a wide solid purple band over the red conflict card), the settings tab underline (`Settings`, `SettingsDiagnostics`), and the notifications checkbox (`light-theme`), in both themes. Details and evidence in `notes/desktop-visual-review-2026-06-06.md` (finding 1).
  Partial 2026-06-06: the desktop `FluentTheme` now defines light and dark `ColorPaletteResources` with Cotton lime `Accent="#96BE02"`, so built-in progress, selection, checkbox, and tab-selected visuals inherit the product accent instead of the default purple without per-control styling. Focused visual-contract tests passed after this change.
  Partial 2026-06-06: conflict rows no longer use a selectable `ListBox`, so the global Cotton accent does not paint a green selected band inside the red conflict surface. Conflict rows now render as action rows with their own `Open conflict location` command, while the surrounding conflict section remains the attention-colored surface. Verification: focused visual/ViewModel tests passed 2/2, full `Cotton.Sync.Desktop.Tests` passed 367/367, `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings, `git diff --check` passed, and `/tmp/cotton-sync-conflict-no-selected-accent/cotton-sync-desktop-linux-conflict.png` was reviewed without the selected accent band.
  Verification 2026-06-06: current `App.axaml` registers Cotton lime as the Fluent light/dark accent and removes the conflicting selected-row surface from conflicts; visual-contract tests cover the accent palette and conflict action-row shape. This satisfies the product-accent requirement without adding per-control custom styling.
- [x] Color-code folder and self-test status.
  Required behavior: a per-folder `Error` row status should read in the error brush, not plain secondary text identical to `Idle`/`Syncing`; self-test pass results should read in the success brush instead of muted grey so an all-pass run looks healthy.
  Observation 2026-06-06 from Windows visual review: `Error` scenario shows the Documents row status as grey "Error"; `SettingsDiagnostics` shows all `OK` results in secondary grey. Evidence in `notes/desktop-visual-review-2026-06-06.md` (finding 2).
  Partial 2026-06-06: sync-pair row statuses now expose an error-state class bound to `CottonErrorBrush`, and diagnostics self-test result labels expose passed/failed classes bound to the success/error brushes instead of using secondary grey. Focused row/view-model and visual-contract tests passed after this change.
  Verification 2026-06-06: current XAML binds folder row error status to `TextBlock.syncPairStatus.errorStatus`, diagnostics result labels to `selfTestResult.passed`/`failed`, and focused row/self-test/visual-contract tests cover those classes. The error and OK status-color requirement is satisfied.
- [x] Improve enlarged-window space usage on the dashboard.
  Required behavior: when the resizable dashboard window is stretched tall, Folders and Activity should not each be forced to half the height; size Folders to content with a bounded scroll and let Activity take the remainder, or cap window growth. Default/min size is already well tuned.
  Observation 2026-06-06 from Windows visual review: at `820x980` both Folders and Activity are mostly empty with 2 folders / 3 activity rows (`resize-large`); `resize-min` looks correct. Evidence in `notes/desktop-visual-review-2026-06-06.md` (finding 3).
  Partial 2026-06-06: the dashboard grid now sizes the Folders section to content with a bounded maximum row height and gives the remaining height to Activity (`Auto`, bounded `Auto`, `*`) instead of forcing Folders and Activity into equal star rows. Focused visual-contract tests passed after this change; keep unchecked until refreshed enlarged-window screenshots are reviewed.
  Verification 2026-06-06: `smoke-gui-screenshot.sh` now supports explicit visual-QA window sizing through `COTTON_SYNC_SCREENSHOT_WINDOW_SIZE=WIDTHxHEIGHT`, and the enlarged dashboard was captured under Xvfb/xfwm4 at `/tmp/cotton-sync-enlarged-dashboard/cotton-sync-desktop-linux-dashboard-820x980.png`. The reviewed screenshot shows Folders sized to two rows at the top and Activity occupying the remaining height without layout overlap or the previous half-empty Folders panel.
- [x] Polish expanded sync-folder editor commands.
  Required behavior: the row `...` button should be the only open/close toggle for expanded controls, so a separate close `X` should not appear unless it has a distinct purpose. Destructive removal must use a trash icon instead of a minus glyph, save must use a save/disk icon instead of a checkmark, and every compact icon command must have a clear tooltip.
  Verification 2026-06-06: expanded folder controls now remove the redundant close `X`; the row `...` remains the editor open/close toggle. The save command uses a disk icon, remove uses a trash icon, and visual-contract coverage rejects the old close command and minus glyph. Verification: `dotnet test src/Cotton.Sync.Desktop.Tests/Cotton.Sync.Desktop.Tests.csproj --configuration Release --no-restore` passed 341/341, `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings, `git diff --check` passed, and the Linux visual smoke screenshot `/tmp/cotton-sync-ui-polish/folder-controls.png` was reviewed.
- [x] Add sync-pair editor.
  Required fields: local folder picker, remote folder picker/resolver, display name, enabled flag, sync mode.
  Partial 2026-06-03: settings now exposes existing sync folders with selected-pair open, enable/disable, and remove actions. `DesktopShellController.SetSyncPairEnabledAsync` persists the enabled flag through the EF sync-pair store and `RemoveSyncPairAsync` deletes the pair; `ShellViewModel` updates selected-row state and selection after commands. Focused controller/view-model tests passed 11/11, full `Cotton.Sync.Desktop.Tests` passed 93/93, and full solution Release build passed with the known NU1903 Avalonia/Tmds.DBus.Protocol warning. Keep unchecked until the add/edit flow receives final visual QA and sync-mode presentation is polished.
  Partial 2026-06-03: add-folder wizard now has a feature-gated sync-mode placeholder; normal release UI stays on full mirror, while `COTTON_SYNC_DESKTOP_SHOW_FUTURE_MODES=1` exposes disabled `Virtual files` / `Not implemented` UI for development review only. Focused desktop feature-flag/ViewModel tests passed 16/16, full `Cotton.Sync.Desktop.Tests` passed 105/105, and full solution Release build passed with the known NU1903 warning. Keep unchecked until the add/edit flow receives final visual QA and sync-mode presentation is polished.
  Partial 2026-06-03: settings now exposes a compact selected-sync-folder display-name editor. `ShellViewModel.SaveSelectedSyncPairNameCommand` trims and validates the draft name, `DesktopShellController.RenameSyncPairAsync` persists it through the EF sync-pair store, and diagnostics/activity rows refresh after save. Focused controller/view-model tests passed 21/21, full `Cotton.Sync.Desktop.Tests` passed 112/112, and full solution Release build passed with the known NU1903 warning. Keep unchecked until the add/edit flow receives final visual QA and sync-mode presentation is polished.
  Partial 2026-06-04: removing a sync folder now also deletes its persisted sync baseline entries and remote change cursor from `sync-state.db`, so removed folders do not leave stale local state behind. Full `Cotton.Sync.Tests` passed 96/96, full `Cotton.Sync.Desktop.Tests` passed 141/141, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Partial 2026-06-04: dashboard `Folders` now exposes selected-pair management directly below the sync-pair list: edit display name, save, enable/disable, open selected local folder, and remove. The duplicated settings sync-folder management remains out of product direction. `DesktopSetupVisualContractTests` passed 13/13, focused `ShellViewModelSyncPairCommandTests` passed 34/34, full `Cotton.Sync.Desktop.Tests` passed 201/201, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings. Keep unchecked until screenshot QA confirms dashboard spacing with one and multiple configured folders.
  Partial 2026-06-06: expanded dashboard folder controls now include a local-folder picker action for existing sync pairs. `ShellViewModel.ChangeSelectedSyncPairLocalFolderCommand` validates overlap against other roots before saving, `DesktopShellController.SetSyncPairLocalFolderAsync` persists through the active app-layer host so sync state reset remains centralized, and the inline controls expose the local-folder edit as a compact icon action. Verification: focused local-folder edit/controller/visual-contract tests passed, full `Cotton.Sync.Desktop.Tests` passed 337/337, `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings, and the reviewed window-bound screenshot is `/tmp/cotton-sync-local-folder-edit/cotton-sync-desktop-linux-folder-controls.png`. Keep unchecked until remote cloud-root editing and final editor visual QA are complete.
  Partial 2026-06-06: expanded dashboard folder controls now also include a cloud-folder edit action for existing sync pairs. The existing cloud folder browser is reused in edit mode, `UseRemoteFolderCommand` keeps add-flow and edit-flow behavior separate, and `DesktopShellController.SetSyncPairRemoteFolderAsync` resolves the selected cloud path to a real remote root id before saving through the active app-layer host. Verification: focused remote-folder edit/controller/visual-contract tests passed, full `Cotton.Sync.Desktop.Tests` passed 341/341, `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings, and the reviewed window-bound screenshots are `/tmp/cotton-sync-remote-folder-edit/cotton-sync-desktop-linux-folder-controls.png` and `/tmp/cotton-sync-remote-folder-edit/cotton-sync-desktop-linux-add-folder.png`. Keep unchecked until final editor visual QA and sync-mode editing/presentation are complete.
  Partial 2026-06-06: the current inline folder editor now exposes the already-implemented local-folder and cloud-folder edit commands as compact Material icon buttons, avoiding the earlier regression where only rename/enable/remove were reachable from the expanded row. Verification: focused editor command/visual-contract tests passed 5/5, `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings, and `/tmp/cotton-sync-editor-actions/folder-controls.png` was reviewed without compact-window overlap.
  Partial 2026-06-04: existing sync-pair mode now flows from `SyncPairSettings` into `DesktopSyncPairSnapshot`, the dashboard row view model, and the selected-pair management panel, which displays `Full mirror` before the local path. Focused visual/ViewModel tests passed, full `Cotton.Sync.Desktop.Tests` passed 203/203, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings. Keep unchecked until sync-mode editing/presentation is screenshot-reviewed.
  Partial 2026-06-04: removing a sync folder from the dashboard now requires an inline confirmation before the pair and its local sync state are deleted, preventing accidental one-click removal. Focused `ShellViewModelSyncPairCommandTests` passed 35/35, `DesktopSetupVisualContractTests` passed 14/14, full `Cotton.Sync.Desktop.Tests` passed 205/205, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Partial 2026-06-04: adding a sync folder now rolls back the just-saved sync pair if the first `StartSyncAsync`/`SyncNowAsync` fails, so unsupported-server or initial-sync errors remain the visible cause instead of turning the next click into a misleading nested-local-root validation error. `DesktopShellControllerHostLifecycleTests` passed 9/9, full `Cotton.Sync.Desktop.Tests` passed 267/267, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-04: a missing desktop sync changes API action-required state now blocks add-folder wizard commands until the server check is resolved, preventing a second click from replacing the server capability error with local-root overlap validation. Focused add-folder error-gate tests passed 5/5, full `Cotton.Sync.Desktop.Tests` passed 289/289, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-04: the missing desktop sync changes API block now lives in explicit ViewModel state instead of deriving command availability from the current error text. Exporting diagnostics preserves the existing action-required server error and does not re-enable add-folder commands; a passing self-test or changed server clears the block. Status updates that normalize to the same missing-API message also raise the block. Focused `ShellViewModelSyncPairCommandTests` passed 62/62, full `Cotton.Sync.Desktop.Tests` passed 303/303, and full solution Release build passed with 0 warnings.
  Partial 2026-06-04: the self-test path now scans every failed self-test item for the missing desktop sync changes API condition instead of only checking the first visible failure message. This prevents a different first failure, such as token storage, from accidentally re-enabling add-folder actions against an unsupported backend. Focused ViewModel/resolver tests passed 82/82, full `Cotton.Sync.Desktop.Tests` passed 305/305, and full solution Release build passed with 0 warnings.
  Verification 2026-06-06: current dashboard folder editor exposes display-name editing, local-folder picker, cloud-folder picker/resolver, enable/disable state, remove confirmation, and full-mirror sync-mode presentation without duplicating the old settings sync-folder editor. `ShellViewModel` command tests and `DesktopShellController` host lifecycle tests prove rename, local path change, cloud path change, enabled flag persistence, and removal all go through the active app-layer host instead of manual UI-side mutation. Focused editor/controller/visual-contract tests passed 18/18, and the current desktop branch's full `Cotton.Sync.Desktop.Tests` passed 392/392 before this plan-only status update.
  Partial 2026-06-06: app-layer sync-pair updates now clear persisted baselines and remote change cursors when an existing pair's local root, remote root node, or mode changes, preventing a future root editor from reusing stale sync state against a different folder. Display-name-only edits keep state intact. Focused `SyncApplicationServiceTests.SaveSyncPairAsync*` passed 13/13.
- [x] Add remote folder picker or searchable remote folder selector.
  Verification 2026-06-03: add-folder wizard includes a remote cloud folder browser backed by `IDesktopShellController.ListRemoteFoldersAsync`, with root loading, folder selection, open-folder navigation, up navigation, empty state, and selected remote path label. `ShellViewModelSyncPairCommandTests` now cover loading root remote folders and navigating into a selected cloud folder. Focused ViewModel tests passed 7/7, full `Cotton.Sync.Desktop.Tests` passed 107/107, and full solution Release build passed with the known NU1903 warning.
  Verification 2026-06-04: the cloud folder browser now uses compact navigation buttons and supports double-clicking the folder list to open the selected cloud folder, making the add-folder wizard behave more like a desktop file browser. Full `Cotton.Sync.Desktop.Tests` passed 141/141 and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: the add-folder wizard now labels cloud selection explicitly as the current folder, uses polished arrow icon buttons for cloud back/open navigation instead of ASCII `^`/`>` placeholders, and names the primary action `Use this folder`. `DesktopSetupVisualContractTests` passed 12/12, full `Cotton.Sync.Desktop.Tests` passed 197/197, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: the cloud folder browser now includes an explicit create-folder action in the picker header. The create row trims the requested folder name, creates it through the desktop shell controller, navigates to the created folder, and keeps the primary `Use this folder` action visible at compact size. Focused ViewModel/visual-contract tests passed 60/60, full `Cotton.Sync.Desktop.Tests` passed 258/258, `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings, and `/tmp/cotton-sync-ui-current/cotton-sync-desktop-linux-add-folder.png` was reviewed.
- [x] Add settings screen.
  Required sections: account, startup, notifications, diagnostics, about. Sync-pair management lives in the dashboard `Folders` surface to avoid duplicate folder-management UI.
  Partial 2026-06-03: desktop shell now has a settings overlay with account, startup, diagnostics, about, and sign-out controls; the dashboard secondary-action menu opens it. Keep unchecked until sync-pair management, notifications settings, and full settings navigation are complete.
  Partial 2026-06-03: settings now includes persisted notification and appearance controls. Keep unchecked until sync-pair management and full settings navigation are complete.
  Revision 2026-06-04: removed the duplicated settings sync-folders section from the product direction; folder add/open/manage stays on the main dashboard, while Settings stays focused on account, startup, appearance, notifications, diagnostics, and sign-out.
  Partial 2026-06-04: diagnostics self-test results no longer use a nested scroll area inside Settings; the outer Settings scroll is the only scrolling surface. `DesktopSetupVisualContractTests` passed 4/4, full `Cotton.Sync.Desktop.Tests` passed 179/179, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings. Keep unchecked until the full settings surface is screenshot-reviewed and visually tightened.
  Partial 2026-06-04: Settings now uses tabbed Account, Startup, Preferences, and Diagnostics sections instead of one long scroll surface, keeping navigation visible in the compact desktop window. `DesktopSetupVisualContractTests` passed 11/11, full `Cotton.Sync.Desktop.Tests` passed 196/196, `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings, and Xvfb launch screenshot `/tmp/cotton-desktop-ui.png` confirmed the window still opens centered without clipping.
  Verification 2026-06-05: Settings tab headers now use release-readable labels `Account`, `Startup`, `Preferences`, and `Diagnostics` instead of the prototype abbreviations `Start`, `Prefs`, and `Diag`. Focused visual-contract tests passed 22/22, Linux GUI screenshot smoke captured `/tmp/cotton-sync-settings-tabs-current/cotton-sync-desktop-linux-settings.png` and `/tmp/cotton-sync-settings-tabs-current/cotton-sync-desktop-linux-settings-diagnostics.png`, both screenshots were reviewed for tab wrapping/clipping, full `Cotton.Sync.Desktop.Tests` passed 311/311, and full solution build passed with 0 warnings.
  Verification 2026-06-05: Settings subtitle now matches the available tabs with `Account, startup, preferences, diagnostics` instead of omitting Preferences. Focused visual-contract tests passed 22/22, full `Cotton.Sync.Desktop.Tests` passed 312/312, Linux GUI screenshot smoke captured and reviewed `/tmp/cotton-sync-settings-subtitle-current/cotton-sync-desktop-linux-settings.png`, and full solution build passed with 0 warnings.
  Partial 2026-06-04: the Account tab now includes a compact About section with product purpose and app version without adding a fifth tab that could crowd the compact settings header. `DesktopSetupVisualContractTests` passed 14/14, full `Cotton.Sync.Desktop.Tests` passed 202/202, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings. Keep unchecked until screenshots confirm the full settings surface at minimum window size.
  Partial 2026-06-04: the Diagnostics tab now scrolls as one tab surface when checks produce a long list, and diagnostics export records the bundle path in the UI with an `Open` action for the containing folder. Focused ViewModel/visual-contract tests passed 60/60, full `Cotton.Sync.Desktop.Tests` passed 258/258, `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings, and `/tmp/cotton-sync-ui-current/cotton-sync-desktop-linux-settings-diagnostics.png` was reviewed.
  Partial 2026-06-04: the deterministic `settings-diagnostics` visual-smoke state now runs both self-test and diagnostics export, so screenshot QA covers the exported bundle path row and `Open` action instead of only the checks table. Focused ViewModel/visual-contract tests passed 85/85, full `Cotton.Sync.Desktop.Tests` passed 305/305, `/tmp/cotton-sync-settings-diagnostics-export/cotton-sync-desktop-linux-settings-diagnostics.png` was captured and reviewed, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-04: the Startup tab hides the `Launch on startup` checkbox when the platform/autostart adapter does not support it instead of showing a disabled action; the unsupported case keeps the explanatory status text only. `DesktopSetupVisualContractTests` now requires the visibility binding and rejects the old disabled binding.
  Partial 2026-06-06: the Account tab now labels the device field as `Device name` and binds it to the runtime desktop device identity snapshot instead of hardcoding `Cotton Sync Desktop`, making it clear that this is the remembered client/device name sent with auth requests. Verification: focused ViewModel/visual-contract tests passed 113/113.
  Verification 2026-06-06: the implemented Settings surface now covers every required release section: Account contains identity, sign-out, About, and version; Startup contains launch-at-login and tray lifecycle status; Preferences contains appearance and notifications; Diagnostics contains self-test, diagnostics export, app-data opening, self-test results, and diagnostics rows. Sync-pair management intentionally remains on the dashboard `Folders` surface. Focused settings visual-contract tests and full desktop verification are recorded in the current commit.
- [x] Align Settings overlay width with the dashboard content.
  Required behavior: the Settings overlay should use the same effective content width as the dashboard cards such as Activity and Folders at the compact Windows window size. It must not look like a narrower nested card floating inside the app, and screenshot QA must cover Account and Diagnostics tabs at the normal compact size.
  Verification 2026-06-06: Settings overlay no longer caps itself at the old narrow 372px maximum and now stretches to the same effective compact-window content width as Activity/Folders. Visual-contract coverage rejects the old `MaxWidth="372"` setting overlay, while preserving the add-folder wizard width constraint. Verification: `dotnet test src/Cotton.Sync.Desktop.Tests/Cotton.Sync.Desktop.Tests.csproj --configuration Release --no-restore` passed 341/341, `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings, `git diff --check` passed, and the Linux visual smoke screenshot `/tmp/cotton-sync-ui-polish/settings.png` was reviewed.
- [x] Add conflict/error screen.
  Required behavior: show conflict files, action-required errors, retry/sync-now command.
  Partial 2026-06-03: dashboard now exposes an action-required banner when sync status, command failures, or self-test failures produce an error message. The message resolver is covered by `DesktopActionRequiredMessageResolverTests` 4/4, full `Cotton.Sync.Desktop.Tests` passed 29/29, and solution Release build passed with known NU1903 warnings. Keep unchecked until conflict-file listing and retry/action workflows are complete.
  Partial 2026-06-03: action-required banner now includes compact `Retry` and `Check` actions; `Retry` is visible only when signed in and invokes `SyncNowCommand`, while `Check` runs the desktop self-test. `ShellViewModelSyncPairCommandTests` covers self-test-created action-required state, retry visibility, sync retry invocation, and message clearing. Focused action-required tests passed 9/9, full `Cotton.Sync.Desktop.Tests` passed 94/94, and full solution Release build passed with the known NU1903 warning. Keep unchecked until conflict resolution actions and screenshot review are complete.
  Partial 2026-06-04: command failures now refresh the dashboard progress text so an action-required error no longer appears beside `All folders are up to date`; the progress line changes to `Fix the issue below to continue syncing.` until the error is cleared. The dashboard global status now uses `Action required` for signed-in command failures, setup auth failures keep `Sign-in failed`, and the generic empty-message fallback is `Operation could not be completed. Check diagnostics and retry.`. Focused `ShellViewModelSyncPairCommandTests` passed 41/41, focused `DesktopActionRequiredMessageResolverTests` passed 15/15, full `Cotton.Sync.Desktop.Tests` passed 253/253, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-04: command failure status resolution now distinguishes setup/load failures from credential failures: pre-login setup failures show `Action required`, while credential-step failures show `Sign-in failed`. Focused `ShellViewModelSyncPairCommandTests` passed 42/42, full `Cotton.Sync.Desktop.Tests` passed 254/254, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-04: add-folder action-required messages now render inside the wizard overlay so database/setup errors are visible near the workflow instead of being hidden behind the modal surface. The message is trimmed with a tooltip for the full text at compact size. Focused ViewModel/visual-contract tests passed 60/60, full `Cotton.Sync.Desktop.Tests` passed 258/258, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-04: add-folder wizard action-required messages now wrap up to three lines before trimming, matching the dashboard error treatment so long SQLite/server/deployment errors are readable inside the compact wizard. `DesktopSetupVisualContractTests` passed 21/21, full `Cotton.Sync.Desktop.Tests` passed 291/291, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-04: dashboard action-required detail text now wraps up to three lines and keeps the full message in a tooltip, so old-backend/sync API errors are readable in the compact window instead of being reduced to a one-line ellipsis beside the retry/check icons. Dashboard action-required states also no longer render a separate `Sync needs attention` status card above the concrete action-required row; the native/header status plus the detailed row are the single attention surface, reducing duplicated copy and keeping Activity visible. Focused ViewModel/visual-contract tests passed 3/3, full `Cotton.Sync.Desktop.Tests` passed 287/287, `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings, and `/tmp/cotton-sync-error-clean/cotton-sync-desktop-linux-error.png` was reviewed.
  Verification 2026-06-04: direct desktop command failures from Cotton API quota/status responses now use action-required copy instead of raw HTTP failure text. `DesktopActionRequiredMessageResolver` maps HTTP 507 to `Remote storage quota exceeded...` and HTTP 413 to `Remote upload was rejected...`; focused resolver tests passed 21/21, full `Cotton.Sync.Desktop.Tests` passed 307/307, and full solution Release build passed with 0 warnings.
  Verification 2026-06-05: sync-pair error activity rows now use the same normalized action-required text as the dashboard while preserving the raw `LastError` on the sync-pair row for diagnostics. This prevents recent activity from showing raw JSON parser messages such as `'< is an invalid start of a value` after old-backend or wrong-server responses. Focused ViewModel/resolver tests passed 85/85, full `Cotton.Sync.Desktop.Tests` passed 308/308, and full solution Release build passed with 0 warnings.
  Verification 2026-06-05: repeated unchanged sync-pair error status updates no longer spam duplicate `Error` rows in recent Activity; the dedupe resets after the pair reports a clear/no-error status so recurring problems are still visible as new events. Focused ViewModel status tests passed 65/65, full `Cotton.Sync.Desktop.Tests` passed 309/309, and full solution Release build passed with 0 warnings.
  Verification 2026-06-05: local sync-state SQLite table failures such as `SQLite Error 1: 'no such table: sync_change_cursors'` now normalize to `Local sync state database is unavailable. Run diagnostics and restart Cotton Sync.` for both sync status and direct command/exception paths. Focused resolver tests passed 23/23, full `Cotton.Sync.Desktop.Tests` passed 311/311, and full solution Release build passed with 0 warnings.
  Partial 2026-06-04: conflict-only dashboard states now mark the header and summary card as attention-required without showing the generic action-required banner, so the shell no longer displays `Connected / All folders are up to date` above a visible conflicts list. Focused ViewModel/visual-contract tests passed 59/59, full `Cotton.Sync.Desktop.Tests` passed 255/255, `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings, and `/tmp/cotton-sync-conflict-attention/cotton-sync-desktop-linux-conflict.png` was visually reviewed.
  Partial 2026-06-04: action-required messages now normalize raw JSON parser HTML-start errors such as `'<' is an invalid start of a value` into a user-readable server/deployment message, and sync-pair `Error` states without details still surface a generic diagnostic/retry message. Focused `DesktopActionRequiredMessageResolverTests` passed 11/11, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Partial 2026-06-04: action-required messages now also normalize real auth failure responses from the backend, including forbidden invalid-password responses and TOTP failure/lockout messages. Focused `DesktopActionRequiredMessageResolverTests` passed 14/14, full `Cotton.Sync.Desktop.Tests` passed 193/193, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Partial 2026-06-03: dashboard now shows a dedicated conflict list populated from live `Conflict` sync activities, with conflict count, affected file paths, details, and quick `Retry`/`Open` actions. `ShellViewModelSyncPairCommandTests` covers conflict activity insertion into both recent activity and the conflict list. Focused ViewModel tests passed 9/9, full `Cotton.Sync.Desktop.Tests` passed 109/109, and full solution Release build passed with the known NU1903 warning. Keep unchecked until conflict resolution actions and screenshot review are complete.
  Partial 2026-06-03: conflict activities now preserve the originating sync-pair id in `DesktopActivitySnapshot`, the dashboard conflict list has a real selected row, and `Open` resolves the selected conflict to a safe local folder inside the sync root instead of opening an unrelated selected sync pair. `ShellViewModelSyncPairCommandTests` covers conflict selection, opening the conflict parent folder, and rejecting traversal outside the sync root. Focused ViewModel tests passed 13/13, full `Cotton.Sync.Desktop.Tests` passed 114/114, and full solution Release build passed with the known NU1903 warning. Keep unchecked until conflict resolution actions and screenshot review are complete.
  Verification 2026-06-05: the conflict list no longer depends on an ambiguous global `Open selected conflict location` action; each conflict row owns its `Open conflict location` icon command and passes the row as the command parameter, so multiple conflicts can be opened directly without relying on list selection. Focused conflict ViewModel tests passed 4/4, `DashboardActionRows_UseIconButtonsForNarrowActions` passed, full `Cotton.Sync.Desktop.Tests` passed 316/316, `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings, and `/tmp/cotton-sync-conflict-row-open/cotton-sync-desktop-linux-conflict.png` was captured and reviewed.
  Verification 2026-06-06: current error and conflict visual-smoke screenshots were refreshed from a freshly published Linux apphost at `/tmp/cotton-sync-conflict-error-current/cotton-sync-desktop-linux-error.png` and `/tmp/cotton-sync-conflict-error-current/cotton-sync-desktop-linux-conflict.png`. The reviewed error state shows readable action-required copy with retry and self-test actions, while the reviewed conflict state shows a conflict count, affected file path, retry action, row-level conflict-location open action, and recent Activity without section overlap.
- [x] Add not-implemented UX for reserved future modes only in development builds or behind a feature flag.
  Verification 2026-06-03: `DesktopFeatureFlags` reads `COTTON_SYNC_DESKTOP_SHOW_FUTURE_MODES`; the add-folder wizard shows the disabled `Virtual files` / `Not implemented` placeholder only when that flag is enabled, while the default UI remains full-mirror only. `DesktopFeatureFlagsTests` and `ShellViewModelSyncPairCommandTests` cover truthy/falsy flag parsing and ViewModel visibility. Focused desktop tests passed 16/16, full `Cotton.Sync.Desktop.Tests` passed 105/105, and full solution Release build passed with the known NU1903 warning.
- [x] Add polished empty states.
  Verification 2026-06-03: dashboard folders empty state has a direct add action, dashboard activity has an empty state bound to `HasNoActivities`, remote folder browser explains empty cloud folders can still be selected, and settings sync folders empty state now includes a primary add action. `ShellViewModelSyncPairCommandTests` cover activity empty-state property transitions. Focused ViewModel tests passed 8/8, full `Cotton.Sync.Desktop.Tests` passed 108/108, and full solution Release build passed with the known NU1903 warning. Screenshot review remains tracked separately.
- [x] Add dark/light theme support.
  Verification 2026-06-03: added persisted `AppThemeMode` values `System`, `Light`, and `Dark` through EF-generated migration `20260603211332_AddAppThemePreference`; settings exposes an Appearance selector, `ShellViewModel` applies the theme through `IDesktopThemeService`, and `AvaloniaDesktopThemeService` maps preferences to Avalonia `ThemeVariant`. Focused preferences tests passed 5/5, focused desktop theme/controller tests passed 13/13, full `Cotton.Sync.App.Tests` passed 83/83, full `Cotton.Sync.Desktop.Tests` passed 72/72, and full solution Release build passed with the known NU1903 warning.
  Verification 2026-06-04: first-run installs now default to the dark Cotton theme instead of inheriting an unreliable system-light hint, while Settings can still switch to System or Light. `App.axaml` starts dark to avoid a light flash before preferences load, and `AppPreferences` defaults new databases to `AppThemeMode.Dark`. Local screenshot smoke captured `/tmp/cotton-sync-dark-default/first-run.png`; full `Cotton.Sync.App.Tests` passed 114/114, full `Cotton.Sync.Desktop.Tests` passed 251/251, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
- [x] Add responsive layout for minimum window size.
  Partial 2026-06-04: compact desktop XAML now avoids the fixed-width text button in the cloud folder browser, shortens the add-folder primary CTA to `Sync`, and adds tooltips to trimmed local/remote path labels so narrow flyout layouts do not hide critical paths. `Cotton.Sync.Desktop.Tests` passed 139/139 and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Partial 2026-06-04: setup, settings, and add-folder wizard labels were tightened (`Show notifications`, `Check`, `Up`, `Sync`) and tooltips were added where shorter labels could become ambiguous; the empty-state accent glyph now keeps contrast in dark mode. Full `Cotton.Sync.Desktop.Tests` passed 146/146 and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings. Keep unchecked until refreshed screenshots prove the minimum-size layout has no clipping or overlap.
  Partial 2026-06-04: sign-in window sizing was corrected after Xvfb screenshot QA showed setup controls clipped out of the compact window. Autostart is no longer shown on the sign-in screen and remains controlled from Settings/default preferences, so the credential form has room without an unrelated checkbox. Full `Cotton.Sync.Desktop.Tests` passed 150/150 and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings. Keep unchecked until dashboard, settings, conflict, and error states receive the same screenshot pass.
  Partial 2026-06-04: server setup height was reduced after removing the numbered stepper, with `/tmp/cotton-start-no-stepper.png` captured through Xvfb to verify the compact centered first-run window has no clipping or overlap. Full desktop and solution verification is recorded in the current commit.
  Partial 2026-06-04: the numbered setup stepper was removed from XAML and its unused theme styles were deleted, leaving the setup flow as a compact single-form screen per state. `DesktopSetupVisualContractTests` prevents the stepper classes from returning, full `Cotton.Sync.Desktop.Tests` passed 176/176, `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings, and `/tmp/cotton-sync-no-stepper.png` verified the compact centered server step visually.
  Partial 2026-06-04: narrow dashboard action rows were tightened again by replacing `Retry`/`Check`/`Open` text buttons with 32px icon buttons and keeping the longer labels in tooltips. `DesktopSetupVisualContractTests` passed 10/10, full `Cotton.Sync.Desktop.Tests` passed 190/190, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Partial 2026-06-06: dashboard sync folders no longer use a nested fixed-height scroll area; the outer dashboard scroll now owns overflow so expanded folder controls and per-folder progress rows are not clipped inside the folders panel. Focused visual-contract/ViewModel tests passed 93/93, local Xvfb visual smoke captured `/tmp/cotton-sync-visual-after-folders/cotton-sync-desktop-linux-folder-controls.png` and `/tmp/cotton-sync-visual-after-folders/cotton-sync-desktop-linux-progress.png` without clipping, full `Cotton.Sync.Desktop.Tests` passed 332/332, full solution Release build passed with 0 warnings, and `git diff --check` passed.
  Partial 2026-06-06: the signed-in dashboard no longer renders a redundant green status card when every enabled sync pair is already idle with a completed baseline; the header status remains, while first-sync, disabled, action-required, conflict, and active-progress states still keep their explicit dashboard message. Focused ViewModel/visual-contract tests passed 93/93, and the reviewed Xvfb screenshot `/tmp/cotton-sync-visual-healthy/cotton-sync-desktop-linux-dashboard.png` shows the compact dashboard flowing directly from the header into Folders and Activity without duplicated connected/up-to-date copy.
  Partial 2026-06-06: expanded folder controls were compacted by removing the duplicated `Full mirror · local path` line from the inline editor, and Activity details now stay to one visible row with tooltips for full text. Focused visual-contract tests passed 23/23, and the reviewed screenshot `/tmp/cotton-sync-activity-compact/cotton-sync-desktop-linux-folder-controls.png` shows the expanded folder controls plus two complete Activity rows without bottom clipping.
  Partial 2026-06-06: close icon buttons now use the standard `×` glyph instead of a lowercase `x` across folder controls, add-folder wizard, create-folder form, and Settings. Focused visual-contract tests passed 24/24.
  Partial 2026-06-06: more-actions icon buttons now use the standard `…` glyph instead of the text `...` in the shell header and sync-folder row controls. Focused visual-contract tests passed 2/2.
  Verification 2026-06-06: refreshed the full compact Linux window-bound visual-smoke matrix from a freshly published apphost at `/tmp/cotton-sync-responsive-verify`, including `first-run`, `sign-in-error`, `empty-dashboard`, `add-folder`, `dashboard`, `folder-controls`, `progress`, `settings`, `settings-diagnostics`, `error`, and `conflict`. The smoke script verified dimensions, nonblank frames, and runtime health, and visual review confirmed key controls, primary actions, progress details, diagnostics actions, action-required copy, and conflict actions fit the compact window without overlap or primary-action clipping. Clean-machine Windows/Linux screenshot review remains tracked separately.
- [ ] Ensure no user-visible strings are hardcoded if localization is required for desktop release.
- [x] Run Avalonia desktop build.
  Verification: commits `Refine desktop setup shell` and `Refine compact setup sign-in`; `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore`, `dotnet test src/Cotton.Sync.Desktop.Tests/Cotton.Sync.Desktop.Tests.csproj --configuration Release --no-restore`, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings. Headless setup screenshots were captured at `/tmp/cotton-sync-setup.png`, `/tmp/cotton-sync-setup-2.png`, and `/tmp/cotton-sync-setup-ui.png`; dashboard screenshots were captured at `/tmp/cotton-sync-dashboard-ui.png`, `/tmp/cotton-sync-dashboard-ui-fixed.png`, and `/tmp/cotton-sync-dashboard-settings-pass.png` for clipping/spacing inspection.
  Verification 2026-06-04: upgraded desktop Avalonia packages to 12.0.4 to resolve the DBus advisory without the runtime-incompatible `Tmds.DBus.Protocol` 0.94.1 override. `AvaloniaUseCompiledBindingsByDefault=false` preserves existing runtime bindings until the large shell XAML is typed deliberately, and `Watermark` usage was replaced with `PlaceholderText`. `dotnet test src/Cotton.Sync.Desktop.Tests/Cotton.Sync.Desktop.Tests.csproj --configuration Release` passed 136/136, `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings, and Linux Xvfb GUI startup produced setup screenshots without runtime exceptions.
- [ ] Run manual UI walkthrough on Linux.
- [ ] Run manual UI walkthrough on Windows.
- [ ] Capture screenshots for onboarding, dashboard, settings, conflict state, and error state.
  Partial 2026-06-04: captured headless setup screenshots at `/tmp/cotton-sync-setup-current.png` and `/tmp/cotton-sync-setup-bare-server.png`; the bare-host server screenshot verified centered setup layout, normalized `https://app.cottoncloud.dev/`, and visible green server verification. Keep unchecked until onboarding, dashboard, settings, conflict, and error states are all captured and reviewed.
  Partial 2026-06-04: captured current compact setup server-step screenshot at `/tmp/cotton-setup.png` and verified sign-in-step screenshot at `/tmp/cotton-signin.png` using Xvfb plus `ffmpeg` X11 capture. Keep unchecked until dashboard, settings, conflict, and error screenshots are refreshed after the same visual pass.
  Partial 2026-06-04: refreshed current sign-in screenshot at `/tmp/cotton-signin-current.png` after tightening labels and resizing the sign-in window; the screen uses `app.cottoncloud.dev`, shows normalized HTTPS server verification, and keeps autostart out of the login form. Keep unchecked until dashboard, settings, conflict, and error screenshots are refreshed.
  Partial 2026-06-04: captured `/tmp/cotton-sync-no-stepper.png` after removing the setup stepper; the first-run server screen is centered, compact, and no longer shows numbered `Server` / `Account` badges.
  Partial 2026-06-04: captured full-size Xvfb sign-in screenshot at `/tmp/cotton-desktop-smoke-full.png` after server probe through `--server app.cottoncloud.dev`; the sign-in window is centered on the full 1280x1024 virtual screen and credential controls fit without clipping.
  Partial 2026-06-04: added `Packaging/linux/smoke-gui-screenshot.sh` and CI wiring to capture `cotton-sync-desktop-linux-gui.png` from the published Linux apphost under Xvfb. Local verification captured `/tmp/cotton-sync-desktop-linux-gui.png` at 1024x768 with the first-run server screen centered and no cursor occlusion after a clean checksum-enabled publish. Keep unchecked until dashboard, settings, conflict, and error screenshots are captured and reviewed.
  Partial 2026-06-04: Linux GUI screenshot smoke now verifies the captured PNG dimensions with `ffprobe` and rejects single-color frames with `ffmpeg` signal statistics, so CI fails on broken, blank, or wrong-size screenshots instead of only checking that a file exists. Keep unchecked until dashboard, settings, conflict, and error screenshots are captured and reviewed.
  Partial 2026-06-04: Linux GUI screenshot smoke now also verifies the desktop app is still alive after capture and fails on app-log runtime exception signatures such as `TypeLoadException` or missing assemblies. Focused packaging metadata test passed 1/1, local Xvfb sign-in-error screenshot smoke passed with the new guard, full `Cotton.Sync.Desktop.Tests` passed 240/240, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-04: added `Packaging/linux/smoke-gui-screenshot-matrix.sh` so local and CI screenshot capture share the same first-run plus visual-smoke matrix. Local Xvfb verification created all 8 PNGs under `/tmp/cotton-visual-matrix`, including `sign-in-error`, `add-folder`, `dashboard`, `settings`, `settings-diagnostics`, `error`, and `conflict`; full `Cotton.Sync.Desktop.Tests` passed 241/241, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-04: added deterministic `--visual-smoke` states for dashboard, settings, settings-diagnostics, error, and conflict screenshots through a fake desktop shell controller. CI now captures `cotton-sync-desktop-linux-dashboard/settings/settings-diagnostics/error/conflict.png` from the published Linux apphost under Xvfb, and the local Xvfb smoke produced `/tmp/cotton-visual-dashboard.png`, `/tmp/cotton-visual-settings.png`, `/tmp/cotton-visual-settings-diagnostics.png`, `/tmp/cotton-visual-error.png`, and `/tmp/cotton-visual-conflict.png` at 1024x768 with PNG dimension and nonblank-frame checks. Keep unchecked until onboarding plus clean-machine screenshot evidence is reviewed together.
  Partial 2026-06-04: added deterministic `add-folder` visual-smoke state for the signed-in no-sync-pairs onboarding path. CI now includes `add-folder` in the Xvfb visual-smoke matrix, and local verification captured `/tmp/cotton-visual-add-folder.png` at 1024x768 with PNG dimension and nonblank-frame checks. The reviewed screenshot shows the cloud-folder wizard, local folder summary, remote folder list, and `Use this folder` action without clipping. Keep unchecked until clean-machine screenshot evidence is reviewed together.
  Partial 2026-06-04: added deterministic `sign-in-error` visual-smoke state for the compact setup credential form with a visible authentication failure. CI now includes `sign-in-error` in the Xvfb visual-smoke matrix, and local verification captured `/tmp/cotton-visual-sign-in-error.png` at 1024x768 with PNG dimension and nonblank-frame checks.
  Verification 2026-06-04: focused visual-smoke/startup/view-model/packaging contract tests passed, full `Cotton.Sync.Desktop.Tests` passed 234/234, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: focused sign-in-error visual-smoke/startup/view-model/packaging tests passed 82/82, full `Cotton.Sync.Desktop.Tests` passed 240/240, `/tmp/cotton-visual-sign-in-error.png` was captured and reviewed, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-04: Linux GUI screenshot smoke now auto-detects the X11 display size through `ffprobe` when `COTTON_SYNC_SCREENSHOT_SIZE` is not set, while keeping the explicit size override for CI. This fixed local screenshot capture on the current 540x1040 display without weakening PNG dimension and nonblank-frame checks. Local verification captured first-run, sign-in-error, dashboard, and settings screenshots under `/tmp/cotton-sync-visual-autosize`; focused packaging metadata tests passed 27/27, full `Cotton.Sync.Desktop.Tests` passed 250/250, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-04: added a deterministic `empty-dashboard` visual-smoke state for the signed-in no-sync-pairs dashboard without opening the add-folder wizard. Local Xvfb verification captured `/tmp/cotton-sync-header-dedupe/cotton-sync-desktop-linux-empty-dashboard.png`, `/tmp/cotton-sync-header-dedupe/cotton-sync-desktop-linux-add-folder.png`, and `/tmp/cotton-sync-header-dedupe/cotton-sync-desktop-linux-dashboard.png`.
  Partial 2026-06-04: Linux GUI screenshot smoke now captures the actual Cotton Sync X11 window by `_NET_WM_PID` through `xwininfo`/`xprop` plus `xwd`, instead of capturing the full display. Local verification first reproduced a false-positive full-display capture of the IDE on a busy desktop, then captured the app window correctly at 400x540. CI-mode Xvfb/dbus verification captured the full 10-scenario window matrix under `/tmp/cotton-sync-gui-window-matrix`, including first-run, sign-in-error, empty dashboard, add-folder, dashboard, progress, settings, settings-diagnostics, error, and conflict. Verification: focused packaging metadata tests passed 28/28, workflow YAML parsed, full `Cotton.Sync.Desktop.Tests` passed 277/277, `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings, and `git diff --check` passed.
  Verification 2026-06-05: refreshed the current window-bound Linux screenshot matrix after the conflict-row action polish. Local Xvfb capture produced `/tmp/cotton-sync-current-full-matrix` with first-run, sign-in-error, empty-dashboard, add-folder, dashboard, folder-controls, progress, settings, settings-diagnostics, error, and conflict states from the freshly published Linux apphost. The smoke script verified PNG dimensions, nonblank frames, and runtime health for every state. Keep unchecked until the same release matrix is reviewed on clean Windows and Linux machines.
- [ ] Review screenshots for clipping, overlap, weak hierarchy, and inconsistent spacing.
  Partial 2026-06-04: reviewed `/tmp/cotton-setup.png` and `/tmp/cotton-signin.png`; server-step and sign-in-step windows are centered, text fits, and the sign-in step expands to contain the credential fields. Keep unchecked until the full screenshot matrix is reviewed.
  Partial 2026-06-04: reviewed `/tmp/cotton-signin-current.png`; the sign-in window remains centered and compact, credential fields fit, autostart stays out of the login form, and no binding warnings were written to the Xvfb smoke log. Keep unchecked until the dashboard/settings/conflict/error screenshot matrix is reviewed.
  Partial 2026-06-04: reviewed `/tmp/cotton-desktop-smoke-full.png`; the previous cropped 900x700 capture was misleading because Xvfb uses a 1280x1024 screen, and the full capture confirms centered sign-in positioning with no clipping. Keep unchecked until dashboard/settings/conflict/error screenshots are refreshed.
  Partial 2026-06-04: reviewed the visual-smoke dashboard/settings/settings-diagnostics/error/conflict screenshots. The first settings pass caught tab-header wrapping, so Settings now uses compact tab headers; the first conflict/error pass caught dense dashboard compression, so dashboard content now scrolls as one vertical flow and folder management is height-bounded instead of overlapping Activity. The diagnostics tab now shows `Run checks` and `Export bundle` actions before the diagnostics table, and the `settings-diagnostics` smoke state runs self-test so the result rows are visible without scrolling. Final reviewed `/tmp/cotton-visual-settings.png`, `/tmp/cotton-visual-settings-diagnostics.png`, `/tmp/cotton-visual-error.png`, and `/tmp/cotton-visual-conflict.png` no longer show header wrapping, key error-text clipping, hidden diagnostics actions/results, or section overlap. The reviewed `/tmp/cotton-visual-add-folder.png` shows the onboarding add-folder wizard without clipped text or hidden primary action. Keep unchecked until the full release screenshot matrix is captured on clean Windows and Linux machines.
  Partial 2026-06-04: reviewed `/tmp/cotton-visual-sign-in-error.png`; the sign-in error appears inside the compact setup window without pushing the credential form or primary `Sign in` action out of view.
  Partial 2026-06-04: refreshed local screenshot smoke after the latest action-required/status polish at `/tmp/cotton-sync-visual-current`. Reviewed first-run, sign-in-error, dashboard, error, and settings captures; the windows remain centered and compact, sign-in-error keeps the primary action visible, error state no longer mixes action-required with an up-to-date message, and Settings fits without tab/header wrapping. Keep unchecked until the same matrix is reviewed on clean Windows and Linux machines.
  Partial 2026-06-04: reviewed `/tmp/cotton-sync-conflict-attention/cotton-sync-desktop-linux-conflict.png`; conflict state now uses an attention-colored header and summary card with `Conflicts need review` and `Review conflicts below to continue syncing.`, while the separate conflict list remains visible and actionable.
  Partial 2026-06-04: reviewed the window-bound `/tmp/cotton-sync-gui-window-matrix` screenshots. The dashboard, empty dashboard, add-folder, progress, settings, settings-diagnostics, error, and conflict windows render as compact 400x540 app-window captures; first-run and sign-in-error use the smaller setup/sign-in window profile. Reviewed states show no section overlap, hidden primary action, repeated product title in the signed-in header, stale up-to-date message beside action-required errors, or obvious text clipping in the compact window. Keep unchecked until Windows clean-machine screenshots and a human OS walkthrough are reviewed.
  Partial 2026-06-04: refreshed the current Linux window-bound matrix under `/tmp/cotton-sync-current-matrix` after the latest action-required and folder-management changes. The pass caught a folder-controls clipping regression, which was fixed and rechecked with `/tmp/cotton-sync-folder-controls-current/cotton-sync-desktop-linux-folder-controls.png`; the expanded folder row and next folder row are now fully visible in the compact window. Keep unchecked until Windows clean-machine screenshots and a human OS walkthrough are reviewed.
  Partial 2026-06-04: reviewed `/tmp/cotton-sync-header-dedupe/cotton-sync-desktop-linux-empty-dashboard.png` and `/tmp/cotton-sync-header-dedupe/cotton-sync-desktop-linux-dashboard.png`; the signed-in header uses account/status instead of repeating `Cotton Sync`, the empty dashboard no longer shows a redundant connected status card, and the folder dashboard uses the status card only for the concrete sync state.
  Partial 2026-06-04: the visual-smoke error state now uses the same release copy as the real missing desktop sync changes API path instead of the terse placeholder `Sync API unavailable.`, keeping screenshots aligned with the actual old-backend/server-deployment error users see. Focused ViewModel/resolver/visual-contract tests passed 91/91, full `Cotton.Sync.Desktop.Tests` passed 286/286, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-04: reviewed `/tmp/cotton-sync-settings-diagnostics-export/cotton-sync-desktop-linux-settings-diagnostics.png`; diagnostics export now leaves a visible bundle path row plus an `Open` action in the compact Settings window, while the self-test result rows and diagnostics table remain reachable in the single outer scroll surface. Keep unchecked until Windows clean-machine screenshots and a human OS walkthrough are reviewed.
  Partial 2026-06-04: reviewed `/tmp/cotton-sync-progress-current/cotton-sync-desktop-linux-progress.png`; progress is no longer only an activity list, with byte progress, speed, ETA, current file, aggregate file count, and per-folder progress visible in the compact dashboard window. Keep unchecked until Windows clean-machine screenshots and a human OS walkthrough are reviewed.
  Verification 2026-06-04: the underlying `VisualSmokeShellController` error snapshot now uses `DesktopActionRequiredMessageResolver.MissingDesktopSyncChangesApiMessage`, so both direct visual-smoke snapshots and ViewModel-applied smoke states share the same old-backend/server-deployment copy. Focused smoke/ViewModel tests passed 14/14, full `Cotton.Sync.Desktop.Tests` passed 286/286, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-05: reviewed the refreshed `/tmp/cotton-sync-current-full-matrix` screenshots. Add-folder keeps the `Use this folder` action visible, dashboard avoids duplicated product/status text, folder-controls expand inline without a duplicate editor row, progress shows transfer bytes/speed/ETA plus per-folder progress, settings diagnostics shows the exported bundle path and `Open` action, error state keeps the missing sync API message readable without claiming folders are up to date, and conflict rows expose row-level `Open conflict location` actions without a global selected-conflict command. Keep unchecked until Windows clean-machine screenshots and a human OS walkthrough are reviewed.
  Partial 2026-06-06: rechecked the compact Linux `folder-controls` and `progress` screenshots under `/tmp/cotton-sync-folder-layout-after` after bounding the Folders and Activity panels. Expanded folder controls now stay inside the Folders viewport, extra folder rows scroll there instead of overlapping Activity, and the progress state still shows aggregate multi-folder transfer metrics without clipping. Keep unchecked until Windows clean-machine screenshots and a human OS walkthrough are reviewed.
- [x] Run visual QA checklist from `notes/visual-qa-checklist.md` or replace it with an updated desktop checklist.
  Verification 2026-06-04: replaced the stale web/MUI visual QA checklist with a Cotton Sync Desktop Avalonia checklist covering setup, add-folder wizard, dashboard, settings, action-required/conflict states, tray/taskbar, platform storage, and screenshot evidence. The actual screenshot capture/review and OS walkthrough items remain open above.

## Phase 9 - Tray, Autostart, Notifications, And Lifecycle

- [x] Add single-instance enforcement.
  Verification 2026-06-03: commit `Add desktop single-instance guard`; desktop startup acquires a per-user app-data lock file before starting Avalonia, and a second process exits when the lock is already held. Focused `DesktopSingleInstanceGuardTests` passed 3/3, full `Cotton.Sync.Desktop.Tests` passed 18/18, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
  Verification 2026-06-04: second launch now sends a single-instance activation request to the running process through a per-data-dir named pipe before exiting, and the first process posts `MainWindow.ShowShell()` on the Avalonia UI thread. Focused single-instance tests passed 8/8, full `Cotton.Sync.Desktop.Tests` passed 246/246, `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings, and full solution Release build passed with 0 warnings.
- [x] Add close-to-tray behavior.
  Verification 2026-06-03: Windows-only tray lifecycle uses `ShutdownMode.OnExplicitShutdown`; `MainWindow` cancels close and hides to tray when tray lifecycle is available, while Linux keeps normal close semantics because tray lifecycle is unsupported. `DesktopWindowLifecyclePolicyTests` passed 4/4, full `Cotton.Sync.Desktop.Tests` passed 22/22, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings. Manual Windows tray verification remains open below.
- [x] Add explicit quit behavior.
  Verification 2026-06-03: tray Quit calls `MainWindow.RequestQuit()` before application shutdown, and the window lifecycle policy then resolves close as a real close instead of hide-to-tray. `DesktopWindowLifecyclePolicyTests` passed 4/4, full `Cotton.Sync.Desktop.Tests` passed 22/22, and solution Release build passed with known NU1903 warnings.
- [x] Add tray icon states.
  Required states: idle, syncing, paused, offline, error.
  Partial 2026-06-03: added a tested tray status model/resolver for signed-out, idle, syncing, paused, offline, and error states, and wired it to the tray tooltip.
  Verification 2026-06-03: added dedicated tray status icon assets for signed-out, idle, syncing, paused, offline, and error states, plus `DesktopTrayIconAssetResolver`; `DesktopTrayController` now updates the tray icon when status changes. Focused tray tests passed 7/7, full `Cotton.Sync.Desktop.Tests` passed 89/89, and full solution Release build passed with the known NU1903 warning. Manual Windows tray verification remains open below.
  Verification 2026-06-04: tray status now follows the shell attention summary instead of raw action-required errors only, so conflict-only states use the attention/error tray icon and tooltip `Cotton Sync - Conflicts need review` instead of falling back to `Connected`. Focused tray resolver tests passed 8/8, full `Cotton.Sync.Desktop.Tests` passed 256/256, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-05: `DesktopTrayStatusKind` now reserves `Unknown = 0`, and icon resolution rejects unknown tray states instead of mapping default enum values to a valid signed-out icon. Focused tray tests passed 11/11, and full solution Release build passed with 0 warnings.
  Verification 2026-06-06: idle tray state now uses the plain Cotton package icon (`Assets/icon-192.png`), while syncing, paused, offline, error, and signed-out continue to use dedicated state assets. Removed the stale `tray-idle.png` overlay asset. Focused tray resolver tests passed after this change.
- [x] Make active sync visible from tray/taskbar without opening the window.
  Required behavior: during upload/download/scanning, the Windows tray/taskbar surface should visibly indicate that sync is active through a distinct tray icon state, overlay/badge, subtle animation or alternating sync-status icon when supported, tooltip progress, taskbar progress, or another platform-appropriate signal. When everything is connected and idle, the tray must show the normal Cotton icon rather than a sync-specific state. The user should not need to open the dashboard to know that synchronization is currently running.
  Partial 2026-06-06: the tray status resolver now uses the shell's real active-progress flag to switch to the syncing icon, and keeps the normal Cotton idle icon for connected/idle or sync-related text without active work. `DesktopTrayController` listens for `HasCurrentWorkProgress` changes so upload/download/scanning state can update the tray without reopening the app window. Verification: `dotnet test src/Cotton.Sync.Desktop.Tests/Cotton.Sync.Desktop.Tests.csproj --configuration Release --no-restore` passed 343/343, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-06: while active sync work is visible, the tray tooltip now includes the current progress title and compact progress details, preferring byte/speed/ETA header metrics for transfers and falling back to run-progress details such as local scan file-discovery counts. `DesktopTrayController` updates the tooltip when the current work title/details/header metrics change, so the tray can answer what is syncing without opening the dashboard. Verification: focused tray tests passed 15/15, full `Cotton.Sync.Desktop.Tests` passed 367/367, `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings, and `git diff --check` passed.
  Verification 2026-06-06: current tray status tests prove active scan/upload/download progress selects `tray-syncing.png`, keeps the plain Cotton icon for connected/idle and non-active sync text, and exposes active progress title plus byte/speed/ETA or scan details in the tooltip. Focused tray tests passed 7/7, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings. Manual OS tray rendering remains tracked separately under Windows/Linux verification.
- [x] Add tray menu.
  Required commands: show app, open folder, open web, sync now, pause/resume, settings, quit.
  Partial 2026-06-03: commit `Expand desktop tray menu commands`; Windows tray menu now wires show app, open selected local folder, open Cotton Cloud in browser, sync now, pause, resume, settings overlay, and quit.
  Verification 2026-06-03: `DesktopTrayController` keeps the required menu command surface wired through `ShellViewModel` commands while the tray status/icon slice passed focused tray tests, full desktop tests, and solution Release build. Manual Windows tray behavior remains open below.
  Partial 2026-06-04: unavailable tray actions are now hidden instead of kept as disabled menu clutter. The local-folder action remains visible only when the dashboard has exactly one sync folder, while open-web, sync-now, pause/resume, and settings are shown only when their commands can execute. Verification: `DesktopTrayMenuContractTests` passed 2/2, full `Cotton.Sync.Desktop.Tests` passed 277/277, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-06: tray and dashboard secondary menus now label the browser/cloud action as `Open in Cotton Cloud`, keeping it distinct from `Open local folder` when a user's sync folder is itself named `Cloud`. Focused tray/menu visual-contract tests passed after this change.
- [x] Add Windows autostart adapter.
  Acceptable options: installer-managed startup, registry Run entry, startup shortcut, or scheduled task.
  Verification 2026-06-03: `WindowsRunAutostartService` uses the current-user `Software\Microsoft\Windows\CurrentVersion\Run` entry `Cotton Sync` with the packaged launch command, and registry access is behind `IWindowsRunRegistry` so the enable/disable/matching-command behavior is covered without touching a real registry. `WindowsRunAutostartServiceTests` passed 3/3, full `Cotton.Sync.Desktop.Tests` passed 25/25, and solution Release build passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings. Manual reboot verification remains open below.
  Verification 2026-06-04: Windows autostart now refuses development launch contexts such as `dotnet` hosts and non-published `bin/Debug` or `bin/Release` apphost output, so running from Visual Studio/debug builds cannot register a throwaway build path in `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`. Published apphosts and installed apphosts remain valid launch targets. Focused packaging/autostart tests passed 35/35, full `Cotton.Sync.Desktop.Tests` passed 262/262, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-04: Windows Run autostart now formats the registry value with Windows command-line quoting instead of XDG desktop-entry escaping, so installed paths like `C:\Users\qa\AppData\Local\Programs\Cotton Sync\Cotton.Sync.Desktop.exe` are quoted without doubled path backslashes. XDG autostart keeps desktop-entry escaping separately. Focused Windows/XDG autostart tests passed 12/12, full `Cotton.Sync.Desktop.Tests` passed 285/285, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
- [x] Add Linux autostart adapter.
  Target: XDG autostart `.desktop`.
  Verification 2026-06-03: commit `Clarify desktop lifecycle platform support`; Linux uses an XDG autostart `.desktop` adapter, and the factory no longer adds `--start-minimized` on Linux while tray lifecycle is unsupported. `Cotton.Sync.Desktop.Tests` passed 15/15 including the Linux factory regression test, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with known NU1903 Avalonia/Tmds.DBus.Protocol warnings.
  Verification 2026-06-03: the frontend `icon-192.png` is now copied as a loose `Assets/icon-192.png` desktop asset for XDG `.desktop` `Icon=` paths; `ValidateDesktopLooseAssets` fails the build if it is missing. `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` produced `Assets/icon-192.png`, and `dotnet publish ... -p:GeneratePublishChecksums=false` produced the same loose icon file.
  Verification 2026-06-04: first-run `LoadAsync` no longer applies the default autostart preference before a restored or newly authenticated session exists, avoiding external XDG/Run-entry side effects on the sign-in screen. Focused lifecycle tests covered pre-session no-op and post-auth apply behavior, a temp XDG smoke kept autostart empty before login, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: Linux XDG autostart now also refuses development launch contexts through `AutostartLaunchCommand.TryCreateDefault`, matching Windows behavior and preventing a debug or non-published build path from being written to `~/.config/autostart/cotton-sync.desktop` after sign-in. Focused Windows/XDG autostart tests passed 12/12, full `Cotton.Sync.Desktop.Tests` passed 285/285, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
- [x] Add notification adapter.
  Required notifications: initial sync complete, conflict created, action-required error.
  Verification 2026-06-04: `DesktopNotificationTracker` emits initial-sync-complete, conflict, and action-required notification requests; in-app notification history is wired through `ShellViewModel`; settings persists `AppPreferences.EnableNotifications` and suppresses native notifications when disabled. Native platform adapters are implemented through `IDesktopNotificationService`: Linux uses `notify-send` with unsupported fallback, and Windows uses `WindowsToastNotificationService` through an encoded PowerShell toast command. `DesktopNotificationServiceFactoryTests`, `NotifySendNotificationServiceTests`, `WindowsToastNotificationServiceTests`, and full `dotnet test src/Cotton.Sync.Desktop.Tests/Cotton.Sync.Desktop.Tests.csproj --configuration Release --logger "console;verbosity=minimal"` passed 179/179. Manual OS notification behavior remains open in the Windows/Linux verification items below.
  Verification 2026-06-04: initial-sync-complete notifications now require the sync-pair status snapshot to include `LastSyncedAtUtc`, matching the dashboard's successful-baseline source of truth instead of relying on a raw `Syncing` to `Idle` transition. Focused notification tracker tests passed 6/6, full `Cotton.Sync.Desktop.Tests` passed 257/257, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-04: action-required notifications now use the same normalized user-readable sync-pair error text as the dashboard, so old-backend HTML/JSON parser failures do not leak raw exception text into toast or in-app notifications. Error states without details emit the generic action-required message instead of staying silent. Focused notification/resolver tests passed 23/23, full `Cotton.Sync.Desktop.Tests` passed 293/293, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-06: action-required resolver and notification tracker now normalize sync-pair status strings that already contain embedded HTTP problem-details JSON, including `/api/v1/files/from-chunks` 400 upload failures, so banners, activity-derived action text, and Windows toast messages do not expose raw `Response: {...}` payloads or trace ids. Focused resolver/notification tests passed 38/38, full `Cotton.Sync.Desktop.Tests` passed 369/369, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings.
- [ ] Add desktop notifications for core account/session events.
  Required notifications: signed in/session restored, signed out, session revoked, and other core app notifications that the web app would surface for account/session state. These should respect notification preferences and avoid noisy duplicates during startup restore.
  Partial 2026-06-06: explicit sign-in and sign-out now emit native desktop notifications through the shared notification preference/support gate, without adding extra dashboard notification cards on top of Activity. Session restore remains activity-only until startup-noise behavior is manually reviewed. Verification: focused sign-in/sign-out/notification tests passed 31/31, full `Cotton.Sync.Desktop.Tests` passed 354/354, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-06: server-driven session revocation now has a dedicated app-level event publisher, is forwarded through the desktop shell, and moves the UI to signed-out state with a `Session expired` native notification while clearing transient secrets/progress surfaces. Session restore remains activity-only to avoid noisy startup toasts until a manual installed-app review decides otherwise. Verification: focused app/desktop session-revocation tests passed 20/20, full `Cotton.Sync.App.Tests` passed 150/150, full `Cotton.Sync.Desktop.Tests` passed 371/371, desktop Release build passed with 0 warnings, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-06: visible launches with a restored signed-in session now emit a `Session restored` native notification through the same preference/support gate. Tray/autostart starts and visual-smoke launches suppress this startup toast to avoid noisy duplicate notifications. Focused notification tests passed 5/5, full `Cotton.Sync.Desktop.Tests` passed 392/392, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings. Keep unchecked until installed-app Windows and Linux notification behavior is manually reviewed.
- [ ] Fix Windows notification identity and icon.
  Required behavior: Windows toast notifications must display the product name `Cotton Sync`, not implementation names such as `Cotton.Sync.Desktop`, and should use the Cotton app icon when the Windows notification API/path supports passing an app logo or AppUserModelID-backed identity. Verification must include an actual Windows notification screenshot or manual Windows run, because Linux/headless tests cannot prove the OS-facing display name/icon.
  Current implementation note: `WindowsToastNotificationService` uses PowerShell with `Windows.UI.Notifications.ToastNotificationManager.CreateToastNotifier(DesktopAppIdentity.AppUserModelId)`. The fix should investigate the installed Start Menu shortcut/AppUserModelID identity and toast XML image support such as app-logo override before changing constants, because debug launches without shell registration may display different sender metadata than installed apps.
  Partial 2026-06-06: desktop assembly metadata now uses the user-facing `Cotton Sync` title and product name instead of `Cotton Sync Desktop` / `Cotton`, keeping packaged Windows metadata aligned with the installer/window/notification branding. Verification: focused packaging and Windows toast tests passed 31/31, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings. Keep unchecked until installed Windows notification screenshots prove the OS sender name and icon.
  Partial 2026-06-06: Windows toast XML now builds a `ToastGeneric` binding directly before adding `appLogoOverride`, instead of attaching the icon node to the legacy `ToastText02` template. This keeps title/body text node creation safe while making the packaged app icon path compatible with the modern toast logo slot. Verification: focused notification tests passed 34/34, full `Cotton.Sync.Desktop.Tests` passed 387/387, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings. Keep unchecked until installed Windows notification screenshots prove the OS sender name and icon.
- [x] Add tests for single-instance lock where practical.
  Verification 2026-06-03: `DesktopSingleInstanceGuardTests` cover first acquire, blocked second acquire, and acquire after dispose; focused tests passed 3/3.
- [ ] Add manual Windows verification: autostart after reboot, tray behavior, notifications.
- [ ] Add manual Linux verification: autostart after login, tray behavior, notification behavior.
- [ ] Verify Linux desktop notification support and limitations.
  Required behavior: record actual `notify-send`/libnotify behavior on the tested Linux desktop environments, including sender name, icon support, timeout behavior, action support if any, and fallback behavior when notifications are unavailable. If the tested desktop environment needs portal-based notifications instead of `notify-send`, record the adapter change before release readiness.
  Partial 2026-06-06: Linux notification capability detection now requires both `notify-send` and a DBus session bus before the adapter is considered supported, preventing self-test from claiming notifications work in headless/no-session contexts. The self-test details also record the important libnotify limitations: sender name, icon rendering, timeout, and actions are controlled by the desktop notification daemon, and Cotton Sync does not use notification actions. Local Linux self-test on XFCE reported `notify-send`, DBus session bus available, packaged `icon-192.png`, and the daemon-dependent limitations. Verification: focused notification capability tests passed 14/14, full `Cotton.Sync.Desktop.Tests` passed 389/389, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings. Keep unchecked until clean-machine Linux notification center screenshots/manual behavior are recorded.
- [x] Document Linux tray limitations in app capabilities and self-test.
  Verification 2026-06-03: `DesktopPlatformCapabilities.CreateSnapshot()` records OS/session/current desktop and explains why Linux tray lifecycle is not release-supported yet; settings and self-test now surface that reason. `Cotton.Sync.Desktop.Tests` includes a Linux guard that verifies the app does not claim tray lifecycle support on Linux.
- [ ] Record actual tested Linux desktop environments after clean-machine/manual runs.
  Partial 2026-06-03: desktop settings now explicitly explain when tray lifecycle is unavailable, and Linux/XDG autostart tests verify autostart does not use `--start-minimized` without tray lifecycle support. Keep unchecked until manual Linux desktop-environment results are recorded.

## Phase 10 - Diagnostics And Supportability

- [x] Add structured logging for app, sync, SDK, and platform adapters.
  Partial 2026-06-03: desktop composition now provides a trace-backed `ILoggerFactory` to sync runners, local/remote coordinators, periodic sync, session revocation, sync-root probes, filesystem watchers, and platform command services. `DesktopTraceLogger` redacts secrets before writing to the rotating trace log.
  Partial 2026-06-04: `SyncEngine` now accepts an optional `ILogger<SyncEngine>` and records structured start/completion entries for every sync pass with sync-pair id and activity count; desktop composition wires this into the existing trace-backed logger factory. Focused sync logging test passed 1/1, full `Cotton.Sync.Tests` passed 117/117, full `Cotton.Sync.Desktop.Tests` passed 145/145, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: `CottonHttpTransport` now logs SDK HTTP method/path/status/elapsed through an optional `ILoggerFactory`, redacts sensitive query values such as refresh tokens, logs unauthorized refresh retries, and desktop composition passes the trace-backed logger factory into `CottonCloudClient`. Focused SDK transport logging tests passed 3/3, full `Cotton.Sdk.Tests` passed 23/23, full `Cotton.Sync.Desktop.Tests` passed 145/145, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: SDK JSON response failures now surface as `CottonApiException` with HTTP status, content type, and a short sanitized response preview instead of leaking raw `JsonException` text such as `'< is invalid...'` into desktop sync errors. Focused SDK transport tests passed 5/5, full `Cotton.Sdk.Tests` passed 25/25, full `Cotton.Sync.Desktop.Tests` passed 158/158, and full solution Release build passed with 0 warnings.
  Verification 2026-06-04: the SDK sync changes client now has explicit coverage for SPA fallback HTML returned from `GET /api/v1/sync/changes`, ensuring the desktop path receives a `CottonApiException` with request label, content type, and response preview instead of a raw JSON parser error. Focused `CottonSyncClientTests` passed 3/3, full `Cotton.Sdk.Tests` passed 26/26, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: local sync writer cleanup of stale temp downloads now emits trace warnings when IO or permission errors prevent deleting a leftover `.download` file, rather than hiding the condition. Focused writer tests passed 8/8, full `Cotton.Sync.Tests` passed 134/134, and full solution Release build passed with 0 warnings.
- [x] Add log rotation.
  Verification 2026-06-03: desktop startup installs a `RotatingFileTraceListener` into app-data `cotton-sync.log`; it retains rotated files as `.1`, `.2`, `.3` and is idempotent per log path. Focused `RotatingFileTraceListenerTests` passed 3/3. Full desktop tests and solution build are recorded in the commit verification.
- [x] Add diagnostics screen.
  Required fields: app version, server URL, account, sync pair ids, local paths, remote ids, last sync time, current cursor, last error.
  Verification 2026-06-03: settings diagnostics now shows app version, server URL, account, sync pair ids, local paths, remote paths, remote root ids, per-pair status, last sync time, current change cursor, and last error, plus self-test execution/export controls and latest self-test results. `DesktopShellControllerSelfTestTests` cover required self-test entries and enriched sync-pair diagnostics fields from sync state. Focused tests passed 3/3. Full desktop tests and solution build are recorded in the commit verification.
  Verification 2026-06-04: exported diagnostics now serialize sync-pair mode as a readable camel-case string such as `fullMirror` instead of an enum number, keeping support bundles understandable after mode was added to desktop sync-pair snapshots. `DesktopDiagnosticsExporterTests` passed 4/4, full `Cotton.Sync.Desktop.Tests` passed 205/205, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-05: diagnostics now surfaces the desktop data folder plus the preferences database, sync-state database, and token-store paths, so stale local state can be located from the app instead of guessed. Focused controller/ViewModel tests passed 86/86, full `Cotton.Sync.Desktop.Tests` passed 318/318, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-05: the Diagnostics tab now includes an `Open data` action that opens the app data folder directly from the compact settings surface. Focused ViewModel/visual-contract tests passed 3/3, full `Cotton.Sync.Desktop.Tests` passed 319/319, full solution Release build passed with 0 warnings, and `/tmp/cotton-sync-open-data-diagnostics/settings-diagnostics.png` confirmed the three diagnostics actions fit without wrapping.
- [x] Add export diagnostics bundle command.
  Verification 2026-06-03: settings now exposes an Export diagnostics command; `DesktopDiagnosticsExporter` writes a zip bundle under app-data `diagnostics/` containing `diagnostics.json` and rotated trace logs, while excluding token and SQLite files. Focused `DesktopDiagnosticsExporterTests` passed 2/2. Full desktop tests and solution build are recorded in the commit verification.
  Verification 2026-06-05: exported `diagnostics.json` now includes desktop data-path metadata for the app data folder, preferences database, sync-state database, and token-store paths while still excluding token/SQLite file contents from the archive. Focused diagnostics exporter/controller tests passed 7/7, full `Cotton.Sync.Desktop.Tests` passed 321/321, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
- [x] Redact tokens and secrets from logs and diagnostics.
  Verification 2026-06-03: diagnostics export excludes `tokens.json`, `sync-app.db`, and `sync-state.db`, and `DesktopSecretRedactor` redacts bearer tokens plus JSON/query secrets before writing diagnostics JSON or log entries into the bundle. Focused redaction/exporter tests passed 6/6. Full desktop tests and solution build are recorded in the commit verification.
- [x] Add self-test command.
  Required checks: server reachability, auth state, local folder access, remote folder access, SQLite state access, watcher availability.
  Verification 2026-06-03: self-test now checks preferences DB, sync-pair DB, sync-state DB, authentication state, autostart adapter, tray lifecycle, file watcher availability, server identity, local roots, and remote roots when signed in; unsigned remote roots are explicitly reported as requiring sign-in to verify. Focused `DesktopShellControllerSelfTestTests` passed 2/2. Full desktop tests and solution build are recorded in the commit verification.
  Verification 2026-06-03: self-test also reports token-storage capability so diagnostics distinguish Windows DPAPI, Linux Secret Service, and the development-only restricted-file fallback. Focused token-storage/self-test tests passed 10/10, full `Cotton.Sync.Desktop.Tests` passed 88/88, and full solution Release build passed with the known NU1903 warning.
  Verification 2026-06-03: self-test now verifies the loose packaged desktop icon at `Assets/icon-192.png`, catching broken Linux `.desktop` icon packaging at runtime. Focused `DesktopShellControllerSelfTestTests` passed 13/13, full `Cotton.Sync.Desktop.Tests` passed 120/120, and full solution Release build passed with the known NU1903 warning.
  Verification 2026-06-04: command-line self-test now returns a failed result when token storage is the development-only restricted-file fallback, while still passing on release-secure DPAPI/Secret Service storage. Focused self-test/CLI/token-storage tests passed 20/20, full `Cotton.Sync.Desktop.Tests` passed 141/141, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: desktop self-test now verifies the sync-state change-cursor store by reading a default cursor through `SqliteSyncStateStore.GetChangeCursorAsync`, not only opening/migrating the SQLite file, and reports the concrete `sync-state.db` path in diagnostics. Focused self-test passed 1/1, focused `SqliteSyncStateStoreTests` passed 15/15, full `Cotton.Sync.Desktop.Tests` passed 288/288, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-05: failed desktop self-test rows now show the same normalized user-readable error details as action-required messages while preserving the raw exception in trace logs. This prevents Diagnostics and command-line self-test output from leaking raw local SQLite table errors such as `sync_change_cursors`. Focused self-test tests passed 18/18, full `Cotton.Sync.Desktop.Tests` passed 312/312, and full solution Release build passed with 0 warnings.
- [x] Add tests for redaction.
  Verification 2026-06-03: `DesktopSecretRedactorTests` cover bearer, JSON, and query-string secret patterns; `DesktopDiagnosticsExporterTests` cover archive exclusion of token/database files and redaction of secrets in exported logs.
  Verification 2026-06-04: diagnostics redaction now also covers Basic authorization headers, OIDC `id_token`/`client_secret`, camelCase query token names, and API keys. Focused redactor/exporter/logger tests passed 9/9, full `Cotton.Sync.Desktop.Tests` passed 200/200, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
- [ ] Add manual diagnostics export verification.
  Partial 2026-06-03: added command-line diagnostics export with `--export-diagnostics` / `--diagnostics`, using the existing diagnostics exporter without launching Avalonia. The command prints the generated zip path and is covered by startup CLI tests that assert the archive is created under the isolated data directory. Focused startup tests passed 8/8, full `Cotton.Sync.Desktop.Tests` passed 117/117, and full solution Release build passed with the known NU1903 warning. Keep unchecked until the GUI export path is manually verified on a desktop session.
  Partial 2026-06-04: `ShellViewModel.ExportDiagnosticsCommand` is now covered at the GUI command boundary; it calls the shell controller export path, sets `Diagnostics exported`, clears action-required state, and adds a recent activity row with the bundle path. Focused ViewModel export test passed 1/1, full `Cotton.Sync.Desktop.Tests` passed 146/146, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings. Keep unchecked until the archive is manually exported from a real desktop session.
  Partial 2026-06-04: command-line diagnostics export was smoke-tested manually with `dotnet run --project src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-build -- --export-diagnostics --data-dir /tmp/cotton-diagnostics-smoke-KD5pRp`. The command returned 0 and created `/tmp/cotton-diagnostics-smoke-KD5pRp/diagnostics/cotton-sync-diagnostics-20260604-031107-70b9631e.zip`; Python `zipfile` inspection confirmed the archive contains `diagnostics.json` and `logs/cotton-sync.log` and excludes token/SQLite files. Keep unchecked until GUI export is manually verified from a real desktop session.
  Partial 2026-06-04: CI portable-package smoke now exports a diagnostics bundle from the extracted Linux `.tar.gz` apphost and Windows `.zip` apphost through reusable packaging smoke scripts, parses the reported `Bundle:` path, and fails if the archive is missing. Local Linux archive reproduction created a diagnostics zip and Python `zipfile` inspection confirmed `diagnostics.json` and `logs/cotton-sync.log` are present while token/SQLite files remain excluded. Focused packaging metadata tests passed 23/23, full `Cotton.Sync.Desktop.Tests` passed 223/223, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings. Keep unchecked until GUI export is manually verified from a real desktop session.
  Partial 2026-06-04: GUI diagnostics export is now covered in the deterministic `settings-diagnostics` visual-smoke state; the reviewed Linux window screenshot shows the exported bundle path row and `Open` action after export. Focused ViewModel/visual-contract tests passed 85/85, full `Cotton.Sync.Desktop.Tests` passed 305/305, and full solution Release build passed with 0 warnings. Keep unchecked until the archive is manually exported from a real desktop session.

## Phase 11 - Packaging And Installers

- [x] Define release artifact matrix.
  Required artifacts: Windows installer, Windows portable archive, Linux AppImage or archive, Linux `.deb` if feasible.
  Verification 2026-06-03: release matrix is defined as: Windows portable archive `cotton-sync-desktop-win-x64.tar.gz` now, Windows installer before release readiness, Linux portable archive `cotton-sync-desktop-linux-x64.tar.gz` now, Linux `.deb` or AppImage as feasible before release readiness, `checksums.sha256` inside each portable archive, and NuGet/shared package artifact unchanged. CI already uploads the two portable desktop archives; installer, `.deb`/AppImage, clean-machine smoke, uninstall, and upgrade checks remain open below.
  Verification 2026-06-04: Linux `.deb` packaging is now feasible and wired into CI through `src/Cotton.Sync.Desktop/Packaging/linux/package-deb.sh`. The package installs the published app under `/opt/cotton-sync`, adds `/usr/bin/cotton-sync`, installs the desktop entry under `/usr/share/applications`, and installs the hicolor icon under `/usr/share/icons/hicolor/192x192/apps`. Local verification published `linux-x64`, built `/tmp/cotton-sync-desktop-linux-x64.deb` with `dpkg-deb`, inspected package metadata/contents, parsed the CI workflow YAML, passed `DesktopPackagingMetadataTests` 7/7, passed full `Cotton.Sync.Desktop.Tests` 207/207, and passed `dotnet build src/Cotton.sln --configuration Release` with 0 warnings.
  Verification 2026-06-04: Windows portable artifacts now include `cotton-sync-desktop-win-x64.zip` in addition to the existing `.tar.gz`, so Windows users do not need a Unix-style archive tool. The zip is built by reusable script `src/Cotton.Sync.Desktop/Packaging/windows/package-zip.py`; local verification published `win-x64`, built `/tmp/cotton-sync-desktop-win-x64.zip` through that script, and confirmed the archive contains `Cotton.Sync.Desktop.exe` plus `Assets/icon-192.png`. `python3 -m py_compile` passed for the script, `DesktopPackagingMetadataTests` passed 9/9, full `Cotton.Sync.Desktop.Tests` passed 209/209, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: Windows installer script `src/Cotton.Sync.Desktop/Packaging/windows/cotton-sync.iss` now defines the release install layout for Inno Setup: per-user install privileges, x64-compatible architecture, app icon metadata, recursive published-file install, Start Menu shortcut, optional desktop shortcut, and post-install launch. Focused `DesktopPackagingMetadataTests` passed 11/11. CI installer build and clean Windows VM install smoke remain open below.
  Verification 2026-06-04: Windows installer Start Menu group now includes both `Cotton Sync` and `Uninstall Cotton Sync` shortcuts, giving users a visible app-group uninstall entry in addition to the standard Windows installed-apps uninstaller registration. Windows CI smoke checks both shortcuts after install and fails if either remains after uninstall. Local verification parsed `.github/workflows/docker-image.yml`, focused packaging tests passed 2/2, full `Cotton.Sync.Desktop.Tests` passed 287/287, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-04: Windows installer uninstall now cleans the current-user `Cotton Sync` Run-registry autostart value, and Windows CI installer smoke verifies the Start Menu shortcut is created, then seeds the autostart registry value before uninstall and fails if either the shortcut or autostart value remains afterward. Focused packaging/autostart tests passed 35/35, full `Cotton.Sync.Desktop.Tests` passed 262/262, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-04: CI now builds and uploads `cotton-sync-desktop-win-x64-setup.exe` from the Inno Setup script in the Windows smoke job after publishing and self-testing the Windows apphost. The installer artifact is downloaded into release artifacts, where the release job generates the final `release-artifact-checksums.sha256`. Local verification parsed workflow YAML and focused `DesktopPackagingMetadataTests` passed 12/12. Clean Windows VM install smoke remains open below.
  Verification 2026-06-04: Windows installer defaults to `{localappdata}\Programs\Cotton Sync` so `PrivilegesRequired=lowest` is a real per-user install path instead of a non-admin attempt to write under Program Files. Windows CI now silently installs the setup into a temp directory, runs installed `Cotton.Sync.Desktop.exe --self-test --data-dir <temp>`, and silently uninstalls through `unins000.exe`. Clean Windows VM install smoke remains open below.
- [x] Configure self-contained publish for Windows x64.
  Verification 2026-06-03: added `src/Cotton.Sync.Desktop/Properties/PublishProfiles/win-x64.pubxml`; `dotnet publish src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj /p:PublishProfile=win-x64` passed and produced `bin/Release/net10.0/publish/win-x64/Cotton.Sync.Desktop.exe` with the known NU1903 warning.
  Verification 2026-06-04: `Cotton.Sync.Desktop.csproj` now declares release runtime identifiers `win-x64;linux-x64`, and metadata tests assert apphost/icon/runtime-id configuration. `dotnet publish src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --runtime win-x64 --self-contained true --no-restore -p:PublishSingleFile=false -p:PublishTrimmed=false -p:PublishReadyToRun=false -p:GeneratePublishChecksums=false` passed.
- [x] Configure self-contained publish for Linux x64.
  Verification 2026-06-04: after the Avalonia 12.0.4 upgrade, `dotnet publish src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj /p:PublishProfile=linux-x64 -p:GeneratePublishChecksums=false` and `/p:PublishProfile=win-x64 -p:GeneratePublishChecksums=false` both passed.
  Verification 2026-06-03: added `src/Cotton.Sync.Desktop/Properties/PublishProfiles/linux-x64.pubxml`; `dotnet publish src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj /p:PublishProfile=linux-x64` passed and produced executable `bin/Release/net10.0/publish/linux-x64/Cotton.Sync.Desktop` with the known NU1903 warning.
- [x] Add app icon and metadata for packaged apps.
  Verification 2026-06-03: generated `src/Cotton.Sync.Desktop/Assets/app.ico` from the real frontend PNG icon and set `ApplicationIcon=Assets/app.ico`; Avalonia window/tray resources continue to use the frontend `icon-192.png`. Desktop build and both `linux-x64`/`win-x64` publish profiles passed with the known NU1903 warning. Clean Windows visual icon verification remains open under package smoke tests.
  Verification 2026-06-04: regenerated `Assets/app.ico` as a multi-size Windows icon, added it as an Avalonia resource, and set the window icon to `/Assets/app.ico` so runtime window/taskbar metadata and published apphost metadata use the same branded icon. Xvfb startup smoke with the `.ico` resource passed, and `dotnet publish ... /p:PublishProfile=win-x64 -p:GeneratePublishChecksums=false` produced the Windows executable. Manual Windows taskbar visual verification remains open.
  Verification 2026-06-04: Windows installer launch shortcuts now carry `AppUserModelID` matching `DesktopAppIdentity.AppUserModelId`, aligning Start Menu/desktop shortcut identity with the runtime process identity used for taskbar grouping and toast notifications. Focused installer metadata test passed 1/1, packaging metadata tests passed 28/28, full `Cotton.Sync.Desktop.Tests` passed 289/289, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-04: Linux package metadata now includes `Packaging/linux/cotton-sync.desktop`, copied into the `linux-x64` publish output as `cotton-sync.desktop` with application name, launcher command, icon name, categories, and `StartupWMClass`. `DesktopPackagingMetadataTests` passed 5/5, full `Cotton.Sync.Desktop.Tests` passed 199/199, `dotnet publish ... /p:PublishProfile=linux-x64 -p:GeneratePublishChecksums=false` produced the desktop entry, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
- [x] Add version/about metadata.
  Verification 2026-06-03: `Cotton.Sync.Desktop.csproj` now sets explicit title, `VersionPrefix=0.1.0`, `AssemblyVersion=0.1.0.0`, `FileVersion=0.1.0.0`, and `InformationalVersion=0.1.0-dev`. `dotnet msbuild` confirmed `Version=0.1.0`, `InformationalVersion=0.1.0-dev`, and `ApplicationIcon=Assets/app.ico`; desktop tests passed 56/56.
  Verification 2026-06-04: Settings/About and diagnostics now display `AssemblyInformationalVersionAttribute` first, with assembly version only as a fallback, so the UI shows release labels such as `0.1.0-dev` instead of raw assembly versions like `0.1.0.0`. Focused `ShellViewModelSyncPairCommandTests` passed 35/35, full `Cotton.Sync.Desktop.Tests` passed 203/203, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: `DesktopAppVersion` now centralizes the version label for both Settings/About and exported diagnostics bundles, preventing GUI and support diagnostics from diverging. `ExportDiagnosticsAsync_UsesInformationalAppVersion` passed, full `Cotton.Sync.Desktop.Tests` passed 204/204, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
- [x] Add checksum generation.
  Verification 2026-06-03: `Cotton.Sync.Desktop.csproj` now generates `checksums.sha256` after publish. Verified with both `linux-x64` and `win-x64` publish profiles; checksum files include the Linux apphost, Windows `.exe`, and desktop DLL entries. Both publish commands passed with the known NU1903 warning.
  Verification 2026-06-04: desktop package scripts now require `checksums.sha256` before producing release artifacts. `package-deb.sh` fails when the Linux publish output lacks checksums, and `package-zip.py` fails when the Windows publish output lacks checksums. Local verification published `linux-x64` and `win-x64` with checksum generation enabled, built `/tmp/cotton-sync-desktop-linux-x64.deb` and `/tmp/cotton-sync-desktop-win-x64.zip`, confirmed both artifacts contain `checksums.sha256`, passed `DesktopPackagingMetadataTests` 9/9, passed full `Cotton.Sync.Desktop.Tests` 209/209, and passed `dotnet build src/Cotton.sln --configuration Release` with 0 warnings.
  Verification 2026-06-04: the release job now generates `release-artifact-checksums.sha256` after downloading the NuGet package, Linux `.tar.gz`, Linux `.deb`, Windows `.tar.gz`, Windows `.zip`, and Windows installer artifacts, excluding existing `.sha256` files from the input set. Local verification parsed workflow YAML, focused `DesktopPackagingMetadataTests` passed 12/12, full `Cotton.Sync.Desktop.Tests` passed 212/212, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: package smoke now verifies `checksums.sha256` contents instead of only checking that the file exists. Added Linux and Windows verification scripts and wired them into CI publish, archive, `.deb`, zip, installer, and upgrade smoke paths. Local Linux verification caught and fixed a bad linked-content manifest path (`icon-192.png` instead of `Assets/icon-192.png`) and a `.deb` packaging bug that removed `cotton-sync.desktop` from `/opt/cotton-sync` after it had been included in the manifest. `dotnet publish ... /p:PublishProfile=linux-x64`, Linux publish checksum verification, Linux archive checksum verification, and Linux `.deb` checksum verification passed; focused `DesktopPackagingMetadataTests` passed 27/27, full `Cotton.Sync.Desktop.Tests` passed 249/249, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings. Windows checksum verification is wired and contract-tested, but local PowerShell execution was not available in this Linux environment.
- [x] Add signing plan.
  Windows code signing can be deferred only with an explicit release risk decision.
  Verification 2026-06-03: signing plan is explicit: final Windows installer/executable should be Authenticode-signed with a project code-signing certificate; portable archives must always include generated SHA-256 checksums; Linux archives remain checksum-verified, while future `.deb`/repository distribution should use package/repository signing. If Windows code signing is unavailable for an internal preview, the release notes must record an explicit unsigned-build risk decision before release readiness can be checked.
- [x] Add CI workflow for build, test, publish, package, and artifact upload.
  Verification 2026-06-03: `.github/workflows/docker-image.yml` now publishes `linux-x64` and `win-x64` desktop profiles, runs the Linux published `--self-test --data-dir <temp>` smoke command, packages portable `cotton-sync-desktop-linux-x64.tar.gz` and `cotton-sync-desktop-win-x64.tar.gz` archives, uploads both as workflow artifacts, and attaches them to main-branch releases beside the NuGet package. Local workflow command simulation confirmed both archives contain the executable and `checksums.sha256`; YAML parsed successfully with PyYAML.
  Verification 2026-06-04: CI Linux desktop smoke now installs `libsecret-tools` before publishing/smoking the Linux desktop artifact, matching the release-secure token-storage self-test gate instead of relying on runner image defaults. Local YAML parse passed.
  Verification 2026-06-04: CI Linux desktop smoke now installs `dbus-x11` and `gnome-keyring`, then runs the published self-test inside `dbus-run-session` with `gnome-keyring-daemon --components=secrets`, matching the stricter live Secret Service token-storage probe. Local YAML parse and focused desktop token-storage/lifecycle tests passed.
  Verification 2026-06-04: CI test job now runs a `.csproj`-only `dotnet list <project> package --vulnerable --include-transitive --no-restore` guard after solution restore, avoiding the `cotton.client.esproj` package.config limitation while failing the workflow when vulnerable .NET packages are found. Local simulation scanned 28 projects with no findings, and `.github/workflows/docker-image.yml` parsed successfully with PyYAML.
  Verification 2026-06-04: CI now includes a `windows-latest` desktop smoke job that publishes `win-x64` and runs `Cotton.Sync.Desktop.exe --self-test --data-dir <temp>` before the release job can run. Local verification parsed workflow YAML, `DesktopPackagingMetadataTests` passed 8/8, full `Cotton.Sync.Desktop.Tests` passed 208/208, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings. Clean Windows VM install/manual tray verification remains open.
  Verification 2026-06-04: Windows desktop smoke now also runs `Packaging/windows/verify-associated-icon.ps1` against the published apphost, extracted zip apphost, installed Inno Setup apphost, and upgraded installed apphost. The verifier compares the executable associated icon to `Assets/app.ico` through a normalized 32x32 bitmap hash, catching generic taskbar/apphost icons before release artifacts are accepted. Local verification parsed workflow YAML and focused packaging metadata tests passed 28/28; PowerShell execution is deferred to the `windows-latest` CI job because this Linux environment has no `pwsh`.
  Verification 2026-06-04: Windows installer install and upgrade smoke now also run `Packaging/windows/verify-shortcut-app-id.ps1`, reading the installed Start Menu shortcut's `System.AppUserModel.ID` shell property and requiring it to match `Cotton.Sync.Desktop`. Focused workflow/script tests passed 4/4, packaging metadata tests passed 29/29, workflow YAML parsed, full `Cotton.Sync.Desktop.Tests` passed 290/290, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.
  Verification 2026-06-04: the same Windows CI job now installs Inno Setup, builds `cotton-sync-desktop-win-x64-setup.exe`, uploads it as `desktop-windows-installer`, and the release job downloads that artifact beside the portable packages before generating a single release-level checksum file. Local verification parsed workflow YAML and focused `DesktopPackagingMetadataTests` passed 12/12.
  Verification 2026-06-04: CI Linux desktop smoke now installs Xvfb/ffmpeg and captures `desktop-linux-gui-screenshot` from the freshly published Linux GUI apphost. Local verification caught a stale `Tmds.DBus.Protocol` publish-output crash before the clean publish fix, then captured `/tmp/cotton-sync-desktop-linux-gui.png` successfully after clean checksum-enabled publish. The screenshot script now also verifies PNG dimensions and rejects single-color frames. Focused packaging tests passed 23/23, full `Cotton.Sync.Desktop.Tests` passed 223/223, and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-04: CI portable-package smoke now runs reusable diagnostics export scripts from the extracted Linux archive and Windows zip apphosts after self-test, requiring the reported diagnostics bundle file to exist. Local verification parsed workflow YAML, focused packaging tests passed 23/23, and Linux archive reproduction confirmed the exported zip contents.
  Verification 2026-06-04: CI installed-package smoke now also runs reusable diagnostics export scripts from the installed Linux `.deb` apphost and installed Windows Inno Setup apphost after self-test, including upgrade-installed apphosts, requiring the reported diagnostics bundle file to exist before uninstall. Full `Cotton.Sync.Desktop.Tests` passed 223/223 and `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings.
  Verification 2026-06-05: reusable Linux/Windows diagnostics export smoke scripts now inspect the exported diagnostics bundle, require `diagnostics.json`, and verify `dataPaths` metadata for the app data folder, preferences DB, sync-state DB, and token-store paths instead of only checking that the bundle exists. Focused packaging metadata tests passed 2/2, Linux script syntax passed, local Linux published-app smoke exported and verified bundle metadata after a fresh publish, full `Cotton.Sync.Desktop.Tests` passed 321/321, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings. Local PowerShell execution was not available, so Windows script validation is covered by contract tests and CI Windows smoke.
- [x] Add smoke launch command or self-test command for packaged app.
  Verification 2026-06-03: added `--self-test`/`--smoke-test` desktop CLI mode plus `--data-dir` for isolated packaged smoke runs. The command prints self-test results and returns exit code `0` only when checks pass. Verified with `dotnet run --project src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore -- --self-test --data-dir <temp>` and the published Linux executable `bin/Release/net10.0/publish/linux-x64/Cotton.Sync.Desktop --self-test --data-dir <temp>`; both passed. `win-x64` publish, full desktop tests 59/59, and full solution Release build passed with the known NU1903 warning.
- [ ] Test Windows installer on a clean VM.
  Partial 2026-06-04: the Inno Setup script exists and is covered by metadata tests, but the installer has not yet been built on Windows or installed on a clean VM.
  Partial 2026-06-04: CI is now configured to build and upload the installer on `windows-latest`; this still does not replace a clean Windows VM install/uninstall smoke.
  Partial 2026-06-04: Windows CI now performs silent install, installed app self-test, and silent uninstall of the generated setup on `windows-latest`. This still does not replace manual clean Windows VM verification of taskbar icon, tray, autostart, notifications, login, sync, and diagnostics.
  Partial 2026-06-04: Windows CI installed-app and upgrade-installed smoke now also export diagnostics bundles through `Packaging/windows/smoke-diagnostics-export.ps1` and verify the reported bundle path exists before uninstall.
  Partial 2026-06-04: Windows installer now declares an Inno Setup `AppMutex` matching a Windows-only runtime mutex held by the desktop process, so install/uninstall can detect a running Cotton Sync process before replacing or removing installed files. The installer also explicitly keeps `CloseApplications=yes` and `RestartApplications=no` for predictable close-without-relaunch behavior. Focused startup/packaging tests passed 31/31, full `Cotton.Sync.Desktop.Tests` passed 286/286, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings. Real clean Windows uninstall while the app is running remains open for manual VM verification.
- [ ] Test Windows portable archive on a clean VM.
  Partial 2026-06-04: Windows CI now creates a zip archive from the published `win-x64` output, extracts it on `windows-latest`, and runs `Cotton.Sync.Desktop.exe --self-test --data-dir <temp>` from the extracted archive. This verifies archive layout on Windows, but it is not a clean VM smoke of the uploaded release artifact.
  Partial 2026-06-04: the Windows zip CI smoke now also exports diagnostics from the extracted archive through `Packaging/windows/smoke-diagnostics-export.ps1` and verifies the reported diagnostics bundle path exists. This still does not replace clean VM verification of the final uploaded artifact.
- [ ] Test Linux package/archive on a clean VM.
  Partial 2026-06-04: locally extracted `/tmp/cotton-sync-desktop-linux-x64.deb` with `dpkg-deb -x` and ran `/opt/cotton-sync/Cotton.Sync.Desktop --self-test --data-dir <temp>` from the extracted package layout. The packaged apphost started, found the packaged desktop icon, opened SQLite state, detected Linux/XFCE platform capabilities, and reported notification/file-watcher checks. The self-test correctly failed only at `Token storage` because this local environment lacks `secret-tool`, so this does not replace the required clean Linux VM smoke with Secret Service available.
  Partial 2026-06-04: Linux CI now extracts `cotton-sync-desktop-linux-x64.tar.gz` and `cotton-sync-desktop-linux-x64.deb`, verifies the `.deb` desktop entry, icon, and `/usr/bin/cotton-sync` symlink, then runs the packaged apphost self-test from both extracted layouts inside a `dbus-run-session` Secret Service environment. Local verification parsed workflow YAML, passed focused `DesktopPackagingMetadataTests` 14/14, extracted the current `/tmp` Linux archive/deb layouts, passed full `Cotton.Sync.Desktop.Tests` 214/214, and passed `dotnet build src/Cotton.sln --configuration Release` with 0 warnings. This still does not replace a clean Linux VM install/uninstall smoke.
  Partial 2026-06-04: Linux archive smoke now also exports diagnostics from the extracted `.tar.gz` apphost through `Packaging/linux/smoke-diagnostics-export.sh` and verifies the diagnostics bundle exists. Local reproduction from the current publish output inspected the zip contents and confirmed diagnostics entries are present while token/SQLite files remain excluded.
  Partial 2026-06-04: Linux CI now also installs `cotton-sync-desktop-linux-x64.deb` with `sudo dpkg -i`, verifies `/opt/cotton-sync/Cotton.Sync.Desktop` and `/usr/bin/cotton-sync`, runs the installed apphost self-test inside a Secret Service session, uninstalls with `sudo dpkg -r cotton-sync-desktop`, and fails if the apphost or symlink remain. This still does not replace manual clean Linux VM verification of desktop environment behavior, autostart, notifications, login, sync, and diagnostics.
  Partial 2026-06-04: Linux installed and upgrade-installed `.deb` smoke now also export diagnostics bundles from `/opt/cotton-sync/Cotton.Sync.Desktop` through `Packaging/linux/smoke-diagnostics-export.sh` and verify the reported bundle path exists before uninstall.
- [ ] Test uninstall behavior.
  Partial 2026-06-04: Windows CI now runs the generated Inno Setup uninstaller silently and fails if `Cotton.Sync.Desktop.exe` remains under the temp install directory. Linux uninstall and real clean-machine uninstall behavior remain open.
  Partial 2026-06-04: Linux CI now runs `sudo dpkg -r cotton-sync-desktop` after installed-package self-test and verifies the installed apphost and `/usr/bin/cotton-sync` symlink are removed. Real clean-machine uninstall behavior remains open.
  Verification 2026-06-04: Linux `.deb` packaging now includes a `postrm` cleanup script that removes only Cotton Sync user autostart files whose contents contain both `Name=Cotton Sync` and `Exec=/opt/cotton-sync/Cotton.Sync.Desktop`, preventing stale XDG autostart entries after uninstall without deleting unrelated user desktop files. CI seeds `$HOME/.config/autostart/cotton-sync.desktop` before `sudo dpkg -r cotton-sync-desktop` and fails if it remains. Local verification ran `bash -n package-deb.sh`, parsed `.github/workflows/docker-image.yml`, passed focused packaging tests 2/2, built a `.deb`, extracted `DEBIAN/postrm`, confirmed it is executable, ran `sh -n` on it, passed full `Cotton.Sync.Desktop.Tests` 287/287, and passed `dotnet build src/Cotton.sln --configuration Release --no-restore` with 0 warnings. Clean-machine uninstall verification remains open.
  Partial 2026-06-06: Windows installer upgrade smoke now also seeds the Cotton Sync `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` value after upgrade and verifies the Inno uninstaller removes it, matching the normal autostart cleanup path instead of only checking shortcut/file removal. Focused packaging metadata tests passed 29/29, full solution Release build passed with 0 warnings, and `git diff --check` passed.
  Partial 2026-06-06: Linux `.deb` upgrade smoke now seeds a Cotton Sync XDG autostart file before installing the upgraded package, then verifies `dpkg -r cotton-sync-desktop` removes the upgraded apphost, `/usr/bin/cotton-sync`, and the user autostart file. Focused packaging metadata tests passed 29/29, workflow YAML parsed, `bash -n package-deb.sh` passed, full solution Release build passed with 0 warnings, and `git diff --check` passed. Keep unchecked until clean-machine uninstall/upgrade behavior is verified manually.
- [ ] Test upgrade over previous build.
  Partial 2026-06-04: Linux CI now builds a lower-version `cotton-sync-desktop-linux-x64-old.deb`, installs it, installs the current `.deb` over it, verifies `dpkg-query` reports the current GitVersion SemVer, runs the upgraded apphost self-test inside a Secret Service session, and removes the package. Windows installer upgrade and real clean-machine upgrade behavior remain open.
  Partial 2026-06-04: Windows CI now builds a lower-version Inno Setup installer, installs it silently into a temp directory, installs the current setup over it using the same `AppId` and install path, runs installed apphost self-test, and uninstalls. Real clean-machine upgrade behavior remains open.
  Partial 2026-06-04: Windows installer upgrade smoke now verifies the upgraded Start Menu launch and uninstall shortcuts exist after installing the current setup over an older setup, then fails if either shortcut remains after the upgraded app is uninstalled. Local verification parsed `.github/workflows/docker-image.yml`, focused `DesktopPackagingMetadataTests.CiWorkflow_SmokesWindowsInstallerUpgrade` passed 1/1, full `Cotton.Sync.Desktop.Tests` passed 287/287, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings.

## Phase 12 - End-To-End Test Matrix

- [x] One client: local create uploads to remote.
  Verification 2026-06-03: added `SyncClientEndToEndTests.RunOnceAsync_UploadsLocalFileThroughSdkToServer`, which logs in through `CottonCloudClient`, creates a remote sync root, runs `SyncEngine` with `LocalFileScanner`, `RemoteTreeCrawler`, `SdkRemoteFileSynchronizer`, and `SqliteSyncStateStore` against the integration-test server, verifies the uploaded remote file content by downloading it through the SDK, and verifies the local baseline. Focused server integration test passed 1/1, and full solution Release build passed with the known NU1903 warning.
- [x] One client: local update uploads to remote.
  Verification 2026-06-03: extended `SyncClientEndToEndTests` with `RunOnceAsync_UploadsLocalUpdateThroughSdkToServer`, which performs an initial upload, edits the local file, reruns `SyncEngine` with the same SQLite baseline, verifies the server file content changed through SDK download, and verifies the updated baseline hashes. Focused server-backed sync E2E tests passed 2/2, and full solution Release build passed with the known NU1903 warning.
- [x] One client: local delete moves remote item to trash or configured delete behavior.
  Verification 2026-06-03: extended `SyncClientEndToEndTests` with `RunOnceAsync_MovesRemoteFileToTrashWhenLocalFileIsDeleted`, which uploads a local file through `SyncEngine`, deletes it locally, reruns sync, verifies the normal remote folder no longer lists the file, verifies the server kept the `node_files` row but moved it away from the original folder, and verifies the local SQLite baseline was removed. Focused server-backed sync E2E tests passed 5/5, and full solution Release build passed with the known NU1903 warning.
- [x] One client: remote create downloads locally.
  Verification 2026-06-03: extended `SyncClientEndToEndTests` with `RunOnceAsync_DownloadsRemoteCreatedFileThroughSdkToLocal`, which creates a remote folder and file through SDK node/chunk/file APIs, runs `SyncEngine` against an empty local root, verifies the local downloaded file content, and verifies the SQLite baseline. Focused server-backed sync E2E tests passed 3/3, and full solution Release build passed with the known NU1903 warning.
- [x] One client: remote update downloads locally.
  Verification 2026-06-03: extended `SyncClientEndToEndTests` with `RunOnceAsync_DownloadsRemoteUpdateThroughSdkToLocal`, which downloads an initial remote file, updates its content through SDK chunk/file APIs with the previous ETag, reruns `SyncEngine`, verifies the local file content updates, and verifies the SQLite baseline hashes. Focused server-backed sync E2E tests passed 4/4, and full solution Release build passed with the known NU1903 warning.
- [x] One client: remote delete applies locally through safe delete behavior.
  Verification 2026-06-03: extended `SyncClientEndToEndTests` with `RunOnceAsync_MovesLocalFileToQuarantineWhenRemoteFileIsDeleted`, which creates and downloads a remote file, deletes the remote file through the SDK, reruns `SyncEngine`, verifies the local file is removed from the sync root, verifies its content is preserved under `.cotton-sync/deleted`, and verifies the SQLite baseline was removed. Focused server-backed sync E2E tests passed 6/6, and full solution Release build passed with the known NU1903 warning.
- [x] Two clients: local change on client A reaches client B.
  Verification 2026-06-03: extended `SyncClientEndToEndTests` with `RunOnceAsync_PropagatesClientALocalChangeToClientB`, using two authenticated `CottonCloudClient` instances, two local roots, and two independent SQLite sync state stores. Client A uploads a created file, client B downloads it, client A updates the file, and client B downloads the updated content through a second sync pass. Focused server-backed sync E2E tests passed 7/7, and full solution Release build passed with the known NU1903 warning.
- [x] Two clients: simultaneous edit creates conflict and preserves both versions.
  Verification 2026-06-03: extended `SyncClientEndToEndTests` with `RunOnceAsync_CreatesConflictWhenTwoClientsEditSameFile`, where two clients share an initial baseline, client A uploads an edit, client B makes a divergent local edit before syncing, and client B's sync emits a conflict activity. The test verifies client B's local version remains in place, the remote version is saved as a `Cotton conflict` file, and the baseline records divergent local/remote hashes. Focused server-backed sync E2E tests passed 8/8, and full solution Release build passed with the known NU1903 warning.
- [x] Two clients: rename/move propagates correctly.
  Verification 2026-06-03: `SyncClientEndToEndTests.RunOnceAsync_PropagatesClientARenameMoveToClientB` verifies two independent clients and SQLite state stores converge after client A moves and renames `Docs/draft.txt` to `Archive/Reports/final.txt`. The test verifies client A uploads the new path and deletes the old remote path, client B downloads the new path, moves the old local copy into `.cotton-sync/deleted`, removes the old baseline, records the new baseline, and preserves file content locally and remotely. Focused rename/move E2E passed 1/1, full server-backed sync E2E tests passed 18/18, and full solution Release build passed with the known NU1903 warning.
- [x] Offline: local changes accumulate and sync after reconnect.
  Verification 2026-06-03: `SyncClientEndToEndTests.SyncPairRunner_UploadsLocalChangesAfterOfflineRecoveryThroughSdkAndServer` verifies a local file created before connectivity is available remains unsynced while the first `SyncPairRunner.SyncNowAsync` call fails with a transient `ServiceUnavailable` offline condition, leaving remote state and SQLite baseline empty. A second sync using the same runner after recovery runs real `SyncEnginePairWork` against the SDK/integration-test server, uploads the accumulated local file, returns the runner to idle, and records the remote file id/hash in SQLite. Focused offline-local E2E passed 1/1, full server-backed sync E2E tests passed 19/19, and full solution Release build passed with the known NU1903 warning.
- [x] Offline: remote changes accumulate and sync after reconnect.
  Verification 2026-06-03: `SyncClientEndToEndTests.SyncPairRunner_DownloadsRemoteChangesAfterOfflineRecoveryThroughSdkAndServer` verifies a remote file created while the client is unavailable remains absent locally while the first `SyncPairRunner.SyncNowAsync` call fails with a transient `ServiceUnavailable` offline condition and leaves SQLite baseline empty. A second sync using the same runner after recovery runs real `SyncEnginePairWork` against the SDK/integration-test server, downloads the accumulated remote file, returns the runner to idle, and records the remote file id/hash in SQLite. Focused offline-remote E2E passed 1/1, full server-backed sync E2E tests passed 20/20, and full solution Release build passed with the known NU1903 warning.
- [x] Server restart during sync recovers.
  Partial 2026-06-03: `SyncClientEndToEndTests.RunOnceAsync_RecoversAfterServerHostRestartWithExistingLocalState` verifies the integration-test server host can be disposed and recreated with the same PostgreSQL database while the client keeps its local SQLite sync state. After restart, a new SDK client logs in, updates an already-synced local file, syncs through `SyncEngine`, updates the same remote file id, and refreshes the SQLite baseline. Focused restart smoke passed 1/1, full server-backed sync E2E tests passed 22/22, and full solution Release build passed with the known NU1903 warning.
  Verification 2026-06-04: `SyncClientEndToEndTests.RunOnceAsync_RecoversAfterServerHostRestartDuringUploadBeforeBaselineUpdate` now exercises the dangerous mid-sync restart point: the remote upload succeeds, the integration-test server host restarts before the client records SQLite baseline, the first run surfaces `503 ServiceUnavailable`, and a second run with a new SDK client adopts the already-uploaded remote file without duplication and records the baseline. Focused server restart E2E passed 1/1.
- [ ] Client crash during sync recovers.
  Partial 2026-06-03: `SyncClientEndToEndTests.RunOnceAsync_RecoversAfterRemoteUploadBeforeBaselineUpdateThroughSdkAndServer` simulates the dangerous crash point where the SDK/server upload succeeds but local SQLite baseline persistence fails before commit. The next run uses a fresh `SyncEngine` with the same SQLite state, adopts the already-uploaded remote file without duplicating it, records the baseline, and emits no extra upload activity. Focused crash-point E2E passed 1/1, full server-backed sync E2E tests passed 23/23, and full solution Release build passed with the known NU1903 warning. Keep unchecked until a process-level client kill/restart smoke is run.
  Partial 2026-06-04: sync-core now also covers the corresponding download-side crash point through `RunOnceAsync_RecoversAfterRemoteDownloadBeforeBaselineUpdate`, proving the engine adopts an already-written local copy after baseline persistence fails. Full `Cotton.Sync.Tests` passed 121/121. Keep unchecked until a process-level client kill/restart smoke is run.
  Partial 2026-06-04: sync-core now covers delete-side baseline failures too: remote delete succeeds before baseline delete failure and local safe-delete succeeds before baseline delete failure. The next run removes the stale baseline without repeating remote operations or re-uploading locally deleted files. Focused recovery tests passed 4/4 and full `Cotton.Sync.Tests` passed 123/123. Keep unchecked until a process-level client kill/restart smoke is run.
  Partial 2026-06-04: added `SyncCliCommandRunnerTests.SyncOnce_ExternalProcessRecoversAfterRemoteUploadBeforeBaselineUpdate`, which starts a real external `dotnet Cotton.Sync.Cli.dll sync-once` process against a local HTTP server, blocks the server response immediately after `/api/v1/files/from-chunks` commits the remote file, kills the process before SQLite baseline persistence, then starts a second external CLI process using the same local root and state database. The recovery process reports `Activities: 0`, records one baseline, and the server sees only one file-create request, proving process-level upload crash recovery without a duplicate upload. Verification: full `Cotton.Sync.Cli.Tests` passed 13/13, `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings, and `git diff --check` passed. Keep unchecked until process-level download/delete or packaged-client kill/restart smoke covers the remaining crash points.
  Partial 2026-06-04: added `SyncCliCommandRunnerTests.SyncOnce_ExternalProcessRecoversAfterRemoteDeleteBeforeBaselineDelete`, which pre-seeds a SQLite baseline for a remote file, starts a real external `dotnet Cotton.Sync.Cli.dll sync-once` process, blocks the server response immediately after `DELETE /api/v1/files/{id}?skipTrash=false` commits the remote delete, kills the process before baseline deletion, then starts a second external CLI process with the same local root and database. The recovery process reports `Activities: 0`, removes the stale baseline, and the server sees only one delete request. Verification: focused process-delete crash test passed 1/1 and full `Cotton.Sync.Cli.Tests` passed 15/15. Keep unchecked until process-level download or packaged-client kill/restart smoke covers the remaining crash points.
  Partial 2026-06-04: added process-level download crash coverage through `SyncCliCommandRunnerTests.SyncOnce_ExternalProcessRecoversAfterCrashDuringRemoteDownload`. The first external CLI process is killed while a remote download is blocked halfway and a partial temp file exists; the second external CLI process removes the stale temp file, downloads the full file, and records the baseline. Verification: full `Cotton.Sync.Tests` passed 134/134 and full `Cotton.Sync.Cli.Tests` passed 16/16. Keep unchecked until a packaged desktop kill/restart smoke is run against the GUI lifecycle.
- [ ] Disk full shows action-required error.
  Partial 2026-06-03: `SyncPairRunner` classifies local disk-full `IOException` failures into the user-facing action-required message `Local disk is full. Free space on this computer and retry sync.`. `SyncPairRunner_ReportsDiskFullDuringDownloadThroughSdkAndServer` verifies the action-required path while downloading a real remote file through SDK/server and using a local writer that throws a disk-full HResult before baseline update, leaving no local file and no SQLite baseline. Focused disk-full smoke passed 1/1, full server-backed sync E2E tests passed 24/24, full `Cotton.Sync.App.Tests` passed 91/91, and full solution Release build passed with the known NU1903 warning. Keep unchecked until a real filesystem/quota disk-full scenario is run end to end.
  Partial 2026-06-04: desktop action-required dashboard and notification copy now normalize disk-full-like `IOException` messages into `This computer does not have enough free disk space for sync. Free space and retry.` instead of surfacing raw OS/backend text. Focused resolver/notification/shell tests passed 88/88, full `Cotton.Sync.Desktop.Tests` passed 301/301, and full solution Release build passed with 0 warnings. Keep unchecked until a real filesystem/quota disk-full scenario is run end to end.
- [ ] Permission denied shows action-required error.
  Partial 2026-06-03: `LocalFileScanner` now distinguishes real local permission failures through `LocalFilePermissionDeniedException` instead of retryable `LocalFileUnavailableException`, so unreadable files are action-required rather than transient. `SyncPairRunner_ReportsPermissionDeniedForUnreadableLocalFileThroughSdkAndServer` verifies a Unix file with no read permission bits fails through real `SyncPairRunner` + `SyncEnginePairWork`, reports `Permission denied while accessing local sync files. Check folder permissions and retry.`, uploads nothing, and records no SQLite baseline. Focused scanner test passed 1/1, focused Linux permission E2E passed 1/1, full `Cotton.Sync.Tests` passed 91/91, full server-backed sync E2E tests passed 21/21, and full solution Release build passed with the known NU1903 warning. Keep unchecked until Windows ACL permission-denied coverage is verified on Windows.
  Partial 2026-06-04: desktop action-required dashboard and notification copy now normalize local permission-denied exceptions/messages into direct file-access guidance, including the affected relative path when available. Focused resolver/notification/shell tests passed 88/88, full `Cotton.Sync.Desktop.Tests` passed 301/301, and full solution Release build passed with 0 warnings. Keep unchecked until Windows ACL permission-denied coverage is verified on Windows.
- [x] Quota exceeded shows action-required error.
  Verification 2026-06-03: server file operations now convert logical storage-quota failures into HTTP 507 through `StorageQuotaExceededExceptionHandler`, matching the sync client's action-required contract. `SyncClientEndToEndTests.SyncPairRunner_ReportsQuotaExceededThroughSdkAndServer` sets a tiny default quota, runs real `SyncPairRunner` + `SyncEnginePairWork` + SDK/server upload, verifies the runner reports `Remote storage quota exceeded. Free space in Cotton Cloud or choose a smaller sync folder.`, verifies the failed upload creates no remote file and no SQLite baseline, and resets the server quota so E2E tests remain isolated. Focused quota E2E passed 1/1, full server-backed sync E2E tests passed 17/17, `ChunksAndFilesEndpointsTests` passed 26/26, and full solution Release build passed with the known NU1903 warning.
  Verification 2026-06-04: desktop direct-command error normalization now maps HTTP 507 `CottonApiException` to the same quota action-required message as `SyncPairRunner`, and maps HTTP 413 to a clear upload-limit message. Focused resolver tests passed 21/21, full `Cotton.Sync.Desktop.Tests` passed 307/307, and full solution Release build passed with 0 warnings.
- [x] Large file upload and download complete.
  Verification 2026-06-03: `SyncClientEndToEndTests.RunOnceAsync_UploadsAndDownloadsLargeFilesThroughSdkAndServer` verifies a binary local file larger than the sync test chunk size uploads through `SyncEngine`/SDK/server, round-trips from the server byte-for-byte, records SQLite baseline hashes, and creates a multi-chunk server manifest. The same test creates a separate multi-chunk remote binary file, syncs it down locally, and verifies byte-for-byte local content plus baseline hashes. Focused server-backed sync E2E tests passed 11/11, and full solution Release build passed with the known NU1903 warning.
- [x] Many small files complete.
  Verification 2026-06-03: `SyncClientEndToEndTests.RunOnceAsync_UploadsManySmallFilesThroughSdkAndServer` verifies 32 small local files across multiple nested folders upload through `SyncEngine`/SDK/server, produce one upload activity per file, preserve remote file contents, and create one SQLite baseline entry per relative path. Focused server-backed sync E2E tests passed 12/12, and full solution Release build passed with the known NU1903 warning.
- [x] Deep nested paths complete or fail with clear path error.
  Verification 2026-06-03: `SyncClientEndToEndTests.RunOnceAsync_SyncsDeepNestedPathsThroughSdkAndServer` verifies 10-level nested local paths upload through `SyncEngine`/SDK/server and 10-level nested remote paths download locally, preserving content and SQLite baselines in both directions. Focused server-backed sync E2E tests passed 13/13, and full solution Release build passed with the known NU1903 warning.
- [x] Windows reserved names are blocked or mapped with clear UX.
  Verification 2026-06-03: `SyncPathTests` cover reserved Windows device names, reserved characters, trailing spaces/dots, segment length, and total relative path length; `SyncClientEndToEndTests.RunOnceAsync_RejectsWindowsReservedLocalFileNameThroughSdkAndServer` verifies a local `Docs/CON.txt` path throws `SyncPathValidationException`, includes the invalid segment/reason, uploads nothing to the server, and records no SQLite baseline. Focused `SyncPathTests` passed 13/13, focused reserved-name E2E passed 1/1, full server-backed sync E2E tests passed 15/15, and full solution Release build passed with the known NU1903 warning.
- [x] Case conflict is detected and explained.
  Verification 2026-06-03: `SyncClientEndToEndTests.RunOnceAsync_RejectsLocalCaseInsensitivePathCollisionThroughSdkAndServer` verifies a case-sensitive local filesystem conflict between `Case/File.txt` and `case/file.txt` throws `SyncPathCollisionException`, includes the colliding paths in the message/properties, uploads nothing to the server, and records no SQLite baseline. Focused server-backed sync E2E tests passed 14/14, and full solution Release build passed with the known NU1903 warning.
- [x] Unicode names sync consistently.
  Verification 2026-06-03: `SyncEngine` has focused Unicode path coverage and `SyncClientEndToEndTests.RunOnceAsync_SyncsUnicodePathsThroughSdkAndServer` verifies Unicode paths through the SDK and integration-test server in both directions: local upload of `Документы/設計-notes.txt` and remote download of `共有/設計-remote.txt`, preserving relative paths and SQLite baselines. Focused server-backed sync E2E tests passed 9/9, and full solution Release build passed with the known NU1903 warning.
- [x] Temporary files are ignored.
  Verification 2026-06-03: `LocalFileScanner` delegates to `LocalFileIgnoreRules`, and `SyncClientEndToEndTests.RunOnceAsync_DoesNotUploadIgnoredTemporaryFiles` verifies Cotton metadata, Office locks, Emacs locks, Vim swap files, browser partial downloads, common temp suffixes, and OS metadata files are not uploaded or recorded in SQLite baseline during a server-backed sync while `Docs/keep.txt` uploads normally. Focused server-backed sync E2E tests passed 10/10, and full solution Release build passed with the known NU1903 warning.
- [x] Locked files retry and eventually sync after unlock.
  Verification 2026-06-03: `SyncPairRunnerTests.SyncNowAsync_RetriesLockedLocalFileAfterItBecomesReadable` verifies app-layer retry after `LocalFileUnavailableException`, and `SyncClientEndToEndTests.SyncPairRunner_UploadsLockedFileAfterItBecomesReadableThroughSdkAndServer` verifies the real `SyncPairRunner` + `SyncEnginePairWork` + SDK/server path retries after a locked local file is released, uploads the file, returns to idle, and records SQLite baseline hashes. Focused runner tests passed 15/15, full server-backed sync E2E tests passed 16/16, and full solution Release build passed with the known NU1903 warning.

## Phase 13 - Performance And Soak

- [x] Define performance targets.
  Verification 2026-06-03: initial smoke targets are now concrete enough to guard regressions without pretending to be final benchmarks: 1,000-file no-op sync must finish under 20 seconds in `Cotton.Sync.Tests`, no remote mutations may occur during no-op, and future release targets must add separate 10k-file, large-file, memory, and UI-responsiveness measurements before Phase 13 is complete.
- [x] Add benchmark or measured smoke for no-op sync.
  Verification 2026-06-03: `SyncEnginePerformanceSmokeTests.RunOnceAsync_NoOpForOneThousandFilesCompletesWithinSmokeTarget` creates 1,000 local files with matching remote snapshots and SQLite baselines, measures only the `SyncEngine.RunOnceAsync` no-op pass, asserts no upload/download/delete activity occurs, keeps all 1,000 baselines, and enforces a 20-second smoke ceiling. Focused no-op performance smoke passed 1/1, full `Cotton.Sync.Tests` passed 92/92, and full solution Release build passed with the known NU1903 warning.
- [x] Add measured smoke for 1,000 small files.
  Verification 2026-06-04: `SyncEnginePerformanceSmokeTests.RunOnceAsync_UploadsOneThousandSmallFilesWithinSmokeTarget` creates 1,000 small local files across 10 folders, measures the initial `SyncEngine.RunOnceAsync` upload pass with real `LocalFileScanner` and EF Core SQLite state, asserts 1,000 upload activities, no downloads/deletes, 1,000 distinct remote file ids in baseline, and enforces a 30-second smoke ceiling. Focused 1,000-file upload smoke passed 1/1, full `Cotton.Sync.Tests` passed 93/93, and full solution Release build passed with the known NU1903 warning.
- [ ] Sync tens of thousands of small files and/or folders without crashing.
  Required scenario: a single sync pair containing tens of thousands of small files and/or deep folder trees (target 10k, 30k, and 50k entries) must complete an initial full-mirror pass and a following no-op pass without crashing, without silent process exit, with bounded memory, and with a responsive UI. Any failure at scale must surface a user-visible error and a diagnostic log entry instead of disappearing. Measure local scan latency, peak memory, and UI/tray responsiveness on Windows.
  Scale invariant: the product must degrade safely for arbitrarily large trees, including hundreds of thousands or millions of entries. A million-file pair may be slow and may require a dedicated release/performance run, but it must not rely on unbounded in-memory result/activity/UI queues, must keep reporting progress or a clear bounded failure, and must move toward paged/streamed reconciliation instead of materializing the entire local tree, remote tree, baseline, and merged key set at once. The difference between 1k, 10k, and 1M entries is throughput and memory pressure, not correctness: no supported file count should silently crash the desktop app.
  Observation 2026-06-06 from Windows manual testing: the desktop app currently crashes silently on a local folder of ~30,000 small files — no error dialog, no notification, no visible diagnostic, the process just exits. Reproduce, capture the crash signature in the rotating trace log, fix the root cause, then add a measured smoke escalating from the existing 1,000-file smoke plus a real Windows desktop run before checking this item. Until fixed this is a release blocker (see Phase 14 "No known crash-on-start bugs" / "No known data-loss bugs").
  Partial 2026-06-06: desktop startup now installs rotating trace logging and global crash reporters before command-line or GUI modes run. `AppDomain.CurrentDomain.UnhandledException` and `TaskScheduler.UnobservedTaskException` write redacted fatal diagnostics into the desktop log without suppressing the fault, giving the next large-folder Windows reproduction a crash signature instead of a silent process exit. Focused diagnostics/startup tests passed 6/6, desktop Release build passed with 0 warnings, and `git diff --check` passed. Keep unchecked until the 30k/50k-file root cause is reproduced, fixed, and measured.
  Partial 2026-06-06: desktop Activity now applies UI backpressure to high-volume successful transfer events (`Uploaded`, `Downloaded`, local/remote deletes). Bursts for the same sync pair update the newest Activity row inside a short coalescing window instead of inserting/removing a row for every file, while conflicts, skipped/action-required events, errors, account, settings, diagnostics, and different sync pairs remain separate. Focused ViewModel tests passed 4/4, desktop Release build passed with 0 warnings, and `git diff --check` passed. Keep unchecked until the measured 30k/50k-file Windows run proves responsive UI and bounded memory.
  Partial 2026-06-06: high-volume Activity backpressure now happens before background-thread events enter the Avalonia UI dispatcher queue. While a coalesced transfer Activity is already scheduled, newer successful transfer events for the same sync pair replace the pending snapshot instead of posting another UI action; different sync pairs and non-transfer events still post separately. Focused ViewModel dispatcher/backpressure tests passed 5/5, desktop Release build passed with 0 warnings, and `git diff --check` passed.
  Partial 2026-06-06: transfer-progress and run-progress updates now use the same latest-wins backpressure before entering the Avalonia UI dispatcher queue. Bursts for the same active transfer or same run-progress stage replace the pending UI snapshot, so large uploads and large-tree scans render the latest progress instead of replaying stale progress frames. Focused progress dispatcher tests passed 6/6, desktop Release build passed with 0 warnings, and `git diff --check` passed.
  Partial 2026-06-06: sync-core no-op performance coverage now escalates beyond the original 1,000-file smoke with `RunOnceAsync_NoOpForThreeThousandFilesCompletesWithinSmokeTarget`. The 3,000-file pass creates matching local files, remote snapshots, and SQLite baselines, then asserts no upload/download/delete activity and a bounded no-op runtime. Focused 1k+3k no-op smoke tests passed 2/2. Keep unchecked until the actual 10k/30k/50k desktop Windows runs prove no crash, bounded memory, and responsive UI.
  Partial 2026-06-06: added an explicit manual `RunOnceAsync_NoOpForTenThousandFilesCompletesWithinManualSmokeTarget` sync-core smoke. It is intentionally not part of the regular fast test run because the 3k smoke already runs near its local timeout in this environment; use the explicit 10k smoke during dedicated release/performance verification before escalating to 30k/50k Windows desktop runs.
  Verification 2026-06-06: after local scan pruning and sequential-scan hash hardening, the explicit manual `RunOnceAsync_NoOpForTenThousandFilesCompletesWithinManualSmokeTarget` sync-core smoke was run by name with `NUnit.Explicit=true` and passed 1/1 in 1m35s on the local Linux environment. This proves the current core no-op path handles 10k matching files under the existing smoke targets; Windows desktop 30k/50k crash reproduction and UI responsiveness remain open.
  Verification 2026-06-06: the explicit manual `RunOnceAsync_NoOpForThirtyThousandFilesCompletesWithinManualSmokeTarget` sync-core smoke was run by name with `NUnit.Explicit=true` and passed 1/1 in 4m43s on the local Linux environment. This raises current core no-op evidence to 30k matching files; it still does not replace the required Windows desktop 30k/50k run because the reported crash involved the packaged GUI process and UI responsiveness.
  Verification 2026-06-07: the explicit manual `RunOnceAsync_NoOpForFiftyThousandFilesCompletesWithinManualSmokeTarget` sync-core smoke was run by name with `NUnit.Explicit=true` and passed 1/1 in 7m48s on the local Linux environment. This raises current core no-op evidence to 50k matching files; it still does not replace the required Windows desktop 30k/50k run because the reported crash involved the packaged GUI process and UI responsiveness.
  Partial 2026-06-06: `SyncRunResult` now bounds retained activity history through `SyncRunOptions.MaximumStoredResultActivities` while still counting `TotalActivityCount`, reporting every live `ActivityProgress` event, and preserving the first action-required message even when it happens after the retained history cap. This removes one unbounded memory path for large initial uploads; it does not yet make million-file trees fully safe because the current engine still materializes local snapshots, remote snapshots, SQLite baselines, dictionaries, and merged path keys for the whole pair. Added explicit manual release-scale smoke gates for 30k/50k no-op runs and 10k/30k initial small-file uploads, plus managed heap delta checks in the existing performance smokes. Focused `SyncRunResult` + 1k/3k no-op + 1k upload smoke tests passed 4/4.
  Partial 2026-06-06: SQLite state loading now streams EF entities into sync baseline models instead of materializing a separate entity list before the model list, and `SyncEngine` no longer copies the whole pair baseline list unless ignored `.cotton` state entries actually need to be filtered out. The 1k upload smoke now also proves engine-level activity capping by retaining only 100 returned activities while receiving all 1,000 live progress events. Full `Cotton.Sync.Tests` passed 159/159.
  Partial 2026-06-06: file reconciliation path merging now uses one sorted key set instead of building a case-insensitive hash set and then a second sorted list for all local/remote/baseline file keys. This removes another full-size transient collection from large no-op and initial-upload passes without changing deterministic reconciliation order. Full `Cotton.Sync.Tests` passed 159/159.
  Partial 2026-06-06: sync-state baselines now persist `LocalSizeBytes` through a generated local EF migration, and baseline comparisons reuse the stored local content hash when metadata proves the local file is unchanged (`LastWriteUtc` and size both match). This avoids re-reading and hashing every unchanged file during no-op passes after one migration/verification run; old rows without size keep the old hash-and-refresh behavior once. `RunOnceAsync_ReusesBaselineHashWhenMetadataIsUnchanged` proves unchanged metadata avoids content hashing, state-store tests round-trip the new size field, full `Cotton.Sync.Tests` passed 160/160, `Cotton.Sync.App.Tests` passed 150/150, and desktop Release build passed with 0 warnings.
  Partial 2026-06-06: `ISyncStateStore` now exposes streamed pair-entry loading, and `SyncEngine` builds file/directory baseline dictionaries directly from that async stream instead of first materializing the whole baseline as a `List<SyncStateEntry>`. `LoadPairAsync` remains as a collect-wrapper for callers/tests that need a list, while the hot sync path proves it uses `LoadPairEntriesAsync`. Focused sync-state/engine tests passed 17/17, full `Cotton.Sync.Tests` passed 162/162, full `Cotton.Sync.App.Tests` passed 150/150, full `Cotton.Sync.Desktop.Tests` passed 393/393, and desktop Release build passed with 0 warnings.
  Partial 2026-06-06: remote Cotton Cloud tree scans now report file-discovery heartbeat progress through an optional remote crawler progress interface, `SyncEngine` maps those samples into existing `ScanningRemote` run progress, and the desktop progress card renders `N cloud files found` plus the current cloud item. Multi-folder remote scans aggregate discovery counts in the top progress card so the app keeps visible progress during cloud preparation as well as local preparation. Focused remote scan tests passed 7/7, full `Cotton.Sync.Tests` passed 165/165, full `Cotton.Sync.Desktop.Tests` passed 396/396, desktop Release build passed with 0 warnings, and `git diff --check` passed.
  Partial 2026-06-06: local and remote tree scans now also emit bounded directory-discovery heartbeat samples, covering directory-heavy sync roots that may not discover a file for a while. The desktop progress text now keeps showing the current scan path even when the discovered file count is still zero, so preparation does not look frozen on large folder trees. Focused directory-heartbeat tests passed 4/4, full `Cotton.Sync.Tests` passed 167/167, full `Cotton.Sync.Desktop.Tests` passed 398/398, desktop Release build passed with 0 warnings, and `git diff --check` passed.
  Partial 2026-06-06: directory reconciliation now reports folder totals and current folder progress instead of reusing the file reconciliation count. The desktop progress card renders `N of M folders` and row operations such as `Preparing folders 3 of 10`, preventing large folder preparation from showing misleading text like `0 of 29185 files`. Focused folder-progress tests passed 2/2, full `Cotton.Sync.Tests` passed 168/168, full `Cotton.Sync.Desktop.Tests` passed 399/399, desktop Release build passed with 0 warnings, and `git diff --check` passed.
  Partial 2026-06-06: high-volume file and folder reconciliation progress is now sampled for large runs instead of reporting before and after every item. Runs with 100 or fewer items keep exact per-item progress; larger runs report first/last and every 100 items, reducing callback pressure for tens of thousands to millions of entries while preserving visible forward motion. Focused throttling tests passed 2/2, full `Cotton.Sync.Tests` passed 170/170, desktop Release build passed with 0 warnings, and `git diff --check` passed.
  Partial 2026-06-06: scan snapshots are now converted to path lookup dictionaries inside a short-lived async helper before `RunOnceAsync` loads baselines and starts reconciliation. This does not yet stream reconciliation, but it prevents the local and remote snapshot list containers from remaining direct `RunOnceAsync` locals throughout the whole pass; the hot path keeps only the lookup dictionaries and remote root node needed for reconciliation. Full `Cotton.Sync.Tests` passed 170/170, desktop Release build passed with 0 warnings, and `git diff --check` passed.
  Partial 2026-06-06: the real local scanner now supports metadata-only scans directly into normalized path lookup dictionaries through `ILocalFileMetadataTreeLookupScanner`, and `SyncEngine` uses that capability on the metadata+lazy-hash hot path. This removes the intermediate `LocalTreeSnapshot` directory/file list containers for the local side before reconciliation while preserving the old scanner API for test doubles and other implementations. Focused lookup tests passed 2/2, full `Cotton.Sync.Tests` passed 172/172, full `Cotton.Sync.App.Tests` passed 150/150, full `Cotton.Sync.Desktop.Tests` passed 399/399, and desktop Release build passed with 0 warnings.
  Partial 2026-06-06: the real remote crawler now supports recursive crawls directly into normalized path lookup dictionaries through `IRemoteTreeLookupCrawler`, and `SyncEngine` uses that capability before falling back to `RemoteTreeSnapshot`. Local and remote lookup insertion now share one `SyncPathLookup` helper so key normalization and collision detection cannot drift. Focused lookup tests passed 4/4, full `Cotton.Sync.Tests` passed 174/174, full `Cotton.Sync.App.Tests` passed 150/150, full `Cotton.Sync.Desktop.Tests` passed 399/399, and desktop Release build passed with 0 warnings.
  Partial 2026-06-07: the real remote crawler now walks paged remote folders depth-first with bounded stack frames instead of accumulating a breadth-first queue of discovered directories. For wide remote trees, page N+1 of a parent is deferred behind the child folders from page N, so the crawler does not need to keep every sibling directory discovered across all parent pages before doing useful subtree work. Focused `RemoteTreeCrawlerTests` passed 7/7, the fast 3k no-op + 1k upload scale smokes passed 2/2, full `Cotton.Sync.Tests` passed 175/175, full `Cotton.Sync.App.Tests` passed 150/150, full `Cotton.Sync.Desktop.Tests` passed 399/399, and desktop Release build passed with 0 warnings.
  Partial 2026-06-06: file and directory reconciliation key sets now deduplicate into a compact `List<string>` backed by a temporary `HashSet` and sort the list once, replacing the previous `SortedSet` path. This keeps deterministic file ordering and depth-first directory ordering while avoiding per-key tree nodes during large sync passes. Focused reconciliation/progress tests passed 12/12, full `Cotton.Sync.Tests` passed 174/174, full `Cotton.Sync.App.Tests` passed 150/150, full `Cotton.Sync.Desktop.Tests` passed 399/399, and desktop Release build passed with 0 warnings.
  Partial 2026-06-07: directory reconciliation no longer mirrors every remote directory id into a second dictionary before creating local-only folders remotely. Parent lookup now reads from the existing mutable remote-directory lookup and falls back to the remote root id for top-level folders, removing one full-size directory map from large folder-heavy passes. Focused directory reconciliation tests passed 5/5, full `Cotton.Sync.Tests` passed 174/174, full `Cotton.Sync.App.Tests` passed 150/150, full `Cotton.Sync.Desktop.Tests` passed 399/399, and desktop Release build passed with 0 warnings.
  Partial 2026-06-07: directory delete reconciliation now reuses the existing depth-sorted `directoryPathKeys` and iterates depth groups from deepest to shallowest instead of sorting baseline directory keys into a second list. Non-baseline directory paths are skipped, preserving delete semantics while removing another full-size key list from directory-heavy passes. Focused directory reconciliation/delete tests passed 6/6, full `Cotton.Sync.Tests` passed 174/174, full `Cotton.Sync.App.Tests` passed 150/150, full `Cotton.Sync.Desktop.Tests` passed 399/399, and desktop Release build passed with 0 warnings.
  Partial 2026-06-07: reconciliation key building now has a single-source fast path. When only one of local, remote, or baseline key sets is non-empty (for example, a first local-only upload into an empty cloud folder), `SyncEngine` copies and sorts that key source directly instead of allocating a full-size `HashSet` just to deduplicate already-unique dictionary keys. Multi-source reconciliation also pre-sizes its temporary key list and dedupe set from the largest known source to reduce resize churn. Focused 1k upload + 3k no-op scale tests passed 2/2, full `Cotton.Sync.Tests` passed 174/174, full `Cotton.Sync.App.Tests` passed 150/150, full `Cotton.Sync.Desktop.Tests` passed 399/399, and desktop Release build passed with 0 warnings.
  Partial 2026-06-07: first-run reconciliation now skips baseline-only passes when there is no baseline. Local content hash refresh iterates only existing file state entries instead of every merged file key, and delete-guard planning returns an empty guard immediately when both file and directory baseline dictionaries are empty. This removes two full key passes from large initial uploads where deletes are impossible by definition. Focused baseline/scale tests passed 3/3, full `Cotton.Sync.Tests` passed 174/174, full `Cotton.Sync.App.Tests` passed 150/150, full `Cotton.Sync.Desktop.Tests` passed 399/399, and desktop Release build passed with 0 warnings.
  Partial 2026-06-07: file delete-guard planning now iterates baseline file state entries directly instead of scanning every merged file key and discarding local-only or remote-only files that cannot be deleted safely without a baseline. This preserves delete-limit semantics while removing another full pass over large mixed trees with many new files. Focused delete/matrix/scale tests passed 11/11, full `Cotton.Sync.Tests` passed 174/174, full `Cotton.Sync.App.Tests` passed 150/150, full `Cotton.Sync.Desktop.Tests` passed 399/399, and desktop Release build passed with 0 warnings.
  Verification 2026-06-07: after the initial-upload key/pass reductions, the explicit manual `RunOnceAsync_UploadsTenThousandSmallFilesWithinManualSmokeTarget` sync-core smoke was run by name with `NUnit.Explicit=true` and passed 1/1 in 1m35s on the local Linux environment. This raises current core initial-upload evidence to 10k small files; Windows desktop 30k/50k runs and the 30k initial-upload explicit smoke remain open.
  Remaining scale debt: move the sync engine toward paged/streamed reconciliation so extremely large pairs (hundreds of thousands to millions of files) do not require holding all local, remote, baseline, and path-key records at once. Until that architectural step and a Windows run prove bounded memory/responsive UI, this item remains open.
- [x] Add measured smoke for one large file.
  Verification 2026-06-04: `SyncEnginePerformanceSmokeTests.RunOnceAsync_UploadsOneLargeFileWithinSmokeTarget` writes one deterministic 8 MiB local file, measures the initial `SyncEngine.RunOnceAsync` upload pass with real `LocalFileScanner` hashing and EF Core SQLite state, verifies one upload, no downloads/deletes, matching local and remote baseline hashes, and enforces a 15-second smoke ceiling. Focused large-file upload smoke passed 1/1, full `Cotton.Sync.Tests` passed 94/94, and full solution Release build passed with the known NU1903 warning.
- [ ] Add a real desktop large-file upload smoke on Windows.
  Required scenario: upload at least one 100 MiB local file through the running Windows desktop app against a public instance, then repeat with at least one 10 GiB local file only against a public/dedicated backend account with enough quota. The public-instance run must use an account/instance with at least 100 GiB available quota so the test measures client behavior instead of quota exhaustion. Record pre-upload scan latency, average upload throughput, UI responsiveness, tray visibility, server convergence, and final local SQLite baseline state.
- [x] Optimize first-upload large-file pipeline to avoid a separate full-file pre-hash pass.
  Required behavior: for a local-only upload candidate, stream the file once, compute per-chunk hashes, upload missing chunks, and compute the full-file content hash in the same pass before `CreateFromChunks`. Keep full-content hashes for baseline/conflict correctness, but do not read a new file twice when the upload path can prove all required hashes from one stream.
  Verification 2026-06-06: `LocalFileScanner` now supports metadata-only tree scans plus lazy content hashing, `SyncEngine` uses metadata-only scans when the scanner supplies a hasher, local-only first uploads are passed to the remote uploader without a precomputed content hash, and `SdkRemoteFileSynchronizer` computes the full content hash during the same chunk-upload stream used for `CreateFromChunks`. Existing baseline/remote comparison paths still lazy-hash local content before delete guards and conflict decisions. Focused regression tests passed 4/4, full `Cotton.Sync.Tests` passed 152/152, full `Cotton.Sync.App.Tests` passed 147/147, full `Cotton.Sync.Desktop.Tests` passed 354/354, and `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings.
  Partial 2026-06-07: `SdkRemoteFileSynchronizer` now passes the already-built chunk-hash list directly into `CreateFileFromChunksRequestDto` instead of copying it with `ToList()`. The ordered chunk manifest is still required by the server, but large uploads no longer keep a second identical list right before file creation/update. Focused remote upload tests passed 10/10.
- [x] Add measured large-file pipeline regression coverage.
  Required checks: one large upload should report time spent in local metadata scan separately from content transfer, and the measured smoke should fail if a local-only large upload regresses to an avoidable double full-file read.
  Verification 2026-06-06: `SyncEnginePerformanceSmokeTests.RunOnceAsync_UploadsOneLargeFileWithinSmokeTarget` now captures run-progress samples for the local metadata scan, records elapsed time to the first upload call, and asserts the local-only upload enters `IRemoteFileSynchronizer.UploadFileAsync` with an empty `ContentHash`. This fails the smoke if the large first-upload path regresses to an eager full-file pre-hash before upload while still verifying final baseline hashes after the upload stream computes content hash. Focused large-file performance smoke passed 1/1.
- [x] Review native desktop transfer parallelism.
  Required scope: evaluate safe bounded parallelism for chunk existence checks and chunk uploads, while preserving file order for manifest creation, respecting server limits, keeping disk reads sequential where that is faster, and avoiding unbounded memory buffering.
  Verification 2026-06-06: `SdkRemoteFileSynchronizer` now keeps local file reads sequential but batches already-read chunk existence/upload network work with `SdkRemoteFileSynchronizerOptions.MaxConcurrentChunkUploads` defaulting to 3. The upload path preserves manifest chunk order, bounds in-memory buffered chunks, and deduplicates repeated chunk hashes within one file so identical chunks are checked/uploaded once while still appearing multiple times in `CreateFromChunks`. Focused remote synchronizer tests passed 10/10.
- [x] Add measured smoke for remote changes catch-up through cursor.
  Verification 2026-06-04: `RemoteChangeFeedReaderTests.ReadAsync_CatchesUpFiveThousandRemoteChangesWithinSmokeTarget` replays 5,000 remote changes through 10 pages of 500 changes, acknowledges each page through `RemoteChangeFeedReader`, verifies saved SQLite cursor advances to 5,000, verifies page requests use the expected since-cursors and limits, and enforces a 10-second smoke ceiling. Focused cursor catch-up smoke passed 1/1, full `Cotton.Sync.Tests` passed 95/95, and full solution Release build passed with the known NU1903 warning.
- [ ] Run 24-hour soak test with one client.
- [ ] Run 24-hour soak test with two clients.
  Partial 2026-06-05: extended `Cotton.Sync.Cli sync-soak` with optional second-client arguments: `--second-local-root`, `--second-sync-pair`, and `--second-database`. In two-client mode the harness runs client A, then client B, aggregates activity/state-entry totals, and requires a final convergence pass across both clients before returning success. Verification: focused `sync-soak` tests passed 6/6, full `Cotton.Sync.Cli.Tests` passed 18/18, and `dotnet build src/Cotton.sln --configuration Release --no-restore` passed with 0 warnings. Keep unchecked until the actual 24-hour two-client soak run completes and records memory, CPU, failures, and final convergence.
  Partial 2026-06-05: two-client `sync-soak` now rejects shared or nested local roots, duplicate sync-pair ids, and duplicate sync-state databases before sign-in, preventing the soak harness from producing false convergence by accidentally reusing one client's state. Verification: focused soak tests passed 7/7.
- [ ] Record memory growth, CPU usage, sync errors, and final convergence.

## Phase 14 - Release Readiness Gate

Release can be considered only when every item in this section is checked.

- [ ] Full solution Release build passes.
  Partial 2026-06-04: desktop dependency cleanup was corrected to upgrade Avalonia to 12.0.4 instead of pinning runtime-incompatible `Tmds.DBus.Protocol` 0.94.1 on Avalonia 11. `dotnet test src/Cotton.Sync.Desktop.Tests/Cotton.Sync.Desktop.Tests.csproj --configuration Release` passed 136/136, `dotnet build src/Cotton.sln --configuration Release` passed with 0 warnings, Linux Xvfb GUI startup no longer throws `Tmds.DBus.Protocol.Connection` `TypeLoadException`, and current setup/sign-in screenshots were captured. Keep unchecked until the full release gate is re-run as a final clean-machine build.
  Partial 2026-06-04: `Cotton.Sync.Desktop.csproj` now removes the target publish directory before `PrepareForPublish`, preventing stale runtime DLLs from surviving package version changes. Local verification reproduced the stale `Tmds.DBus.Protocol.Connection` crash in the previous publish output, then republished cleanly with `checksums.sha256` generation and captured a Linux Xvfb GUI screenshot from the fresh publish output.
  Partial 2026-06-04: scanned all 28 `.csproj` files with `dotnet list <project> package --vulnerable --include-transitive`; vulnerability findings were 0. The solution-level command itself is not usable because `src/cotton.client/cotton.client.esproj` uses package.config-style NuGet metadata, so the per-project scan is the current .NET package evidence.
  Partial 2026-06-04: full local `dotnet test src/Cotton.sln --configuration Release --no-restore` passed after merging current `origin/develop` into `feature/desktop-sync-client`. Notable suites included `Cotton.Sync.Tests` 124/124, `Cotton.Sync.App.Tests` 114/114, `Cotton.Sync.Desktop.Tests` 254/254, `Cotton.Sdk.Tests` 40/40, `Cotton.Sync.Cli.Tests` 9/9, and `Cotton.Server.IntegrationTests` 365/365. Keep unchecked until the final clean-machine release gate is re-run.
  Partial 2026-06-04: full local `dotnet test src/Cotton.sln --configuration Release --no-restore` passed on `feature/desktop-sync-client` after the current desktop progress, CLI soak, and sync-state cursor migration coverage. Notable suites included `Cotton.Sync.Tests` 132/132, `Cotton.Sync.App.Tests` 121/121, `Cotton.Sync.Desktop.Tests` 277/277, `Cotton.Sdk.Tests` 40/40, `Cotton.Sync.Cli.Tests` 12/12, and `Cotton.Server.IntegrationTests` 369/369. Keep unchecked until the final clean-machine release gate is re-run.
  Partial 2026-06-06: full local `dotnet build src/Cotton.sln --configuration Release --no-restore` passed on `feature/desktop-sync-client` with 0 warnings and 0 errors after the latest desktop dashboard layout and sync performance-smoke slices. Keep unchecked until final clean-machine package and test gates are re-run.
  Partial 2026-06-04: full local `dotnet test src/Cotton.sln --configuration Release --no-restore` passed after the latest desktop packaging and action-required hardening. Notable suites included `Cotton.Sync.Tests` 134/134, `Cotton.Sync.App.Tests` 121/121, `Cotton.Sync.Desktop.Tests` 290/290, `Cotton.Sdk.Tests` 40/40, `Cotton.Sync.Cli.Tests` 16/16, `Cotton.Storage.Tests` 97/97, and `Cotton.Server.IntegrationTests` 373/373. `dotnet build src/Cotton.sln --configuration Release --no-restore` also passed with 0 warnings. Keep unchecked until the final clean-machine release gate is re-run.
  Partial 2026-06-06: full local `dotnet build src/Cotton.sln --configuration Release --no-restore` passed on `feature/desktop-sync-client` with 0 warnings and 0 errors after aggregate desktop transfer-progress verification. Keep unchecked until the final clean-machine release gate is re-run.
- [ ] SDK tests pass.
  Partial 2026-06-06: local `dotnet test src/Cotton.Sdk.Tests/Cotton.Sdk.Tests.csproj --configuration Release --no-restore` passed 43/43 on `feature/desktop-sync-client`. Keep unchecked until the final release gate is re-run.
- [ ] Sync core tests pass.
  Partial 2026-06-06: local `dotnet test src/Cotton.Sync.Tests/Cotton.Sync.Tests.csproj --configuration Release --no-restore` passed 158/158 on `feature/desktop-sync-client`. The first attempt overlapped another build and hit a compiler output file lock, then the standalone rerun passed. Keep unchecked until the final release gate is re-run.
- [ ] Server integration tests for sync endpoints pass.
- [ ] Desktop build passes.
  Partial 2026-06-06: local `dotnet build src/Cotton.Sync.Desktop/Cotton.Sync.Desktop.csproj --configuration Release --no-restore` passed with 0 warnings and 0 errors, and `Cotton.Sync.Desktop.Tests` passed 387/387 after the aggregate transfer-progress change. Keep unchecked until clean-machine package smoke is complete.
- [ ] CLI build passes.
  Partial 2026-06-06: local `dotnet build src/Cotton.Sync.Cli/Cotton.Sync.Cli.csproj --configuration Release --no-restore` passed with 0 warnings and 0 errors. Keep unchecked until the final release gate is re-run.
- [ ] Packaged app smoke passes on clean Windows VM.
- [ ] Packaged app smoke passes on clean Linux VM.
  Partial 2026-06-04: local published Linux artifact self-test passed from `src/Cotton.Sync.Desktop/bin/Release/net10.0/publish/linux-x64/Cotton.Sync.Desktop --self-test --data-dir <temp>`, including preferences DB, sync-pair DB, sync-state DB, authentication state, desktop icon, Linux platform capabilities, notification adapter, file watcher, and server identity checks. This predated the stricter release-secure token-storage gate; current Linux environments without `secret-tool` intentionally fail self-test on `Token storage`. Keep unchecked until a clean Linux VM/package smoke run includes Secret Service capability and passes.
- [ ] End-to-end sync matrix passes.
- [ ] UI screenshot review passes.
- [ ] Tray/autostart lifecycle verification passes.
- [ ] Secure token storage verification passes.
- [ ] Diagnostics export verification passes.
- [ ] No known data-loss bugs remain open.
- [ ] No known crash-on-start bugs remain open.
- [ ] No known broken-login bugs remain open.
- [ ] No known package-install failure remains open.
- [ ] Release notes are written.
  Partial 2026-06-04: created `notes/desktop-sync-release-notes-draft.md` with the current release story, artifact matrix, verified evidence, open release gates, and explicit non-goals. Keep unchecked until clean Windows/Linux VM smoke, screenshot review, soak tests, final artifact checksums, and final release diff review are complete.
- [ ] Checksums are generated.
  Partial 2026-06-04: current local package outputs were generated with `checksums.sha256`, and the Linux `.deb` plus Windows `.zip` packaging scripts now refuse publish directories without that file. Keep unchecked until final release artifacts are generated by the release workflow and their uploaded checksums are reviewed.
  Partial 2026-06-04: CI now generates a single top-level `release-artifact-checksums.sha256` file in the release job for the NuGet package and all desktop release artifacts, including the Windows installer. Keep unchecked until the final release workflow output is reviewed.
- [ ] Final release branch diff is reviewed.

## Future Platform Features

These are intentionally outside the first releasable full-mirror sync product.

- [ ] Windows virtual files/placeholders through Cloud Files API.
- [ ] Linux virtual files strategy through FUSE or desktop portal research.
- [ ] Selective sync.
- [ ] Bandwidth limits.
- [ ] Multiple accounts.
- [ ] Browser-based OAuth/PKCE desktop login.
- [ ] Auto-update implementation.
- [ ] File manager overlay icons.
- [ ] macOS support.
- [ ] Mobile support.

## Working Rules For This Plan

- [ ] Prefer finishing one phase slice with tests over spreading partial work across many phases.
- [ ] Keep commits small and phase-scoped.
- [ ] Update this plan whenever scope changes.
- [ ] Add a verification note next to any checkbox before marking it complete.
- [ ] Do not remove hard tasks from the plan just because they are uncomfortable.
- [ ] Do not ship prototype-only shortcuts in the final release.
